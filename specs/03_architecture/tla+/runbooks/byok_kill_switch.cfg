\* CoreLink WI-S20-007 — byok_kill_switch TLC config (runbook 3/4).
\* TLC v1.8.0 SHA-256 pinned (ADR-0042 §A1).
\* CI runtime target: ≤ 60s on ubuntu-latest (4 vCPU; -workers auto).

CONSTANTS
  Tenants  = {"t1"}
  SlaTicks = 2
  MaxTicks = 4

SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoEncryptAfterRevoke
  DekCacheCleanOnRevoke
  DegradeWithinSLA

PROPERTIES
  RevokeLeadsToDegrade
  ReEnableRestoresActive

CHECK_DEADLOCK FALSE
