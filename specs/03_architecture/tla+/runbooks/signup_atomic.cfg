\* CoreLink WI-S20-007 — signup_atomic TLC config (runbook 1/4).
\* TLC v1.8.0 SHA-256 pinned (ADR-0042 §A1).
\* CI runtime target: ≤ 60s on ubuntu-latest (4 vCPU; -workers auto).

CONSTANTS
  MaxAttempts = 2

SPECIFICATION Spec

INVARIANTS
  TypeOK
  AtomicSignup
  NoOrphanIdentity
  NoOrphanBilling
  DPAFirstHolds

PROPERTIES
  EventuallyTerminates

CHECK_DEADLOCK FALSE
