\* CoreLink WI-S20-007 — dpa_versioning_grace TLC config (runbook 2/4).
\* TLC v1.8.0 SHA-256 pinned (ADR-0042 §A1).
\* CI runtime target: ≤ 60s on ubuntu-latest (4 vCPU; -workers auto).

CONSTANTS
  Tenants   = {"t1", "t2"}
  Versions  = {1, 2}
  GraceLen  = 2
  MaxClock  = 4
  MaxWrites = 2

SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoSilentLockout
  ConsentChainHolds
  NoWriteAfterDegrade
  GraceWindowMonotone
  ReAcceptRestoresAccess

CHECK_DEADLOCK FALSE
