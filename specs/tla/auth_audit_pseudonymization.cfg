(*
 * TLC config for auth_audit_pseudonymization.tla (PR lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_audit_pseudonymization
 *
 * Closes INV-AUTH-AUDIT-PSEUDONYMIZATION (CRITICAL sec 3.14).
 * DEBT-005 batch 6 FINAL.
 *
 * Bounds (TLC < 30 s):
 *   - 3 subjects, MaxOps = 8
 *)

SPECIFICATION Spec

CONSTANTS
    Subjects = {s1, s2, s3}
    MaxOps   = 8

INVARIANT
    InvAuthAuditPseudonymization
    InvAuditChainErasureSafe

CHECK_DEADLOCK FALSE
