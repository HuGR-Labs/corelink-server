(*
 * TLC config for offboarding_audit_complete.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh offboarding_audit_complete offboarding_audit_complete_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 tenants
 *   - GraceThreshold = 3
 *   - MaxOps = 20
 *)

SPECIFICATION Spec

CONSTANTS
    Tenants        = {t1, t2, t3}
    GraceThreshold = 3
    MaxOps         = 20

INVARIANT
    InvAuditPairsTransition
    InvAuditRowsAreValidTransitions
    InvGraceRespected

CHECK_DEADLOCK FALSE
