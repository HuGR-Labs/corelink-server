(*
 * TLC config for consent_proof_verifiable.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh consent_proof_verifiable consent_proof_verifiable_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 subjects x 3 notices, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    Subjects = {s1, s2, s3}
    Notices  = {n1, n2, n3}
    MaxOps   = 16

INVARIANT
    InvConsentProofVerifiable
    InvVerifierSound

CHECK_DEADLOCK FALSE
