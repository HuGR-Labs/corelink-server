(*
 * TLC config for sub_processor_audit_fail_closed.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh sub_processor_audit_fail_closed sub_processor_audit_fail_closed_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 op kinds x 2 sub-processors, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    OpKinds = {"publish", "record_change", "file_objection"}
    MaxOps  = 16

INVARIANT
    InvSubProcessorAuditFailClosed
    InvAuditBeforeMutate

CHECK_DEADLOCK FALSE
