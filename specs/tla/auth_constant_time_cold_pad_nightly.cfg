(*
 * TLC config for auth_constant_time_cold_pad.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_constant_time_cold_pad auth_constant_time_cold_pad_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 4 cold-path failure kinds, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    FailureKinds = {"parse_fail", "sig_mismatch", "token_id_absent", "hash_mismatch"}
    MaxOps       = 16

INVARIANT
    InvAuthConstantTimeColdPad
    InvColdPathResultUniform

CHECK_DEADLOCK FALSE
