(*
 * TLC config for audit_emit_atomic.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh audit_emit_atomic audit_emit_atomic_nightly
 *
 * Extended bounds:
 *   - 5 handlers
 *   - MaxOps = 14
 *)

SPECIFICATION Spec

CONSTANTS
    Handlers = {h1, h2, h3, h4, h5}
    MaxOps   = 14

INVARIANT
    InvAtomicPairing
    InvOutcomeConsistency
    InvNoOrphanOutbox
    InvNoOrphanDomain

CHECK_DEADLOCK FALSE
