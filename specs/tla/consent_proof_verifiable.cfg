(*
 * TLC config for consent_proof_verifiable.tla (PR lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh consent_proof_verifiable
 *
 * Closes INV-CONSENT-PROOF-VERIFIABLE (CRITICAL §3 row 171).
 * DEBT-005 batch 6 FINAL.
 *
 * Bounds (TLC < 30 s):
 *   - 2 subjects × 2 notices, MaxOps = 8
 *)

SPECIFICATION Spec

CONSTANTS
    Subjects = {s1, s2}
    Notices  = {n1, n2}
    MaxOps   = 8

INVARIANT
    InvConsentProofVerifiable
    InvVerifierSound

CHECK_DEADLOCK FALSE
