(*
 * TLC config for gc_reachable_set_complete.tla (PR lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh gc_reachable_set_complete
 *
 * Closes INV-GC-REACHABLE-SET-COMPLETE (CRITICAL §3 row 349).
 * DEBT-005 batch 6 FINAL.
 *
 * Bounds (TLC < 30 s):
 *   - 3 digests, MaxOps = 12
 *)

SPECIFICATION Spec

CONSTANTS
    Digests = {d1, d2, d3}
    MaxOps  = 12

INVARIANT
    InvGcReachableSetComplete
    InvReachableSetMonotone

CHECK_DEADLOCK FALSE
