(*
 * TLC config for auth_schema_rls_default_on.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_schema_rls_default_on auth_schema_rls_default_on_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 tables, MaxOps = 14
 *)

SPECIFICATION Spec

CONSTANTS
    Tables = {tbl_users, tbl_sessions, tbl_pats}
    Forms  = {"plaintext", "encrypted"}
    MaxOps = 14

INVARIANT
    InvSchemaRlsDefaultOn
    InvPiiEncrypted

CHECK_DEADLOCK FALSE
