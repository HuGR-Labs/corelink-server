(*
 * TLC config for auth_constant_time_cold_pad.tla (PR lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_constant_time_cold_pad
 *
 * Closes INV-AUTH-CONSTANT-TIME-COLD-PAD (CRITICAL sec 3.14).
 * DEBT-005 batch 6 FINAL.
 *
 * Bounds (TLC < 30 s):
 *   - 4 cold-path failure kinds, MaxOps = 8
 *)

SPECIFICATION Spec

CONSTANTS
    FailureKinds = {"parse_fail", "sig_mismatch", "token_id_absent", "hash_mismatch"}
    MaxOps       = 8

INVARIANT
    InvAuthConstantTimeColdPad
    InvColdPathResultUniform

CHECK_DEADLOCK FALSE
