(*
 * TLC config for gc_reachable_set_complete.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh gc_reachable_set_complete gc_reachable_set_complete_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 4 digests, MaxOps = 20
 *)

SPECIFICATION Spec

CONSTANTS
    Digests = {d1, d2, d3, d4}
    MaxOps  = 20

INVARIANT
    InvGcReachableSetComplete
    InvReachableSetMonotone

CHECK_DEADLOCK FALSE
