(*
 * TLC config for auth_webauthn_uv_admin.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_webauthn_uv_admin auth_webauthn_uv_admin_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 users, 2 scopes, MaxOps = 14
 *)

SPECIFICATION Spec

CONSTANTS
    Users  = {u1, u2, u3}
    Scopes = {"admin", "member"}
    MaxOps = 14

INVARIANT
    InvAttestationVerified
    InvAdminUvRequired

CHECK_DEADLOCK FALSE
