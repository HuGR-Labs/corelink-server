(*
 * TLC config para gc_correctness.tla
 *
 * Model checking run:
 *   cd specs/tla
 *   tlc -config gc_correctness.cfg -deadlock gc_correctness.tla
 *
 * Endereça INV-GC-001 + INV-GC-004 (CRITICAL em invariant_registry §3.4).
 * Evidence: EVT-022 (CTRL-FORMAL-001, FF-HR-011).
 *)

SPECIFICATION Spec

CONSTANTS
    Blobs = {b1, b2}
    AC_Entries = {e1}
    MaxTime = 8
    GracePeriod = 3

INVARIANT
    InvGCReachableNeverDeleted
    InvGCReRefProtected

CHECK_DEADLOCK FALSE
