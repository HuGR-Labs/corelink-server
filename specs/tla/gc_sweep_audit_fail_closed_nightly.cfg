(*
 * TLC config for gc_sweep_audit_fail_closed.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh gc_sweep_audit_fail_closed gc_sweep_audit_fail_closed_nightly
 *
 * Extended bounds:
 *   - 3 sweep ops, 3 reconcile ops
 *   - MaxOps = 18
 *)

SPECIFICATION Spec

CONSTANTS
    SweepOps     = {s1, s2, s3}
    ReconcileOps = {r1, r2, r3}
    MaxOps       = 18

INVARIANT
    InvSweepAtomicPairing
    InvSweepOutcomeConsistent
    InvReconcileAtomicPairing
    InvReconcileOutcomeConsistent
    InvNoOrphanSweepAudit
    InvNoOrphanReconcileAudit

CHECK_DEADLOCK FALSE
