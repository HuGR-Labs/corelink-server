(*
 * TLC config for cas_immutability.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh cas_immutability cas_immutability_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 4 byte sets, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    ByteSets = {b1, b2, b3, b4}
    MaxOps   = 16

INVARIANT
    InvImmutability
    InvIdempotency
    InvCorrectness

CHECK_DEADLOCK FALSE
