(*
 * TLC config for sub_processor_audit_fail_closed.tla (PR lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh sub_processor_audit_fail_closed
 *
 * Closes INV-SUB-PROCESSOR-AUDIT-FAIL-CLOSED (CRITICAL sec 3 row 410).
 * DEBT-005 batch 6 FINAL.
 *
 * Bounds (TLC < 30 s):
 *   - 3 op kinds x 2 sub-processors, MaxOps = 8
 *)

SPECIFICATION Spec

CONSTANTS
    OpKinds = {"publish", "record_change", "file_objection"}
    MaxOps  = 8

INVARIANT
    InvSubProcessorAuditFailClosed
    InvAuditBeforeMutate

CHECK_DEADLOCK FALSE
