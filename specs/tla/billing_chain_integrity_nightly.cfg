(*
 * TLC config for billing_chain_integrity.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh billing_chain_integrity billing_chain_integrity_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 payload alphabet
 *   - MaxLen = 6
 *   - MaxOps = 10
 *)

SPECIFICATION Spec

CONSTANTS
    Payloads = {p1, p2, p3}
    MaxLen   = 6
    MaxOps   = 10

INVARIANT
    InvChainExtendOnly
    InvChainHeadVerifiable
    InvNoFork

CHECK_DEADLOCK FALSE
