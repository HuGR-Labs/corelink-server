(*
 * TLC config for auth_audit_pseudonymization.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_audit_pseudonymization auth_audit_pseudonymization_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 4 subjects, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    Subjects = {s1, s2, s3, s4}
    MaxOps   = 16

INVARIANT
    InvAuthAuditPseudonymization
    InvAuditChainErasureSafe

CHECK_DEADLOCK FALSE
