(*
 * TLC config for obs_no_pii.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh obs_no_pii obs_no_pii_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 5 events
 *   - MaxOps = 14
 *)

SPECIFICATION Spec

CONSTANTS
    Events = {e1, e2, e3, e4, e5}
    MaxOps = 14

INVARIANT
    InvNoPiiInSink
    InvSinkEqAdmitted
    InvPendingNotInSink

CHECK_DEADLOCK FALSE
