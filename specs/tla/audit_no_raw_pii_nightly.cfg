(*
 * TLC config for audit_no_raw_pii.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh audit_no_raw_pii audit_no_raw_pii_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 payload kinds, MaxOps = 16
 *)

SPECIFICATION Spec

CONSTANTS
    PayloadKinds = {"clean", "pii_raw", "body_bytes"}
    MaxOps       = 16

INVARIANT
    InvAuditNoRawPii
    InvNoPiiInLogs
    InvNoBodyInLogs

CHECK_DEADLOCK FALSE
