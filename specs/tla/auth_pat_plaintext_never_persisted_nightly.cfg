(*
 * TLC config for auth_pat_plaintext_never_persisted.tla (NIGHTLY lane).
 *
 * Run:
 *   bash scripts/run_tlc_corelink.sh auth_pat_plaintext_never_persisted auth_pat_plaintext_never_persisted_nightly
 *
 * Extended bounds (TLC < 5 min CI):
 *   - 3 tenants, 3 PAT ids, MaxOps = 14
 *)

SPECIFICATION Spec

CONSTANTS
    Tenants = {t1, t2, t3}
    PatIds  = {p1, p2, p3}
    MaxOps  = 14

INVARIANT
    InvPatStoreNoPlaintext
    InvAuditNoPlaintext

CHECK_DEADLOCK FALSE
