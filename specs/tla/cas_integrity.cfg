(*
 * TLC config para cas_integrity.tla
 *
 * Model checking run:
 *   cd specs/tla
 *   tlc -config cas_integrity.cfg -deadlock cas_integrity.tla
 *
 * Endereça INV-CAS-INTEGRITY + INV-CAS-IDEMPOTENCY + INV-CAS-IMMUTABILITY
 * (invariant_registry §3.2, todas CRITICAL).
 * Evidence: EVT-022 (CTRL-FORMAL-001).
 *)

SPECIFICATION Spec

CONSTANTS
    Bodies = {b1, b2}
    Digests = {d1, d2, d_wrong}
    Clients = {c1}
    MaxOps = 3

INVARIANT
    InvCASIntegrity
    InvCASImmutability
    InvPoisoningRejected
    InvClientVerifyDetectsRot

CHECK_DEADLOCK FALSE
