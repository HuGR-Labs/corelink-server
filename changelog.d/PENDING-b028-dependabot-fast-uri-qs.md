### Security

- **Patch the newly published `fast-uri` and `qs` Dependabot advisories in a
  narrow B-028 lockfile update.** The affected transitive resolutions move from
  `fast-uri` 3.1.5 to 3.1.6 and `qs` 6.15.2 to 6.16.0. The three separately
  tracked high alerts for unpatched `image-size` and `extract-zip` remain open
  and are not dismissed.
