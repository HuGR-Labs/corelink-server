- Add disjoint `ostore`/`oaccounting` phase attribution across adapter D1 and
  CAS/R2 work, scoped ledger propagation across blocking tasks, and a
  fail-closed owner packet for the remaining production performance evidence.
