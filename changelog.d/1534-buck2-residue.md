### Fixed

- **O material publicado deixa de vender Buck2 como cliente suportado (B-094).** Buck2 fala REAPI **só sobre gRPC**, e o servidor gRPC foi removido (`main.rs:12-17` — servia apenas o Health service, com o router composto DESCARTADO); `bazel_v2.rs:331` admite no código que *"Buck2 cannot use these routes"*. Mesmo assim a superfície publicada continuava prometendo Buck2 na tabela de preços, nas **6** páginas de comparação com concorrentes, na referência REAPI, no `og:description` do site e no diagrama do `README.md`. **Triado, não só contado:** das 250 posições em 119 arquivos, **114 eram falsas** (reescritas), **53 já estavam retratadas** e **83 são legítimas e não foram tocadas** — `04-buck2-quickstart.mdx` existe justamente para dizer que Buck2 não é suportado, `vs-bazel-remote-s3.mdx:135` descreve o **concorrente**, e `styles/`/`accept.txt` são listas de vocabulário do linter que precisam da palavra. Onde havia um rol de clientes entrou o **critério** ("qualquer cliente REAPI que fale HTTP/REST"), que é verificável sem afirmar nada sobre Please/BuildStream/recc — não auditados, e registrados como resíduo NÃO medido. Os *"Buck2/Pants no roadmap"* do `lighthouse-kit/` saíram por contradizerem a posição honesta do próprio repo (upstream `facebook/buck2#459`, fechado `wontfix`). Locales decididos por **amostragem de prosa**, não pelo banner `i18n:MT`: os stubs em inglês verbatim foram espelhados, e `intro.md` / `06-verify-cache-hit.mdx` / `reference/reapi/index.mdx` / `calculator.mdx` — traduções de verdade e **defasadas**, ainda afirmando o que o inglês já retratara — foram traduzidas. ⚠️ Correção ao enunciado do item: `apps/docs/docs/pricing/index.mdx` tem `draft: true` e **não é publicada**, então o checkmark citado como "pior caso da superfície publicada" não estava no ar; corrigido assim mesmo. O item fecha com `verify` de **polaridade INVERTIDA** (verde limpo, vermelho se a alegação voltar), com controle positivo, piso de corpus e piso de retratação — medido dos dois lados: 0 na árvore corrigida, 100 na base `0ec772dc`.

- **O guard de B-094 também cobre o metadado global que o Docusaurus publica.** `apps/docs/docusaurus.config.ts` agora entra no inventário e hash, mas TypeScript de implementação continua fora do corpus por desenho. Variantes positivas que antes escapavam — *offers/provides/has native integration*, *first-class integration* e as duas direções de *use … as a remote cache* — agora ficam vermelhas mesmo depois de rederivar o manifesto; `e.g.` também não é mais uma exceção ampla. A única contraexemplo instrucional preservado é caminho+linha exatos no template de suporte, marcado como algo que agentes não podem enviar; negativas e contexto de protocolo/concorrente continuam válidos. A exclusão no verificador de B-153 e a allowlist de gitleaks permanecem limitadas ao caminho exato do manifesto gerado, com testes que impedem ampliar essas exceções por basename.
