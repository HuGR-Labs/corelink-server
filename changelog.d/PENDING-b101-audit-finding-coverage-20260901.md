### Added

- **B-101 now has a deterministic finding-to-decision coverage gate.** It parses the three named historical audits, requires every live finding to map to an existing canonical backlog item or an explicit decline with its accepted price, and runs on the self-hosted CI fleet.
