### Added

- **22 achados medidos entram no `BACKLOG.md` como itens rastreados (B-144…B-165).** Estavam fora do registro — e a regra da casa é que achado sem item não volta. Inclui dois casos de documentação publicada instruindo vazamento de credencial (o `.bazelrc` do Bazel e o `HOMEBREW_DOCKER_REGISTRY_TOKEN` do Homebrew), dois portões que não conseguem reprovar o que dizem medir (o comparador docs-vs-realidade aceita qualquer caminho sob `/v1`; o `backlog_verify.py` não lê `status` para decidir nada), e a constatação de que nenhum PAT publicado na documentação tem forma que o produto parseia. Cada item carrega evidência com `file:line`, o controle quando existe, o que não decide, e um `verify` cujos dois lados foram executados.
