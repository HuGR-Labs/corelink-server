### Fixed

- **B-314 now keeps the GDPR Sigstore transfer-table decision owner-bound and fail-closed.**
  The four-locale census rejects case-insensitive duplicate rows, the Legal/DPO completion
  packet names the required signed evidence and published diff, and the backlog workflow
  runs the guard independently for pull requests and pushes.
