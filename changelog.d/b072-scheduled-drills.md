### Fixed

- **The unproven synthetic weekly cron is retired and its handoff remains fail-closed (B-072).** The default Worker has no active synthetic trigger; the dormant scheduled handler retains its internal service-binding seam and deterministic delivery id for a future owner-controlled non-production trigger. Unknown crons call `noRetry()`; a missing binding, thrown delivery call, or non-2xx response remains an explicit retryable failure. No PagerDuty URL, routing key, or other secret is introduced, and all five production environments explicitly override inherited crons with empty lists.
- When an owner-approved trigger is reintroduced, synthetic week-3 `boundary_handoff` carries `delivery_mode=deferred` and an effective `emit_at_ms` at the following Sunday 23:59 UTC, so the receiver must not page at the trigger time. This remains a local handoff only: the change does not prove external PagerDuty delivery and B-072 stays open pending the owner packet's end-to-end evidence.
