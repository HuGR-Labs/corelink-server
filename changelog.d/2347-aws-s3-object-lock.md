### Security

- **Tenant-bound AWS S3 Object Lock Compliance archive remains disabled pending protected evidence (B-046).** Adds the optional adapter's fail-closed archive contract, exact-version readback, durable audit intent, and D1 metadata migration without enabling R2 fallback or any provider target.
