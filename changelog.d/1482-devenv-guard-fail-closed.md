### Fixed

- **O guard do plano de compute mais caro do produto autorizava por omissão — e uma queda do D1 autorizava todo mundo.** `checkDevenvQuota` (`worker/src/lib/devenv_guard.ts`), ligado em `/v1/customer/devenv*` e `/v1/devenv*`, só negava quando encontrava uma linha com `install_status = "suspended"`; **sem linha em `runners_entitlement`** (o `if (row)` não tinha `else`) e **D1 lançando** (o `catch` era um comentário `/* Fail-open … */` vazio) caíam ambos no `return { allowed: true }` do fim da função. Um tenant que nunca comprou o SKU recebia compute faturável pelo primeiro buraco; todo tenant recebia pelo segundo, enquanto durasse a indisponibilidade. Os dois caminhos agora negam, e `CONFIG_DB` desligado também — o campo é não-opcional em `Env` e está ligado nos sete ambientes do `wrangler.toml`, então binding ausente é defeito de configuração e nunca autorização. A negação por ausência de linha é a semântica que `migrations/d1/0070_runners_entitlement.sql` já documentava ("Empty table = no cap = reject"); o guard do DevEnv era o único leitor que discordava. Fechado por um teste unitário novo de 13 casos (7 vermelhos contra o guard velho, 13 verdes depois, com controle positivo), e o `verify` de B-075 passa a executar esse teste em vez de ler a estrutura do TypeScript por regex.
