### Fixed

- **O guard do plano de compute mais caro do produto autorizava 100% das requisições, e a causa era uma coluna que não existe.** `checkDevenvQuota` (`worker/src/lib/devenv_guard.ts`), ligado em `/v1/customer/devenv*` e `/v1/devenv*`, fazia `SELECT … install_status FROM runners_entitlement` — mas `install_status` **não é coluna**: nenhuma migração a cria, e ela é sintetizada na resposta JSON por `customer_runners.rs:285`, que grava `"installed"` fixo porque a linha existe. Medido contra o D1 de produção: `PRAGMA table_info(runners_entitlement)` devolve `tenant_id, max_concurrency, plan, created_at_ms, max_vcpu_h`, e a query do guard falha com `no such column: install_status`. Ou seja: o D1 lançava em toda chamada, o `catch` vazio engolia, e o guard **nunca negava nada** — o ramo "sem linha de entitlement" jamais era alcançado. O `SELECT` passa a nomear só colunas reais e o predicado é transcrito das migrações em vez de inventado: `0072` ratifica que `max_concurrency` ausente ⇒ sem entitlement ⇒ REJECT enquanto `max_vcpu_h` ausente ⇒ wall-off ⇒ prossegue, e `0070` fecha com `CHECK (max_concurrency > 0)`, que faz da presença da linha a expressão do direito. Nega em: sem tenant, `CONFIG_DB` desligado, D1 lançando, sem linha, e linha sem cap positivo. **O teto mensal de vCPU segue não aplicado e isso ficou escrito**: `devenv_monthly_vcpu` não tem quem escreva nem quem leia, e `consumed_vcpu_h` é `0` literal marcado `[stub]`. O teste novo (24 casos) parseia o DDL das migrações e fixa a lista de colunas contra o schema real — a primeira versão dele fabricava a linha no mock e por isso passava 13/13 até com `SELECT totally_nonexistent_column`; mock que inventa schema não testa schema. **Dois reparos de revisão fria, ambos provados por mutante:** (1) a docstring afirmava que `max_vcpu_h` "is read so that a non-positive value can be refused" — falso na mesma classe que fez a v1 ser rejeitada: o campo é `SELECT`ado e tipado, e **nunca entra num predicado**; `max_concurrency` é o único campo que decide. A recusa NÃO foi implementada de propósito (as 8 linhas de prod têm `max_vcpu_h = NULL` e `0072` ratifica que ausente ⇒ wall-off ⇒ prossegue; recusar mudaria a semântica declarada), então o que mudou foi o texto — no guard e no conceito OKF, que repetia a mesma falsidade por outro caminho ("only to honour 0072's wall-off rule": honrar wall-off exige **zero** leitura). Um caso novo fixa que `max_vcpu_h = -5` com cap positivo ainda ALLOWa, de modo que reintroduzir a recusa fica vermelho em vez de silencioso. (2) O braço `typeof … !== "number"` era o único mutante sobrevivente de 9: removê-lo mantinha os 22 casos verdes, e sem ele `max_concurrency: "8"` **autorizaria** — o que importa aqui porque o D1 via REST é conhecido por devolver número como REAL. Caso novo com a string `"8"` esperando `allowed: false`. Ambos os mutantes reprovados e restaurados: 24/24 verde → mutante A (remove o `typeof`) 23 verdes / 1 vermelho, exatamente o caso novo → restaurado 24/24; mutante B (adiciona a recusa de `max_vcpu_h`) 23 verdes / 1 vermelho → restaurado 24/24.
