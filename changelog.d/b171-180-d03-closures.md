### Fixed

- **D03 B-171–B-180:** closed tenant namespace fail-closed behavior, bounded OCI
  manifests and upload sessions, wired the FK-parent-safe D1 migration runner,
  routed OCI bearer traffic by tenant residency, persisted/served erasure
  attestations, enabled data-plane rate/request quotas, and reconciled the OCI
  token-key environment aliases. Each item has an inverted focal verifier and
  mutation test; the backlog now records the executable closure evidence.
