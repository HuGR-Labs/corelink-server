### Fixed

- **D03 bundle compilation and tests are fail-closed again.** Document the
  alert transport field required by the workspace lint policy, remove two
  sealed-archive Clippy violations without changing behavior, teach the D1
  migration replay splitter that `CASE ... END` does not close a trigger, and
  bind three extracted audit-chain test modules to their sibling files.
