//! AC lookup + update handlers + audit sink — wave-33 canonical AC
//! surface.
//!
//! Re-exports the entire public API of `corelink-handler-ac`. The
//! actual implementation lives in `crates/corelink-handler-ac/`
//! (Stage 1 Stream A sub-step A.2b Option-A aggregator pattern).

pub use corelink_handler_ac::*;
