//! AC region + simulation schema — wave-33 canonical AC surface.
//!
//! Re-exports the entire public API of `corelink-ac-schema`. The
//! actual implementation lives in `crates/corelink-ac-schema/`
//! (Stage 1 Stream A sub-step A.2b Option-A aggregator pattern).

pub use corelink_ac_schema::*;
