//! Action Cache Merkle codec + dual-side verifier — wave-33 canonical
//! AC surface.
//!
//! Re-exports the entire public API of `corelink-ac-core` (the
//! historical `corelink-ac` crate, renamed in A.2a). The actual
//! implementation lives in `crates/corelink-ac-core/` (Stage 1 Stream
//! A sub-step A.2b Option-A aggregator pattern).

pub use corelink_ac_core::*;
