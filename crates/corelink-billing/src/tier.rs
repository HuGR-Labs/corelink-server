//! Tier-selection orchestrator — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-tier-selection`. The
//! actual implementation lives in `crates/corelink-tier-selection/`
//! (Stage 1 Stream B sub-step B.1 Option-A aggregator pattern).

pub use corelink_tier_selection::*;
