//! Abuse classifier + tenant reputation — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-abuse`. The actual
//! implementation lives in `crates/corelink-abuse/` (Stage 1 Stream B
//! sub-step B.1 Option-A aggregator pattern).

pub use corelink_abuse::*;
