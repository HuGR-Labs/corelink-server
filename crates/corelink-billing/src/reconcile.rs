//! Stripe ↔ D1 reconcile — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-billing-reconcile`.
//! The actual implementation lives in `crates/corelink-billing-reconcile/`
//! (Stage 1 Stream B sub-step B.1 Option-A aggregator pattern).

pub use corelink_billing_reconcile::*;
