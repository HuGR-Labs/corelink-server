//! RFC 6585 + RateLimit IETF draft response headers — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-rate-headers`. The
//! actual implementation lives in `crates/corelink-rate-headers/`
//! (Stage 1 Stream B sub-step B.1 Option-A aggregator pattern).

pub use corelink_rate_headers::*;
