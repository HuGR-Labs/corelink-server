//! BLAKE3 cryptographic primitives — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-hash`. The actual
//! implementation lives in `crates/corelink-hash/` (Stage 0 sub-step 2
//! Option-A aggregator pattern; see crate-level rustdoc).

pub use corelink_hash::*;
