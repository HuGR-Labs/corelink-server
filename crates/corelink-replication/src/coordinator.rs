//! R-PREP wave-15 multi-region replication coordinator — wave-33
//! canonical surface.
//!
//! Re-exports the entire public API of
//! `corelink-replication-coordinator`. The actual implementation
//! lives in `crates/corelink-replication-coordinator/` (Stage 1
//! Stream C sub-step C.1 Option-A aggregator pattern).

pub use corelink_replication_coordinator::*;
