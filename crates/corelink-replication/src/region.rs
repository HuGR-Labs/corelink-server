//! Multi-region topology + region pinning — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-region`. The actual
//! implementation lives in `crates/corelink-region/` (Stage 1 Stream C
//! sub-step C.1 Option-A aggregator pattern).

pub use corelink_region::*;
