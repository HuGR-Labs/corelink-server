//! Per-region replica worker apply lane — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-replica-worker`. The
//! actual implementation lives in `crates/corelink-replica-worker/`
//! (Stage 1 Stream C sub-step C.1 Option-A aggregator pattern).

pub use corelink_replica_worker::*;
