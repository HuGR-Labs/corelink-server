//! Staged rollout controller — wave-33 canonical surface.
//!
//! Re-exports the entire public API of `corelink-rollout-controller`.
//! The actual implementation lives in
//! `crates/corelink-rollout-controller/` (Stage 1 Stream C sub-step
//! C.1 Option-A aggregator pattern).

pub use corelink_rollout_controller::*;
