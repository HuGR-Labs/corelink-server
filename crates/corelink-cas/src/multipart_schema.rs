//! Multipart wire schema — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-multipart-schema`.
//! The actual implementation lives in `crates/corelink-multipart-schema/`
//! (Stage 1 Stream A sub-step A.1 Option-A aggregator pattern).

pub use corelink_multipart_schema::*;
