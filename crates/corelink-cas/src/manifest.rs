//! CAS manifest types + builder + verifier — wave-33 canonical CAS
//! surface.
//!
//! Re-exports the entire public API of `corelink-manifest`. The actual
//! implementation lives in `crates/corelink-manifest/` (Stage 1 Stream
//! A sub-step A.1 Option-A aggregator pattern).

pub use corelink_manifest::*;
