//! Variable-size chunking primitives — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-chunker`. The actual
//! implementation lives in `crates/corelink-chunker/` (Stage 1 Stream
//! A sub-step A.1 Option-A aggregator pattern; see crate-level rustdoc).

pub use corelink_chunker::*;
