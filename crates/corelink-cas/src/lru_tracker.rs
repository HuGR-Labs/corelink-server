//! LRU stats tracker — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-lru-tracker`. The
//! actual implementation lives in `crates/corelink-lru-tracker/`
//! (Stage 1 Stream A sub-step A.1 Option-A aggregator pattern).

pub use corelink_lru_tracker::*;
