//! CAS meta-store ports + types — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-meta`. The actual
//! implementation lives in `crates/corelink-meta/` (Stage 1 Stream A
//! sub-step A.1 Option-A aggregator pattern).

pub use corelink_meta::*;
