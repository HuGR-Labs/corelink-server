//! Edge cache adapter — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-edge`. The actual
//! implementation lives in `crates/corelink-edge/` (Stage 1 Stream
//! A sub-step A.1 Option-A aggregator pattern; see crate-level rustdoc).

pub use corelink_edge::*;
