//! R2 multipart upload state machine — wave-33 canonical CAS surface.
//!
//! Re-exports the entire public API of `corelink-r2-multipart`. The
//! actual implementation lives in `crates/corelink-r2-multipart/`
//! (Stage 1 Stream A sub-step A.1 Option-A aggregator pattern).

pub use corelink_r2_multipart::*;
