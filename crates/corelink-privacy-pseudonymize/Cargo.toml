[package]
name = "corelink-privacy-pseudonymize"
description = "CoreLink privacy pseudonymization helper (WI-S11-002 — S-11 Privacy Pipeline HIGH_RISK lane). Canonical sha256(subject_id || erasure_salt) helper + marker `pii_redacted=true` insertion + verify (re-derive) helper for forensic re-correlation per GDPR Recital 26 + Art. 11 + WP29 Opinion 05/2014 endorsed by EDPB (anonymization techniques) escape valve regulatory rationale. SHA-256 chosen over BLAKE3 per WI-S11-002 §9.3 DD-002 trade-off (NIST FIPS 180-4 regulatory defensibility wins Privacy Officer signature in ADR docs). Separated from `corelink-privacy-erasure-worker` so the S-14 BYOK refactor can swap in a customer-controlled erasure_salt vault KMS surface without touching the orchestrator. wasm32-unknown-unknown clean (sha2 crate is wasm32-OK; no ring / no C toolchain dependency)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
subtle = { workspace = true }
uuid = { workspace = true, features = ["serde"] }

[dev-dependencies]
proptest = { workspace = true }
serde_json = { workspace = true }

[[test]]
name = "pseudonymization_invariants"
path = "tests/pseudonymization_invariants.rs"
