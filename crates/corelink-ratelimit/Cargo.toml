[package]
name = "corelink-ratelimit"
description = "CoreLink rate-limit token-bucket engine — per-tenant DO RateLimiter trait + lazy-refill state machine + 5-tier canonical refill rate ladder + RFC 6585 §4 / RFC 9331 Retry-After semantic + audit fail-closed envelope (WI-S08-001). Ships RateLimiter trait + InMemoryTokenBucketRateLimiter orchestrator + RateLimitDecision + RateLimitConfig knobs (per-tenant / per-IP / per-tenant-per-endpoint key dimensions) + 5-tier refill_rate_for_tier ladder (free/solo/team/business/enterprise) + canonical D1 migration `0010_ratelimit_buckets.sql` durable mirror. Production Cloudflare DO singleton + real D1 binding deferred to WI-S08-006 PRR ship gate per `trait-abstraction-defer` charter pattern."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }
uuid = { workspace = true, features = ["v7"] }
corelink-eviction = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[[test]]
name = "prop_ratelimit"
path = "tests/prop_ratelimit.rs"

[[test]]
name = "migration_canonical_0010"
path = "tests/migration_canonical_0010.rs"
