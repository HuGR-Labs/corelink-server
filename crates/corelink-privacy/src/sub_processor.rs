//! Sub-processor change notification (Art. 28 + DPA §16) — wave-33 canonical surface.
//!
//! Re-exports the entire public API of
//! `corelink-privacy-sub-processor-emit`. The actual implementation
//! lives in `crates/corelink-privacy-sub-processor-emit/` (Stage 1
//! Stream B sub-step B.5 Option-A aggregator pattern).

pub use corelink_privacy_sub_processor_emit::*;
