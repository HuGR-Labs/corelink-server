workspace(name = "corelink_bazel_starter")

# Bazel starter project for CoreLink remote cache integration (WI-S15-002 / WI-S15-005).
# See .bazelrc for remote cache configuration.
#
# NOTE: Bzlmod (MODULE.bazel) is the source of truth for dependencies — modern
# Bazel does NOT load this WORKSPACE file on a default build. rules_cc is declared
# via bazel_dep in MODULE.bazel. The http_archive below is retained only as a
# fallback for legacy `--enable_workspace` (WORKSPACE-mode) invocations.

load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# rules_cc: C++ build rules.
# SHA-256 verified; pin version for reproducibility.
http_archive(
    name = "rules_cc",
    sha256 = "2037875b9a4456dce4a79d112a8ae885bbc4aad968e6587dca6e64f3a0900cdf",
    strip_prefix = "rules_cc-0.0.9",
    urls = [
        "https://mirror.bazel.build/github.com/bazelbuild/rules_cc/releases/download/0.0.9/rules_cc-0.0.9.tar.gz",
        "https://github.com/bazelbuild/rules_cc/releases/download/0.0.9/rules_cc-0.0.9.tar.gz",
    ],
)

load("@rules_cc//cc:repositories.bzl", "rules_cc_dependencies")

rules_cc_dependencies()
