name: openapi validate (3.1 spec + breaking-change detection)

# Quality gate for `openapi/corelink-v1.yaml` and its generated JSON sibling.
# Steps:
#   1. Redocly lint (0 errors, warnings ok).
#   2. OpenAPI 3.1 meta-schema validation via openapi-spec-validator.
#   3. Drift check: `scripts/openapi_sync.py --check` ensures the JSON
#      sibling is kept in sync with the YAML.
#   4. Breaking-change detection vs the base branch (PR only) via
#      `oasdiff` — reports breaking changes as a job warning; fail
#      only on PRs that toggle a `breaking-api-ok` label.

on:
  pull_request:
    paths:
      - "openapi/**"
      - "tools/openapi/**"
      - "scripts/openapi_sync.py"
      - ".github/workflows/openapi-validate.yml"
  push:
    branches: [main]
    paths:
      - "openapi/**"
      - "tools/openapi/**"
      - "scripts/openapi_sync.py"
      - ".github/workflows/openapi-validate.yml"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: openapi-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-validate:
    name: lint + meta-schema + JSON sync
    runs-on: corelink  # zero-hosted: node/python already baked into the corelink image
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Node (for Redocly CLI)
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"

      - name: Install Redocly CLI
        run: npm install --global @redocly/cli@1.25.11

      # No actions/setup-python: the self-hosted mac fleet can't reliably
      # provision Python via setup-python (RUNNER_TOOL_CACHE is unset → it
      # falls back to the unwritable /Users/runner hosted path and dies with
      # `mkdir: /Users/runner: Permission denied`). We use the system `python3`
      # (ships on every runner) and isolate the pyyaml/openapi-spec-validator
      # deps in a repo-local venv. Adding the venv's bin to $GITHUB_PATH makes
      # the `python3` in every subsequent step resolve to the venv. (2026-06-03)
      - name: Install Python deps (venv on system python3)
        run: |
          set -euo pipefail
          python3 -m venv .venv
          .venv/bin/python3 -m pip install --upgrade pip
          .venv/bin/python3 -m pip install pyyaml openapi-spec-validator
          echo "$PWD/.venv/bin" >> "$GITHUB_PATH"

      - name: Redocly lint (0 errors required)
        run: redocly lint openapi/corelink-v1.yaml --format=stylish

      - name: OpenAPI 3.1 meta-schema validation + JSON sync check
        run: python3 scripts/openapi_sync.py --check

  breaking-change-detection:
    name: breaking-change detection (vs base branch)
    if: github.event_name == 'pull_request'
    # Runner: `corelink` (zero-hosted). We do NOT bake Go into the shared box
    # image just for this rare lane — oasdiff ships a prebuilt static binary, so
    # the job downloads the SAME pinned version (v1.10.27) at runtime,
    # sha256-verified, instead of `go install`. github.com egress is available at
    # job time (same as actions/checkout). Keeps the runner image lean.
    runs-on: corelink
    timeout-minutes: 10
    # PR context passed via env (not interpolated into run: shell) —
    # template-injection-safe (branch names + label JSON can carry shell
    # metacharacters). This job is pull_request-only (if: above).
    env:
      PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
      PR_LABELS_JSON: ${{ toJson(github.event.pull_request.labels) }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Materialize base branch spec
        id: base
        run: |
          set -e
          BASE_REF="$PR_BASE_REF"
          git fetch origin "$BASE_REF" --depth=1
          mkdir -p /tmp/openapi-base
          if git show "origin/${BASE_REF}:openapi/corelink-v1.yaml" \
              > /tmp/openapi-base/corelink-v1.yaml 2>/dev/null; then
            echo "has_base=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_base=false" >> "$GITHUB_OUTPUT"
            echo "info: no spec on base branch — first introduction; skipping diff"
          fi

      # Prebuilt oasdiff binary — verified pinned download (X4 supply-chain floor),
      # replacing the previous `go install …@v1.10.27` so no Go toolchain is needed
      # on the corelink box. Same version. The sha256 is from the release's OWN
      # published checksums.txt (oasdiff/oasdiff v1.10.27, line
      # `…  oasdiff_1.10.27_linux_amd64.tar.gz`) — NEVER fabricated.
      - name: Install oasdiff (prebuilt binary, sha256-verified)
        if: steps.base.outputs.has_base == 'true'
        env:
          OASDIFF_VERSION: '1.10.27'
          OASDIFF_SHA256: '09648692362fd63658bc83188d25465b47c900f59036f77cf9ae6064d41e3ed7'
        run: |
          set -euo pipefail
          curl -fsSL \
            "https://github.com/oasdiff/oasdiff/releases/download/v${OASDIFF_VERSION}/oasdiff_${OASDIFF_VERSION}_linux_amd64.tar.gz" \
            -o /tmp/oasdiff.tar.gz
          echo "${OASDIFF_SHA256}  /tmp/oasdiff.tar.gz" | sha256sum -c -
          mkdir -p "$HOME/.local/bin"
          tar -xzf /tmp/oasdiff.tar.gz -C "$HOME/.local/bin" oasdiff
          rm -f /tmp/oasdiff.tar.gz
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          "$HOME/.local/bin/oasdiff" --version

      - name: Run oasdiff breaking-change scan
        if: steps.base.outputs.has_base == 'true'
        id: diff
        run: |
          set +e
          oasdiff breaking \
              /tmp/openapi-base/corelink-v1.yaml \
              openapi/corelink-v1.yaml \
              --fail-on ERR \
              | tee /tmp/oasdiff-report.txt
          echo "code=$?" >> "$GITHUB_OUTPUT"

      - name: Allow breaking change only with `breaking-api-ok` label
        if: steps.base.outputs.has_base == 'true' && steps.diff.outputs.code != '0'
        run: |
          if echo "$PR_LABELS_JSON" | grep -q '"breaking-api-ok"'; then
            echo "::warning::breaking API change accepted (label: breaking-api-ok)"
            exit 0
          fi
          echo "::error::breaking change detected — apply 'breaking-api-ok' label after sign-off"
          exit 1
