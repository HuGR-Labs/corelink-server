name: Issue 2623 ownership contract

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/issue-2623-ownership-contract.yml
      - crates/corelink-adapter-host/src/cargo/bridge.rs
      - crates/corelink-adapter-host/src/cargo/ports.rs
      - crates/corelink-adapter-host/src/cargo/server.rs
      - crates/corelink-billing-stripe-traits/src/lib.rs
      - crates/corelink-handler-cas/src/handler.rs
      - crates/corelink-handler-cas/src/lib.rs
      - crates/corelink-privacy-erasure-worker/src/audit_emit.rs
      - crates/corelink-privacy-erasure-worker/src/idempotency.rs
      - crates/corelink-privacy-erasure-worker/src/orchestrator.rs
      - crates/corelink-stripe-real/src/webhook_dispatch.rs
      - crates/corelink-container/src/adapter_cache.rs
      - crates/corelink-container/src/byok_control_transition.rs
      - crates/corelink-container/src/byte_accounting/b126_m2_impl_01_part_02.rs
      - crates/corelink-container/src/main.rs
      - crates/corelink-container/src/routes/byok_admin.rs
      - crates/corelink-container/src/routes/cargo/part-00.rs
      - crates/corelink-container/src/routes/cargo/part-00-01.rs
      - crates/corelink-container/src/routes/cargo/tests-00-00.rs
      - crates/corelink-container/src/routes/dsr.rs
      - crates/corelink-container/src/routes/signup.rs
      - crates/corelink-container/src/signup_d1_http.rs
      - crates/corelink-container/src/storage/r2_s3_parts/cas_core.rs
      - crates/corelink-container/src/storage/r2_s3_parts/cas_write.rs
      - crates/corelink-container/src/storage/staging_load_test_admission.rs
      - crates/corelink-container/src/storage/staging_load_test_ownership.rs
      - apps/signup-worker/src/staging_load_test_ownership.ts
      - apps/signup-worker/src/lib/d1.ts
      - apps/signup-worker/tests/staging_load_test_ownership.test.ts
      - migrations/d1/0149_staging_load_test_r2_intents.sql
      - scripts/verify_issue_2623_ownership_contract.py
      - tests/test_issue_2623_ownership_contract.py

permissions:
  contents: read

concurrency:
  group: issue-2623-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  contract:
    if: >-
      github.repository == 'HuGR-dev/corelink-server' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Check out exact candidate
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Bind exact candidate and protected base
        env:
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha }}
          EXPECTED_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"
          test "$(git merge-base "$EXPECTED_BASE" "$EXPECTED_HEAD")" = "$EXPECTED_BASE"

      - name: Install pinned Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable toolchain action
        with:
          toolchain: 1.91.1

      - name: Install pinned Protobuf compiler
        uses: arduino/setup-protoc@f4d5893b897028ff5739576ea0409746887fa536 # v3.0.0

      - name: Set up pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22

      - name: Install focused Worker dependencies
        run: pnpm install --frozen-lockfile --filter @corelink/signup-worker... --ignore-scripts

      - name: Verify frozen seam
        run: python3 -I scripts/verify_issue_2623_ownership_contract.py

      - name: Verify additive migrations and R2 state machine
        run: |
          python3 scripts/check_migration_prefixes.py
          python3 scripts/check_migrations_additive.py
          python3 -I tests/test_issue_2623_ownership_contract.py

      - name: Check formatting and compile shared Rust API
        run: |
          cargo fmt --all -- --check
          cargo check --locked -p corelink-server --lib

      - name: Run focused Rust contract tests
        run: |
          cargo test --locked -p corelink-server --lib storage::staging_load_test_admission::tests::worker_ownership_envelope_is_domain_separated_bounded_and_request_bound -- --exact
          cargo test --locked -p corelink-server --lib storage::staging_load_test_admission::tests::loopback_store_consumes_a_verified_claim_in_the_real_d1_batch_shape -- --exact
          cargo test --locked -p corelink-server --lib storage::staging_load_test_admission::tests::present_admission_header_fails_closed_when_the_gate_is_not_configured -- --exact
          cargo test --locked -p corelink-server --lib storage::staging_load_test_ownership::tests::d1_and_r2_contracts_accept_only_bounded_canonical_inputs -- --exact

      - name: Typecheck and test focused Worker envelope
        run: |
          pnpm --filter @corelink/signup-worker typecheck
          pnpm --filter @corelink/signup-worker exec vitest run tests/staging_load_test_ownership.test.ts
