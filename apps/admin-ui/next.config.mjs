/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // typedRoutes intentionally left off until WI-S16-007 ship gate.
  },
};

export default nextConfig;
