---
id: cli-query-write-status
title: CLI — query an in-flight ByteStream write
sidebar_label: CLI · query write status
---

<!-- i18n:TODO (es-419) — auto-generated stub; replace with native-speaker translation by D+10 -->

> Esta página está en traducción. La versión canónica en inglés es la fuente de verdad hasta la revisión por hablante nativo (D+10).
>
> Canonical EN source: `ByteStream/cli-query-write-status.mdx`

# CLI — `corelink put --query-status`

Inspect the committed offset of an in-flight upload (useful for resume / debugging).

## Code

```bash
corelink put --query-status "uploads/abc-123" --format=json
```

## Expected output

```json
{"committed_size": 16777216, "complete": false}
```

## Common errors

| CLI exit code | Cause | Fix |
| --- | --- | --- |
| `4` | Resource name not found | Upload session expired (TTL 24h) — restart |
| `3` | `COR_AUTH_PAT_INVALID` | Re-mint PAT |
