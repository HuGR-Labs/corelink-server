---
id: openapi
title: REST API (OpenAPI 3.1)
sidebar_label: REST API spec
description: Hand-authored OpenAPI 3.1 contract for the CoreLink customer, privacy (DSR + DPA), billing-webhook, admin and ops REST surface. Generated from the canonical openapi/corelink-v1.yaml.
---

<!-- i18n:MT (es-419) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Esta página está en traducción. La versión canónica en inglés es la fuente de verdad hasta la revisión por hablante nativo (D+10).
>
> Canonical EN source: `docs/reference/api/openapi.mdx`


import CodeBlock from "@theme/CodeBlock";

# REST API — OpenAPI 3.1

CoreLink exposes two API surfaces:

- **REAPI v2 (gRPC)** — high-throughput CAS / AC / ByteStream / Capabilities.
  Documented under [REAPI v2 reference](../reapi/index.mdx).
- **REST (this page)** — customer onboarding, privacy (DSR + DPA),
  Stripe billing webhook, admin operations, and ops endpoints.

The canonical contract is
[`openapi/corelink-v1.yaml`](/openapi-corelink-v1.yaml)
in the server monorepo. The static asset shipped with this docs site is
generated from that canonical contract and is the same parsed OpenAPI
document. `scripts/openapi_sync.py --check` and the B-151 closed-world gate
reject path, method, operation identity, or document-level drift.

## Download

- [OpenAPI 3.1 YAML](/openapi-corelink-v1.yaml)
- Browse the source on GitHub:
  [`openapi/corelink-v1.yaml`](/openapi-corelink-v1.yaml)

## Render with Redocly

```bash
npx @redocly/cli build-docs https://corelink-docs.humangr.com/openapi-corelink-v1.yaml \
    --output corelink-rest-api.html
```

## Endpoint coverage

| Tag | Status | Path |
| --- | --- | --- |
| signup | planned | `POST /v1/signup` |
| dpa | planned | `POST /v1/dpa/accept`, `POST /v1/dpa/re-accept` |
| tier | planned | `POST /v1/onboarding/tier-select` |
| pat | **live** | `POST /v1/pats` |
| pat | planned | `GET /v1/pats`, `DELETE /v1/pats/{pat_id}` |
| billing | planned | `POST /v1/billing/stripe-webhook` |
| privacy-dsr | planned | `POST /v1/privacy/dsr/{action}` (6 actions), `GET /v1/privacy/dsr/{request_id}/status` |
| admin | planned | `POST /v1/admin/ops`, `GET /v1/admin/ops/{op_id}`, `POST /v1/admin/ops/{op_id}/approve`, `POST /v1/admin/ops/{op_id}/reject`, `GET /v1/admin/audit/events`, `GET /v1/admin/tenants` |
| enterprise | planned | `POST /v1/enterprise/inquire` |
| users | planned | `GET /v1/users/me`, `PATCH /v1/users/me` |
| data-categories | planned | `GET /v1/data-categories` |
| ops | planned | `GET /api/health`, `POST /api/csp-report` |

## Authentication

| Scheme | Where | Used for |
| --- | --- | --- |
| `BearerPAT` | `Authorization: Bearer <PAT>` | Programmatic API access (default). |
| `ClerkSessionCookie` | `__session` cookie | Browser-driven onboarding flow. |
| `StripeSignature` | `Stripe-Signature` header | The Stripe webhook ingestion route only. |

Every admin mutation also requires an `X-Admin-Operation-Reason` header
captured into the audit envelope.

## Stability contract

The `v1` prefix is **public and semver-compatible**. Breaking changes
require a new path prefix (`/v2/...`); the CI gate
(`openapi-validate.yml`) runs `oasdiff breaking` against the base
branch on every pull request and fails unless a `breaking-api-ok`
label is applied with reviewer sign-off.
