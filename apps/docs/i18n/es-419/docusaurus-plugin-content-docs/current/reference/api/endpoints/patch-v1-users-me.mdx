---
id: patch-v1-users-me
title: "PATCH /v1/users/me"
sidebar_label: "PATCH /v1/users/me"
description: "Update mutable profile fields."
tags: [users, rest-api]
---

<!-- i18n:MT (es-419) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Esta página está en traducción. La versión canónica en inglés es la fuente de verdad hasta la revisión por hablante nativo (D+10).
>
> Canonical EN source: `docs/reference/api/endpoints/patch-v1-users-me.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `PATCH` `/v1/users/me`

> Update mutable profile fields.

- **Operation ID:** `usersMePatch`
- **Tag:** `users`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`
- `ClerkSessionCookie`

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`UserProfilePatch`](#schema-userprofilepatch)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`UserProfile`](#schema-userprofile) | Updated profile. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `422` | [`ErrorEnvelope`](#schema-errorenvelope) | Schema-valid but semantically invalid payload. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X PATCH \
  'https://api.corelink.dev/v1/users/me' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{
  "display_name": "string",
  "locale": "en-US"
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "display_name": "string",
  "locale": "en-US"
}
    "#)?;
    let resp = client
        .patch("https://api.corelink.dev/v1/users/me")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {
  "display_name": "string",
  "locale": "en-US"
}
    resp = httpx.patch(
        "https://api.corelink.dev/v1/users/me",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "display_name": "string",
  "locale": "en-US"
}`)
    req, err := http.NewRequest("PATCH", "https://api.corelink.dev/v1/users/me", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.dev/v1/users/me", {
  method: "PATCH",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "display_name": "string",
  "locale": "en-US"
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

