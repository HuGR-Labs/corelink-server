---
id: post-bazel-v2-by-instance-findMissingBlobs
title: "POST /bazel/v2/{instance}/findMissingBlobs"
sidebar_label: "POST /bazel/v2/{instance}/findMissingBlobs"
description: "Find missing blobs (Bazel REAPI FindMissingBlobs POST)."
tags: [bazel-v2, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/bazel/v2/{instance}/findMissingBlobs`

> Find missing blobs (Bazel REAPI FindMissingBlobs POST).

- **Operation ID:** `bazelFindMissingBlobs`
- **Tag:** `bazel-v2`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `instance` | `path` | yes | `string` |  |

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** `object`

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | `object` | List of digests not present in the cache. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | _(empty body)_ | Cross-tenant attempt. |
| `413` | _(empty body)_ | Batch too large. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://corelink-api.humangr.com/bazel/v2/string/findMissingBlobs' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{}
    "#)?;
    let resp = client
        .post("https://corelink-api.humangr.com/bazel/v2/string/findMissingBlobs")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {}
    resp = httpx.post(
        "https://corelink-api.humangr.com/bazel/v2/string/findMissingBlobs",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{}`)
    req, err := http.NewRequest("POST", "https://corelink-api.humangr.com/bazel/v2/string/findMissingBlobs", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/bazel/v2/string/findMissingBlobs", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

