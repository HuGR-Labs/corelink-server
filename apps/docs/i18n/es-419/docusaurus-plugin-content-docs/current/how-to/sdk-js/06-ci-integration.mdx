---
id: sdk-js-ci-integration
title: Integración con CI (JS/TS)
sidebar_label: Integración con CI
description: Conecte @corelink/client con GitHub Actions, GitLab CI y CircleCI.
tags: [sdk, javascript, typescript, ci, how-to]
---

<!-- i18n:MT (es-419) — TMX-seeded machine-translation stub; replace with native-speaker translation before GA -->

> MT: Esta página está en traducción. La versión canónica en inglés es la fuente de verdad hasta la revisión por hablante nativo (D+10).
>
> Canonical EN source: `docs/how-to/sdk-js/06-ci-integration.mdx`

# Integración con CI

## Cuándo usar esta guía

Su pipeline de compilación ejecuta Node.js y quiere usar CoreLink como caché compartida
sin invocar el binario de la CLI.

## GitHub Actions

```yaml
# .github/workflows/build.yml
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CORELINK_PAT: ${{ secrets.CORELINK_PAT }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: node ./ci/warm-cache.mjs
```

```typescript
// ci/warm-cache.mjs
import { CoreLinkClient } from "@corelink/client";

const client = new CoreLinkClient({ tenantId: process.env.CORELINK_TENANT });
try {
  // A tiny put/get round-trip proves connectivity + auth before the build runs.
  const probe = new TextEncoder().encode(`warm ${Date.now()}`);
  const t0 = performance.now();
  const digest = await client.put(probe);
  await client.get(digest); // BLAKE3-verified
  const ms = performance.now() - t0;
  console.log(`cache reachable; round-trip=${ms.toFixed(1)}ms digest=${digest}`);
  if (ms > 500) console.warn("cache round-trip slower than 500ms");
} finally {
  await client.close();
}
```

## GitLab CI

```yaml
build:
  image: node:22
  variables:
    CORELINK_PAT: $CORELINK_PAT  # masked CI variable
  script:
    - npm ci
    - node ./ci/warm-cache.mjs
```

## CircleCI

```yaml
version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:22.11
    environment:
      CORELINK_PAT: $CORELINK_PAT
    steps:
      - checkout
      - run: npm ci
      - run: node ./ci/warm-cache.mjs
```

## PATs con alcance de tarea

Genere un PAT de corta duración por pipeline mediante la API de administración. Consulte
[Referencia de la CLI](../../reference/cli/index.mdx).

## Próximos pasos

- [Operaciones por lotes](./05-batch-operations.mdx)
- [Solución de problemas](./07-troubleshooting.mdx)
