---
id: post-v1-enterprise-inquire
title: "POST /v1/enterprise/inquire"
sidebar_label: "POST /v1/enterprise/inquire"
description: "Submit an enterprise sales / BYOK inquiry."
tags: [enterprise, rest-api]
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/api/endpoints/post-v1-enterprise-inquire.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/v1/enterprise/inquire`

> Submit an enterprise sales / BYOK inquiry.

- **Operation ID:** `enterpriseInquire`
- **Tag:** `enterprise`
- **Rate-limit class:** anonymous-strict (5 req/min/IP)

## Description

Implemented by `corelink-enterprise-inquiry`. Atomic outbox to
Slack channel + CRM lead; 24h auto-reply SLA.

## Authentication

_Public — no authentication required._

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`EnterpriseInquiryRequest`](#schema-enterpriseinquiryrequest)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `202` | [`EnterpriseInquiryResponse`](#schema-enterpriseinquiryresponse) | Inquiry queued. |
| `422` | [`ErrorEnvelope`](#schema-errorenvelope) | Schema-valid but semantically invalid payload. |
| `429` | [`ErrorEnvelope`](#schema-errorenvelope) | Rate-limit exceeded. |
| `500` | [`ErrorEnvelope`](#schema-errorenvelope) | Server error. Safe to retry idempotent operations. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://api.corelink.dev/v1/enterprise/inquire' \
  -H 'Content-Type: application/json' \
  -d '{
  "byok_provider": "aws",
  "company": "string",
  "expected_egress_gb_month": 0,
  "message": "string",
  "name": "string",
  "work_email": "test-tenant@example.com"
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "byok_provider": "aws",
  "company": "string",
  "expected_egress_gb_month": 0,
  "message": "string",
  "name": "string",
  "work_email": "test-tenant@example.com"
}
    "#)?;
    let resp = client
        .post("https://api.corelink.dev/v1/enterprise/inquire")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Content-Type": "application/json",
    }
    body = {
  "byok_provider": "aws",
  "company": "string",
  "expected_egress_gb_month": 0,
  "message": "string",
  "name": "string",
  "work_email": "test-tenant@example.com"
}
    resp = httpx.post(
        "https://api.corelink.dev/v1/enterprise/inquire",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "byok_provider": "aws",
  "company": "string",
  "expected_egress_gb_month": 0,
  "message": "string",
  "name": "string",
  "work_email": "test-tenant@example.com"
}`)
    req, err := http.NewRequest("POST", "https://api.corelink.dev/v1/enterprise/inquire", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.dev/v1/enterprise/inquire", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "byok_provider": "aws",
  "company": "string",
  "expected_egress_gb_month": 0,
  "message": "string",
  "name": "string",
  "work_email": "test-tenant@example.com"
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

