---
id: post-v8-artifacts-status
title: "POST /v8/artifacts/status"
sidebar_label: "POST /v8/artifacts/status"
description: "Static remote-cache enabled check (Turbo status probe)."
tags: [turbo-v8, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/v8/artifacts/status`

> Static remote-cache enabled check (Turbo status probe).

- **Operation ID:** `turboStatus`
- **Tag:** `turbo-v8`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** no
- **Schema:** `object`

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | _(empty body)_ | Remote cache is enabled. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://api.corelink.humangr.com/v8/artifacts/status' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{}
    "#)?;
    let resp = client
        .post("https://api.corelink.humangr.com/v8/artifacts/status")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {}
    resp = httpx.post(
        "https://api.corelink.humangr.com/v8/artifacts/status",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{}`)
    req, err := http.NewRequest("POST", "https://api.corelink.humangr.com/v8/artifacts/status", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.humangr.com/v8/artifacts/status", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

