---
id: put-v1-ac-by-tenant-by-action_digest
title: "PUT /v1/ac/{tenant}/{action_digest}"
sidebar_label: "PUT /v1/ac/{tenant}/{action_digest}"
description: "Upsert an Action Cache result."
tags: [reapi-http, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `PUT` `/v1/ac/{tenant}/{action_digest}`

> Upsert an Action Cache result.

- **Operation ID:** `acUpdate`
- **Tag:** `reapi-http`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `tenant` | `path` | yes | `string` |  |
| `action_digest` | `path` | yes | `string` |  |

## Request body

- **Media type:** `application/octet-stream`
- **Required:** yes
- **Schema:** `string <binary>`

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | `string` | Idempotent re-write (already existed). |
| `201` | `string` | Stored durably (fresh insert). Body echoes the action digest. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | _(empty body)_ | Cross-tenant attempt — audit row emitted before 403. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X PUT \
  'https://corelink-api.humangr.com/v1/ac/string/string' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{}
    "#)?;
    let resp = client
        .put("https://corelink-api.humangr.com/v1/ac/string/string")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {}
    resp = httpx.put(
        "https://corelink-api.humangr.com/v1/ac/string/string",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{}`)
    req, err := http.NewRequest("PUT", "https://corelink-api.humangr.com/v1/ac/string/string", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/v1/ac/string/string", {
  method: "PUT",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

