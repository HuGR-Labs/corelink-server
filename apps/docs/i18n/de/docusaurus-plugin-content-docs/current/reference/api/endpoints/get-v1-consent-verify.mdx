---
id: get-v1-consent-verify
title: "GET /v1/consent/verify"
sidebar_label: "GET /v1/consent/verify"
description: "Public stateless verify of a grant signature."
tags: [privacy-consent, rest-api]
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/api/endpoints/get-v1-consent-verify.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `GET` `/v1/consent/verify`

> Public stateless verify of a grant signature.

- **Operation ID:** `consentVerifyGrant`
- **Tag:** `privacy-consent`
- **Rate-limit class:** consent-default (60 req/min/tenant)

## Authentication

_Public — no authentication required._

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `signature` | `query` | yes | `string` |  |
| `consent_id` | `query` | yes | `string <uuid>` |  |
| `tenant_short` | `query` | yes | `string` |  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`ConsentVerifyResponse`](#schema-consentverifyresponse) | Verification result. |
| `400` | [`ErrorEnvelope`](#schema-errorenvelope) | Malformed request. |
| `404` | [`ErrorEnvelope`](#schema-errorenvelope) | Resource not found. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X GET \
  'https://api.corelink.dev/v1/consent/verify?signature=string&consent_id=00000000-0000-7000-8000-000000000000&tenant_short=string'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .get("https://api.corelink.dev/v1/consent/verify?signature=string&consent_id=00000000-0000-7000-8000-000000000000&tenant_short=string")

        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {}
    resp = httpx.get(
        "https://api.corelink.dev/v1/consent/verify?signature=string&consent_id=00000000-0000-7000-8000-000000000000&tenant_short=string",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("GET", "https://api.corelink.dev/v1/consent/verify?signature=string&consent_id=00000000-0000-7000-8000-000000000000&tenant_short=string", nil)
    if err != nil {
        panic(err)
    }

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.dev/v1/consent/verify?signature=string&consent_id=00000000-0000-7000-8000-000000000000&tenant_short=string", {
  method: "GET",
  headers: {},
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

