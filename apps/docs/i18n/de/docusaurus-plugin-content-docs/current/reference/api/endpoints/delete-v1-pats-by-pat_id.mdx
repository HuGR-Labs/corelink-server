---
id: delete-v1-pats-by-pat_id
title: "DELETE /v1/pats/{pat_id}"
sidebar_label: "DELETE /v1/pats/{pat_id}"
description: "Revoke a Personal Access Token by id."
tags: [pat, rest-api]
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/api/endpoints/delete-v1-pats-by-pat_id.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `DELETE` `/v1/pats/{pat_id}`

> Revoke a Personal Access Token by id.

- **Operation ID:** `patRevoke`
- **Tag:** `pat`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`
- `ClerkSessionCookie`

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `pat_id` | `path` | yes | `string <uuid>` |  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `204` | _(empty body)_ | Revoked. Subsequent uses MUST fail closed (`INV-PAT-REVOKE-PROPAGATION`). |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | [`ErrorEnvelope`](#schema-errorenvelope) | Authenticated but not authorized for this resource. |
| `404` | [`ErrorEnvelope`](#schema-errorenvelope) | Resource not found. |
| `500` | [`ErrorEnvelope`](#schema-errorenvelope) | Server error. Safe to retry idempotent operations. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X DELETE \
  'https://api.corelink.dev/v1/pats/00000000-0000-7000-8000-000000000000' \
  -H 'Authorization: Bearer <YOUR_PAT>'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .delete("https://api.corelink.dev/v1/pats/00000000-0000-7000-8000-000000000000")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
    }
    resp = httpx.delete(
        "https://api.corelink.dev/v1/pats/00000000-0000-7000-8000-000000000000",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("DELETE", "https://api.corelink.dev/v1/pats/00000000-0000-7000-8000-000000000000", nil)
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.dev/v1/pats/00000000-0000-7000-8000-000000000000", {
  method: "DELETE",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
  },
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

