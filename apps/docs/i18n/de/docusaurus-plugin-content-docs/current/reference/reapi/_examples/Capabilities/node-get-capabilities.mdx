---
id: node-get-capabilities
title: Node — GetCapabilities
sidebar_label: Node · GetCapabilities
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/reapi/_examples/Capabilities/node-get-capabilities.mdx`


# Node / TypeScript — `Capabilities.GetCapabilities`

## Code

```ts
import { CoreLinkClient } from "@corelink/client";

const client = new CoreLinkClient({
  endpoint: "https://acme-dev.corelink.humangr.com",
  pat: process.env.CORELINK_PAT!,
});

const caps = await client.capabilities.get();
console.log("max_batch_total_size_bytes =", caps.cacheCapabilities.maxBatchTotalSizeBytes);
console.log("digest functions =", caps.cacheCapabilities.digestFunctions);
console.log("compressors =", caps.cacheCapabilities.supportedCompressors);
```

## Expected output

```text
max_batch_total_size_bytes = 4194304
digest functions = [ 'SHA256', 'BLAKE3' ]
compressors = [ 'IDENTITY', 'ZSTD' ]
```

## Common errors

| Status | Error taxonomy | Cause |
| --- | --- | --- |
| `UNAUTHENTICATED` | `COR_AUTH_PAT_INVALID` | PAT expired. |
