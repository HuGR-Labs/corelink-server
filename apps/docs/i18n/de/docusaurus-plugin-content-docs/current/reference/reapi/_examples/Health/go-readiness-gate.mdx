---
id: go-readiness-gate
title: Go — Kubernetes readiness gate
sidebar_label: Go · readiness gate
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/reapi/_examples/Health/go-readiness-gate.mdx`

# Go — Kubernetes-Readiness-Gate

`Client.Health` in einen Kubernetes-Readiness-Probe-Handler einbinden.

## Code

```go
package main

import (
    "context"
    "net/http"
    "os"
    "time"

    corelink "go.corelink.humangr.com/corelink-go"
)

func main() {
    client, _ := corelink.NewClient(corelink.Config{
        Endpoint: "https://corelink-api.humangr.com",
        PAT:      os.Getenv("CORELINK_PAT"),
    })
    http.HandleFunc("/ready", func(w http.ResponseWriter, r *http.Request) {
        ctx, cancel := context.WithTimeout(r.Context(), 2*time.Second)
        defer cancel()
        status, err := client.Health(ctx)
        if err != nil || !status.OK {
            http.Error(w, "not ready", http.StatusServiceUnavailable)
            return
        }
        w.WriteHeader(http.StatusOK)
    })
    http.ListenAndServe(":8080", nil)
}
```

## Erwartete Ausgabe

```text
$ curl -i http://localhost:8080/ready
HTTP/1.1 200 OK
```

## Häufige Fehler

| HTTP-Status | Ursache | Fix |
| --- | --- | --- |
| `503` | Upstream `Client.Health` fehlgeschlagen | Pod-Neustart über Liveness-Probe auslösen |
