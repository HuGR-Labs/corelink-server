---
id: go-read
title: Go — ByteStream.Read
sidebar_label: Go · Read
---

<!-- i18n:MT (de) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Diese Seite wird übersetzt. Die englische Fassung gilt als verbindlich, bis ein Muttersprachler die Übersetzung freigibt (D+14).
>
> Canonical EN source: `docs/reference/reapi/_examples/ByteStream/go-read.mdx`

# Go — `ByteStream.Read`

## Code

```go
package main

import (
    "context"
    "fmt"
    "os"

    corelink "go.corelink.humangr.com/corelink-go"
)

func main() {
    ctx := context.Background()
    client, _ := corelink.NewClient(corelink.Config{
        Endpoint: "https://corelink-api.humangr.com",
        PAT:      os.Getenv("CORELINK_PAT"),
        Instance: "default",
    })
    defer client.Close()

    digest := corelink.Digest{Hash: "bdd9ea4a8e6b...", SizeBytes: 41943040}
    out, err := os.Create("/tmp/big.bin")
    if err != nil { panic(err) }
    defer out.Close()

    n, err := client.ByteStream().Read(ctx, digest, out)
    if err != nil { panic(err) }
    fmt.Printf("downloaded %d B\n", n)
}
```

## Erwartete Ausgabe

```text
downloaded 41943040 B
```

## Häufige Fehler

| Fehlertyp | Ursache |
| --- | --- |
| `*corelink.NotFoundError` | Digest evicted oder nie geschrieben. |
| `*corelink.DigestMismatchError` | Client-Verify hat die übertragenen Bytes abgelehnt. |
