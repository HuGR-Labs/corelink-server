---
id: sdk-go-ci-integration
title: CI integration (Go)
sidebar_label: CI integration
description: Wire the Go SDK into GitHub Actions, GitLab CI, and CircleCI.
tags: [sdk, go, ci, how-to]
---


<!-- i18n:MT (pt-BR) — TMX-seeded machine-translation stub; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `sdk-go/06-ci-integration.mdx`

# CI integration

## Quando usar este guia

Seu build é um projeto Go (ou seus smoke tests são escritos em Go) e você
quer o CoreLink como cache compartilhado sem sair chamando a CLI.

## GitHub Actions

```yaml
# .github/workflows/build.yml
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CORELINK_PAT: ${{ secrets.CORELINK_PAT }}
      GOPROXY: https://humangr.com/corelink/docs/goproxy,https://proxy.golang.org,direct
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - run: go get go.corelink.humangr.com/corelink-go@v0.1.0  # GOPROXY definido acima
      - run: go run ./ci/warm-cache
```

```go
// ci/warm-cache/main.go
package main

import (
    "context"
    "log"
    "os"

    "go.corelink.humangr.com/corelink-go"
)

func main() {
    ctx := context.Background()
    cli, err := corelink.NewClient(corelink.Config{
        Endpoint: "https://corelink-api.humangr.com",
        PAT:      os.Getenv("CORELINK_PAT"),
        TenantID: os.Getenv("CORELINK_TENANT"),
    })
    if err != nil {
        log.Fatal(err)
    }
    defer cli.Close()

    // Warm the cache with a real Put/Get round trip and fail the job on error.
    digest, err := cli.Put(ctx, []byte("ci-warm-cache-probe"))
    if err != nil {
        log.Fatalf("cache warm-up failed: %v", err)
    }
    if _, err := cli.Get(ctx, digest); err != nil {
        log.Fatalf("cache verify failed: %v", err)
    }
    log.Println("cache reachable")
}
```

## GitLab CI

```yaml
build:
  image: golang:1.22
  variables:
    CORELINK_PAT: $CORELINK_PAT  # masked
    GOPROXY: "https://humangr.com/corelink/docs/goproxy,https://proxy.golang.org,direct"
  script:
    - go get go.corelink.humangr.com/corelink-go@v0.1.0  # GOPROXY definido acima
    - go run ./ci/warm-cache
```

## CircleCI

```yaml
version: 2.1
jobs:
  build:
    docker:
      - image: cimg/go:1.22
    environment:
      CORELINK_PAT: $CORELINK_PAT
      GOPROXY: https://humangr.com/corelink/docs/goproxy,https://proxy.golang.org,direct
    steps:
      - checkout
      - run: go get go.corelink.humangr.com/corelink-go@v0.1.0  # GOPROXY definido acima
      - run: go run ./ci/warm-cache
```

## PATs por job

Não existe hoje uma API self-service para gerar um PAT de curta duração
por pipeline (veja [Security model](../../security.md)) — envie um e-mail
para [support@humangr.com](mailto:support@humangr.com) para solicitar um
PAT dedicado de CI e injete-o via o secret store do seu provedor de CI,
como mostrado acima.

## Próximos passos

- [Batch operations](./05-batch-operations.mdx)
- [Troubleshooting](./07-troubleshooting.mdx)
