---
title: "Audit chain and Merkle proofs"
slug: "/security/audit-chain"
description: "Append-only audit log with RFC 6962 inclusion proofs and a 7-year retention window."
draft: false
cross_functional_review: TBD
pending_signoff:
  - "Segurança Lead"
  - "Legal"
sources:
  - "specs/03_architecture/security_model.md#68-auditoria--forensics"
last_updated: "2026-05-14"
---

<!-- i18n:MT (pt-BR) — TMX-seeded machine-translation stub; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `security/audit-chain.mdx`

import { DraftBanner } from "@site/src/components/DraftBanner";

<DraftBanner pageType="security" />

# Audit chain and Merkle proofs

> **DRAFT — pending Legal + Finance + Segurança review.** The customer-facing
> proof API is in active design; field names and endpoints below are
> illustrative.

CoreLink writes every administrative or data-plane mutation to an append-only
audit log. The log is structured as a Merkle tree following **RFC 6962**
(Certificate Transparency) so that any consumer can verify, with a small
inclusion proof, that a specific event is part of the public log head without
trusting CoreLink.

## What each event records

`{actor, tenant_id, token_hash, op, path_hash, timestamp, request_id, outcome}`

Sensitive fields (`token_hash`, `path_hash`) are pseudonymized via tenant-scoped
HMAC keys so the log can be retained without retaining the underlying secrets.

## Retention and immutability

- **Retention:** 7 years (aligned with SOC 2 Type II observation expectations).
- **Backing store:** R2 bucket in Object Lock Governance mode.
- **Reconciliation:** daily drift check against Cloudflare analytics; alerts
  fire when drift exceeds 0.1 percent.
- **Chain key rotation:** annual; rotation is forward-only and does not
  invalidate prior inclusion proofs.

## Verificação pelo cliente

As provas de inclusão são entregues pela **exportação** de auditoria, e não
por um comando avulso por evento. A flag `--include-merkle-proofs` embute
uma prova de inclusão Merkle em cada linha de evento exportada, e
`--verify` revalida todas as provas antes de o arquivo ser escrito
(fail-closed):

```sh
corelink audit export --tenant <tenant_id> --since <ms> --until <ms> \
  --include-merkle-proofs --verify
```

Para reconferir uma exportação que você já tem em mãos, use
`corelink audit verify <caminho>`.

Para o envelope NDJSON em streaming vindo de `GET /v1/audit/export` — uma
linha `{event, proof}` por registro de auditoria mais uma linha final de
manifesto — o `corelink audit verify-ndjson` revalida a cadeia inteira
contra a âncora de cabeça de cadeia que o servidor publica no cabeçalho de
resposta `X-CoreLink-Audit-Export-Chain-Head-Anchor`. Ele lê o envelope do
disco (`--ndjson <arquivo> --chain-head-anchor <hex>`) ou por HTTP
(`--url <url> --bearer <PAT>`, que recupera a âncora do próprio cabeçalho),
saindo com 0 quando a integridade da cadeia é total e diferente de 0 em
qualquer divergência.

A chave da cadeia e sua política de rotação estão documentadas em
`security_model.md §7.2`.

## Privacidade interaction with DSR erasure

When a Data Subject Request causes erasure, the `subject_id` field is
pseudonymized to `erased_<hash>` using the audit-pseudonym key so that the
inclusion proof of the original event remains verifiable while the personal
data itself is removed (`CTRL-PRIV-014`).

## Cross-functional review checklist

- [ ] Segurança Lead — confirm RFC 6962 mapping and key hierarchy.
- [ ] Legal — confirm retention statement is consistent with the DPA.
