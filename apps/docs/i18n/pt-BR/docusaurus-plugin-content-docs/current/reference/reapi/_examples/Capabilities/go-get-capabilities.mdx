---
id: go-get-capabilities
title: Go — GetCapabilities
sidebar_label: Go · GetCapabilities
---

<!-- i18n:MT (pt-BR) — TMX-seeded machine-translation stub; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `Capabilities/go-get-capabilities.mdx`

# Go — `Capabilities.GetCapabilities`

## Code

```go
package main

import (
    "context"
    "fmt"
    "os"

    corelink "go.corelink.humangr.com/corelink-go"
)

func main() {
    ctx := context.Background()
    client, _ := corelink.NewClient(ctx, corelink.Config{
        Endpoint: "https://corelink-api.humangr.com",
        PAT:      os.Getenv("CORELINK_PAT"),
    })
    defer client.Close()

    caps, err := client.Capabilities.Get(ctx)
    if err != nil {
        panic(err)
    }
    fmt.Println("max_batch_total_size_bytes =", caps.CacheCapabilities.MaxBatchTotalSizeBytes)
    fmt.Println("digest functions =", caps.CacheCapabilities.DigestFunctions)
    fmt.Println("compressors =", caps.CacheCapabilities.SupportedCompressors)
}
```

## Expected output

```text
max_batch_total_size_bytes = 4194304
digest functions = [SHA256 BLAKE3]
compressors = [IDENTITY ZSTD]
```

## Common errors

| Status | Error taxonomy | Cause |
| --- | --- | --- |
| `UNAUTHENTICATED` | `COR_AUTH_PAT_INVALID` | PAT expired. |
| `UNAVAILABLE` | `COR_NET_TRANSIENT` | Retry with exponential backoff. |
