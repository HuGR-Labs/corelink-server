---
id: go-readiness-gate
title: Go — Kubernetes readiness gate
sidebar_label: Go · readiness gate
---

<!-- i18n:MT (pt-BR) — TMX-seeded machine-translation stub; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `Health/go-readiness-gate.mdx`

# Go — Kubernetes readiness gate

Conecte `Client.Health` a um handler de readiness probe do Kubernetes.

## Código

```go
package main

import (
    "context"
    "net/http"
    "os"
    "time"

    corelink "go.corelink.humangr.com/corelink-go"
)

func main() {
    client, _ := corelink.NewClient(corelink.Config{
        Endpoint: "https://corelink-api.humangr.com",
        PAT:      os.Getenv("CORELINK_PAT"),
    })
    http.HandleFunc("/ready", func(w http.ResponseWriter, r *http.Request) {
        ctx, cancel := context.WithTimeout(r.Context(), 2*time.Second)
        defer cancel()
        status, err := client.Health(ctx)
        if err != nil || !status.OK {
            http.Error(w, "not ready", http.StatusServiceUnavailable)
            return
        }
        w.WriteHeader(http.StatusOK)
    })
    http.ListenAndServe(":8080", nil)
}
```

## Saída esperada

```text
$ curl -i http://localhost:8080/ready
HTTP/1.1 200 OK
```

## Erros comuns

| Status HTTP | Causa | Correção |
| --- | --- | --- |
| `503` | `Client.Health` upstream falhou | Dispare o reinício do pod via liveness probe |
