---
id: get-v1-admin-pilots
title: "GET /v1/admin/pilots"
sidebar_label: "GET /v1/admin/pilots"
description: "List pilot tenants filtered by lifecycle state."
tags: [admin-pilot, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `GET` `/v1/admin/pilots`

> List pilot tenants filtered by lifecycle state.

- **Operation ID:** `adminPilotsList`
- **Tag:** `admin-pilot`
- **Rate-limit class:** default (600 req/min/PAT)

## Description

Replaces the wave-27 `list-pilot-tenants.sh` shell script.
Auth: Clerk JWT with scope `corelink:admin:pilots`; the route
re-checks the scope at the boundary (5-Layer Defense).

## Authentication

- `ClerkSessionCookie`

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `state` | `query` | yes | `"NEW" \| "ACTIVE" \| "GRADUATED" \| "TERMINATED"` |  |
| `offset` | `query` | no | `integer` |  |
| `limit` | `query` | no | `integer` |  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`ListPilotsResponse`](#schema-listpilotsresponse) | Paginated pilot list. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | [`ErrorEnvelope`](#schema-errorenvelope) | Authenticated but not authorized for this resource. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X GET \
  'https://api.corelink.humangr.com/v1/admin/pilots?state=NEW'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .get("https://api.corelink.humangr.com/v1/admin/pilots?state=NEW")

        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {}
    resp = httpx.get(
        "https://api.corelink.humangr.com/v1/admin/pilots?state=NEW",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("GET", "https://api.corelink.humangr.com/v1/admin/pilots?state=NEW", nil)
    if err != nil {
        panic(err)
    }

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.humangr.com/v1/admin/pilots?state=NEW", {
  method: "GET",
  headers: {},
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

