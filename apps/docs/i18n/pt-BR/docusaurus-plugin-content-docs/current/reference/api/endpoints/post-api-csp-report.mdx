---
id: post-api-csp-report
title: "POST /api/csp-report"
sidebar_label: "POST /api/csp-report"
description: "Browser Content-Security-Policy violation report intake."
tags: [ops, rest-api]
---

<!-- i18n:MT (pt-BR) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `docs/reference/api/endpoints/post-api-csp-report.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/api/csp-report`

> Browser Content-Security-Policy violation report intake.

- **Operation ID:** `cspReport`
- **Tag:** `ops`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

_Public — no authentication required._

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`CspReport`](#schema-cspreport)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `204` | _(empty body)_ | Recorded. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://api.corelink.dev/api/csp-report' \
  -H 'Content-Type: application/json' \
  -d '{
  "csp-report": {}
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "csp-report": {}
}
    "#)?;
    let resp = client
        .post("https://api.corelink.dev/api/csp-report")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Content-Type": "application/json",
    }
    body = {
  "csp-report": {}
}
    resp = httpx.post(
        "https://api.corelink.dev/api/csp-report",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "csp-report": {}
}`)
    req, err := http.NewRequest("POST", "https://api.corelink.dev/api/csp-report", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.dev/api/csp-report", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "csp-report": {}
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

