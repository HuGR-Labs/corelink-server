---
id: get-v1-audit-analytics-timeline
title: "GET /v1/audit/analytics/timeline"
sidebar_label: "GET /v1/audit/analytics/timeline"
description: "Event-count timeline bucketed at a chosen granularity."
tags: [audit-analytics, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `GET` `/v1/audit/analytics/timeline`

> Event-count timeline bucketed at a chosen granularity.

- **Operation ID:** `auditAnalyticsTimeline`
- **Tag:** `audit-analytics`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT` (scopes: `admin`)

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `from` | `query` | yes | `integer <int64>` |  |
| `to` | `query` | yes | `integer <int64>` |  |
| `granularity` | `query` | no | `integer <int64>` | Bucket size in milliseconds (capped at 24h). Default chosen by the handler to keep `(to - from) / granularity ≤ 1000`.  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`TimelineResponse`](#schema-timelineresponse) | Timeline buckets. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X GET \
  'https://corelink-api.humangr.com/v1/audit/analytics/timeline?from=0&to=0' \
  -H 'Authorization: Bearer <YOUR_PAT>'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .get("https://corelink-api.humangr.com/v1/audit/analytics/timeline?from=0&to=0")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
    }
    resp = httpx.get(
        "https://corelink-api.humangr.com/v1/audit/analytics/timeline?from=0&to=0",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("GET", "https://corelink-api.humangr.com/v1/audit/analytics/timeline?from=0&to=0", nil)
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/v1/audit/analytics/timeline?from=0&to=0", {
  method: "GET",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
  },
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

