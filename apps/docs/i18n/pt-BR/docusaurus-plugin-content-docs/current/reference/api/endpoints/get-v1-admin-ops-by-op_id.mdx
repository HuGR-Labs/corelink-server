---
id: get-v1-admin-ops-by-op_id
title: "GET /v1/admin/ops/{op_id}"
sidebar_label: "GET /v1/admin/ops/{op_id}"
description: "Inspect an admin op (status + audit envelope)."
tags: [admin, rest-api]
---

<!-- i18n:MT (pt-BR) — bootstrap MT-stub from EN source; replace with native-speaker translation before GA -->

> MT: Esta página está em tradução. A versão canônica em inglês é a fonte de verdade até a revisão por falante nativo (D+10).
>
> Canonical EN source: `docs/reference/api/endpoints/get-v1-admin-ops-by-op_id.mdx`


{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `GET` `/v1/admin/ops/{op_id}`

> Inspect an admin op (status + audit envelope).

- **Operation ID:** `adminOpGet`
- **Tag:** `admin`
- **Rate-limit class:** admin-strict (60 req/min/PAT, dual-approval gate)

## Authentication

- `BearerPAT` (scopes: `admin`)

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `op_id` | `path` | yes | `string <uuid>` |  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`AdminOpResult`](#schema-adminopresult) | Op result. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | [`ErrorEnvelope`](#schema-errorenvelope) | Authenticated but not authorized for this resource. |
| `404` | [`ErrorEnvelope`](#schema-errorenvelope) | Resource not found. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X GET \
  'https://corelink-api.humangr.com/v1/admin/ops/00000000-0000-7000-8000-000000000000' \
  -H 'Authorization: Bearer <YOUR_PAT>'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .get("https://corelink-api.humangr.com/v1/admin/ops/00000000-0000-7000-8000-000000000000")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
    }
    resp = httpx.get(
        "https://corelink-api.humangr.com/v1/admin/ops/00000000-0000-7000-8000-000000000000",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("GET", "https://corelink-api.humangr.com/v1/admin/ops/00000000-0000-7000-8000-000000000000", nil)
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/v1/admin/ops/00000000-0000-7000-8000-000000000000", {
  method: "GET",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
  },
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

