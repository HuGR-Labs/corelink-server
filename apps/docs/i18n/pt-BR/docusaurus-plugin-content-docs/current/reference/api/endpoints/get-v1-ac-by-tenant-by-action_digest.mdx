---
id: get-v1-ac-by-tenant-by-action_digest
title: "GET /v1/ac/{tenant}/{action_digest}"
sidebar_label: "GET /v1/ac/{tenant}/{action_digest}"
description: "Look up an Action Cache result."
tags: [reapi-http, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `GET` `/v1/ac/{tenant}/{action_digest}`

> Look up an Action Cache result.

- **Operation ID:** `acLookup`
- **Tag:** `reapi-http`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `BearerPAT`

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `tenant` | `path` | yes | `string` |  |
| `action_digest` | `path` | yes | `string` |  |

## Request body

_No request body._

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | `object` | ActionResult payload. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | _(empty body)_ | Cross-tenant attempt. |
| `404` | [`ErrorEnvelope`](#schema-errorenvelope) | Resource not found. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X GET \
  'https://api.corelink.humangr.com/v1/ac/string/string' \
  -H 'Authorization: Bearer <YOUR_PAT>'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let resp = client
        .get("https://api.corelink.humangr.com/v1/ac/string/string")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
    }
    resp = httpx.get(
        "https://api.corelink.humangr.com/v1/ac/string/string",
        headers=headers,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    var body []byte
    req, err := http.NewRequest("GET", "https://api.corelink.humangr.com/v1/ac/string/string", nil)
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.humangr.com/v1/ac/string/string", {
  method: "GET",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
  },
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

