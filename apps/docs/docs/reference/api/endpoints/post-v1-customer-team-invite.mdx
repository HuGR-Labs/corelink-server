---
id: post-v1-customer-team-invite
title: "POST /v1/customer/team/invite"
sidebar_label: "POST /v1/customer/team/invite"
description: "Create a one-time cryptographic team invitation."
tags: [users, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/v1/customer/team/invite`

> Create a one-time cryptographic team invitation.

- **Operation ID:** `customerTeamInvite`
- **Tag:** `users`
- **Rate-limit class:** default (600 req/min/PAT)

## Authentication

- `ClerkSessionCookie`
- `BearerPAT`

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`TeamInviteRequest`](#schema-teaminviterequest)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `201` | [`TeamInviteResponse`](#schema-teaminviteresponse) | Invitation created. The token is shown exactly once. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | [`ErrorEnvelope`](#schema-errorenvelope) | Authenticated but not authorized for this resource. |
| `500` | [`ErrorEnvelope`](#schema-errorenvelope) | Server error. Safe to retry idempotent operations. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/customer/keys`](./post-v1-customer-keys.mdx)._

### curl

```bash
curl -X POST \
  'https://corelink-api.humangr.com/v1/customer/team/invite' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{
  "email": "test-tenant@example.com",
  "role": "admin"
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "email": "test-tenant@example.com",
  "role": "admin"
}
    "#)?;
    let resp = client
        .post("https://corelink-api.humangr.com/v1/customer/team/invite")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {
  "email": "test-tenant@example.com",
  "role": "admin"
}
    resp = httpx.post(
        "https://corelink-api.humangr.com/v1/customer/team/invite",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "email": "test-tenant@example.com",
  "role": "admin"
}`)
    req, err := http.NewRequest("POST", "https://corelink-api.humangr.com/v1/customer/team/invite", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/v1/customer/team/invite", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "email": "test-tenant@example.com",
  "role": "admin"
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```
