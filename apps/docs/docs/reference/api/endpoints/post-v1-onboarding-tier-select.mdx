---
id: post-v1-onboarding-tier-select
title: "POST /v1/onboarding/tier-select"
sidebar_label: "POST /v1/onboarding/tier-select"
description: "Select a subscription tier and receive a Stripe Checkout session URL."
tags: [tier, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/v1/onboarding/tier-select`

> Select a subscription tier and receive a Stripe Checkout session URL.

- **Operation ID:** `tierSelect`
- **Tag:** `tier`
- **Rate-limit class:** default (600 req/min/PAT)

## Description

Enforces `INV-ONBOARD-DPA-FIRST` via a D1 lock — tier selection is
rejected until a DPA acceptance row exists for the tenant.
Returns a Stripe Checkout session URL; final activation happens
when the corresponding `checkout.session.completed` event lands
on `/v1/billing/stripe-webhook`.

## Authentication

- `BearerPAT`
- `ClerkSessionCookie`

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`TierSelectRequest`](#schema-tierselectrequest)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `200` | [`TierSelectResponse`](#schema-tierselectresponse) | Checkout session created. |
| `401` | [`ErrorEnvelope`](#schema-errorenvelope) | Missing or invalid credentials. |
| `403` | [`ErrorEnvelope`](#schema-errorenvelope) | DPA-first lock — caller must accept current DPA before selecting a tier. |
| `422` | [`ErrorEnvelope`](#schema-errorenvelope) | Schema-valid but semantically invalid payload. |
| `429` | [`ErrorEnvelope`](#schema-errorenvelope) | Rate-limit exceeded. |
| `500` | [`ErrorEnvelope`](#schema-errorenvelope) | Server error. Safe to retry idempotent operations. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://api.corelink.humangr.com/v1/onboarding/tier-select' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{
  "cancel_url": "https://corelink.humangr.com/example",
  "success_url": "https://corelink.humangr.com/example",
  "tier": "free"
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "cancel_url": "https://corelink.humangr.com/example",
  "success_url": "https://corelink.humangr.com/example",
  "tier": "free"
}
    "#)?;
    let resp = client
        .post("https://api.corelink.humangr.com/v1/onboarding/tier-select")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {
  "cancel_url": "https://corelink.humangr.com/example",
  "success_url": "https://corelink.humangr.com/example",
  "tier": "free"
}
    resp = httpx.post(
        "https://api.corelink.humangr.com/v1/onboarding/tier-select",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "cancel_url": "https://corelink.humangr.com/example",
  "success_url": "https://corelink.humangr.com/example",
  "tier": "free"
}`)
    req, err := http.NewRequest("POST", "https://api.corelink.humangr.com/v1/onboarding/tier-select", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://api.corelink.humangr.com/v1/onboarding/tier-select", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "cancel_url": "https://corelink.humangr.com/example",
  "success_url": "https://corelink.humangr.com/example",
  "tier": "free"
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```

