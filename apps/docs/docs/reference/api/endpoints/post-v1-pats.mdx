---
id: post-v1-pats
title: "POST /v1/pats"
sidebar_label: "POST /v1/pats"
description: "Issue a new Personal Access Token."
tags: [pat, rest-api]
---

{/* AUTO-GENERATED by scripts/gen-api-reference.py — DO NOT EDIT. Source: openapi/corelink-v1.yaml. Drift gated by .github/workflows/api-reference-sync.yml. */}

# `POST` `/v1/pats`

> Issue a new Personal Access Token.

- **Operation ID:** `patIssue`
- **Tag:** `pat`
- **Rate-limit class:** pat-issue (10 req/hour/tenant)

## Description

Creates a PAT for the caller's authenticated tenant. The Worker derives
the tenant from a validated Clerk session or canonical PAT; clients
cannot supply a tenant id and this endpoint never accepts the
operator-only mint authority. The raw token is returned exactly once in
`token` and is never logged or retrievable later. Self-service tokens
expire after 90 days and cannot grant `admin`.
This endpoint and the legacy dashboard alias `POST /v1/customer/keys`
use the same customer-plane mint flow and the same per-tenant
`pat-issue` bucket: burst 10, then 10 tokens per hour (one every 360
seconds). The limiter runs before mint and audit; a denied request
returns `429` with `Retry-After`.

## Authentication

- `BearerPAT`
- `ClerkSessionCookie`

## Parameters

_None._

## Request body

- **Media type:** `application/json`
- **Required:** yes
- **Schema:** [`PatIssueRequest`](#schema-patissuerequest)

## Responses

| Status | Schema | Description |
| --- | --- | --- |
| `201` | [`PatIssueResponse`](#schema-patissueresponse) | PAT issued. |
| `400` | `string` | Malformed JSON or request body rejected by the HTTP extractor. |
| `401` | `string` | Missing or invalid trusted tenant credentials. |
| `403` | `string` | Caller lacks the scope required to mint the requested credential. |
| `422` | `string` | Schema-valid but semantically invalid payload. |
| `429` | `string` | PAT issuance rate limit exceeded. |
| `500` | `string` | PAT storage or handler failure. |
| `503` | `string` | Rate-limit clock or pipeline unavailable; no PAT was minted. |

## Examples

_Snippets are auto-generated from the OpenAPI contract. Replace `<YOUR_PAT>` with a Personal Access Token issued via [`POST /v1/pats`](./post-v1-pats.mdx)._

### curl

```bash
curl -X POST \
  'https://corelink-api.humangr.com/v1/pats' \
  -H 'Authorization: Bearer <YOUR_PAT>' \
  -H 'Content-Type: application/json' \
  -d '{
  "label": "ci-runner-build-cache",
  "scopes": [
    "cache:read"
  ]
}'
```

### Rust (reqwest)

```rust
use reqwest::Client;
use serde_json::Value;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Client::new();
    let body: Value = serde_json::from_str(r#"
{
  "label": "ci-runner-build-cache",
  "scopes": [
    "cache:read"
  ]
}
    "#)?;
    let resp = client
        .post("https://corelink-api.humangr.com/v1/pats")
        .header("Authorization", "Bearer <YOUR_PAT>")
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await?
        .json::<Value>()
        .await?;
    println!("{resp:#?}");
    Ok(())
}
```

### Python (httpx)

```python
import httpx

def main() -> None:
    headers = {
        "Authorization": "Bearer <YOUR_PAT>",
        "Content-Type": "application/json",
    }
    body = {
  "label": "ci-runner-build-cache",
  "scopes": [
    "cache:read"
  ]
}
    resp = httpx.post(
        "https://corelink-api.humangr.com/v1/pats",
        headers=headers,
        json=body,
    )
    resp.raise_for_status()
    print(resp.json())

if __name__ == "__main__":
    main()
```

### Go (net/http)

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{
  "label": "ci-runner-build-cache",
  "scopes": [
    "cache:read"
  ]
}`)
    req, err := http.NewRequest("POST", "https://corelink-api.humangr.com/v1/pats", bytes.NewReader(body))
    if err != nil {
        panic(err)
    }
    req.Header.Set("Authorization", "Bearer <YOUR_PAT>")
    req.Header.Set("Content-Type", "application/json")
    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()
    out, _ := io.ReadAll(resp.Body)
    fmt.Println(string(out))
}
```

### JavaScript (fetch)

```javascript
const resp = await fetch("https://corelink-api.humangr.com/v1/pats", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <YOUR_PAT>",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
  "label": "ci-runner-build-cache",
  "scopes": [
    "cache:read"
  ]
}),
});
if (!resp.ok) {
  throw new Error(`HTTP ${resp.status}`);
}
console.log(await resp.json());
```
