---
id: openapi
title: REST API (OpenAPI 3.1)
sidebar_label: REST API spec
description: Hand-authored OpenAPI 3.1 contract for the CoreLink customer, privacy (DSR + consent + DPA), billing-webhook, admin and ops REST surface. Generated from the canonical openapi/corelink-v1.yaml.
---

import CodeBlock from "@theme/CodeBlock";

# REST API — OpenAPI 3.1

CoreLink exposes two API surfaces:

- **REAPI v2 (gRPC)** — high-throughput CAS / AC / ByteStream / Capabilities.
  Documented under [REAPI v2 reference](../reapi/index.mdx).
- **REST (this page)** — customer onboarding, privacy (DSR + consent + DPA),
  Stripe billing webhook, admin operations, and ops endpoints.

The canonical contract is
[`openapi/corelink-v1.yaml`](https://github.com/HumanGuardrail/corelink-server/blob/main/openapi/corelink-v1.yaml)
in the server monorepo. The static asset shipped with this docs site
mirrors that file byte-for-byte and is gated by the
`openapi-validate.yml` workflow on every PR.

## Download

- [OpenAPI 3.1 YAML](/openapi-corelink-v1.yaml)
- Browse the source on GitHub:
  [`openapi/corelink-v1.yaml`](https://github.com/HumanGuardrail/corelink-server/blob/main/openapi/corelink-v1.yaml)

## Render with Redocly

```bash
npx @redocly/cli build-docs https://corelink-docs.humangr.com/openapi-corelink-v1.yaml \
    --output corelink-rest-api.html
```

## Endpoint coverage

Routes marked **live** are deployed and verified in production today.
Routes marked **planned** are specified but not yet wired (CF Worker entry crate pending).

| Tag | Status | Path |
| --- | --- | --- |
| ops | **live** | `GET /_health` |
| users | **live** | `GET /v1/users/me` |
| reapi-http | **live** | `GET /v1/cas/{tenant}/{hash}`, `PUT /v1/cas/{tenant}/{hash}` |
| reapi-http | **live** | `GET /v1/ac/{tenant}/{action_digest}`, `PUT /v1/ac/{tenant}/{action_digest}` |
| turbo-v8 | **live** | `GET /v8/artifacts/{hash}`, `PUT /v8/artifacts/{hash}`, `POST /v8/artifacts/events`, `POST /v8/artifacts/status` |
| bazel-v2 | **live** | `GET /bazel/v2/{instance}/blobs/{hash}/{size}`, `PUT /bazel/v2/{instance}/uploads/{uuid}/blobs/{hash}/{size}` |
| bazel-v2 | **live** | `GET /bazel/v2/{instance}/blobs/ac/{hash}/{size}`, `PUT /bazel/v2/{instance}/blobs/ac/{hash}/{size}`, `POST /bazel/v2/{instance}/findMissingBlobs` |
| audit-export | **live** | `GET /v1/audit/export` |
| audit-analytics | **live** | `GET /v1/audit/analytics/event-count`, `GET /v1/audit/analytics/timeline` |
| signup-pilot | **live** | `POST /v1/signup/pilot/{token}` |
| admin-pilot | **live** | `GET /v1/admin/pilots`, `POST /v1/admin/pilots/{tenant_id}/grant-tier`, `POST /v1/admin/pilots/{tenant_id}/checkin` |
| admin-read-mutate | **live** | `GET /v1/admin/read/{resource}`, `POST /v1/admin/mutate` |
| signup | planned | `POST /v1/signup` |
| dpa | planned | `POST /v1/dpa/accept`, `POST /v1/dpa/re-accept` |
| tier | planned | `POST /v1/onboarding/tier-select` |
| pat | planned | `POST /v1/pats`, `GET /v1/pats`, `DELETE /v1/pats/{pat_id}` |
| billing | planned | `POST /v1/billing/stripe-webhook` |
| privacy-dsr | planned | `POST /v1/privacy/dsr/{action}` (6 actions), `GET /v1/privacy/dsr/{request_id}/status` |
| privacy-consent | planned | `POST /v1/consent/{purpose}`, `DELETE /v1/consent/{purpose}`, `GET /v1/consent`, `GET /v1/consent/verify`, `GET /v1/consent/revocation/verify` |
| admin | planned | `POST /v1/admin/ops`, `GET /v1/admin/ops/{op_id}`, `POST /v1/admin/ops/{op_id}/approve`, `POST /v1/admin/ops/{op_id}/reject`, `GET /v1/admin/audit/events`, `GET /v1/admin/tenants` |
| enterprise | planned | `POST /v1/enterprise/inquire` |
| data-categories | planned | `GET /v1/data-categories` |
| ops | planned | `GET /api/health`, `POST /api/csp-report` |

## Authentication

| Scheme | Where | Used for |
| --- | --- | --- |
| `BearerPAT` | `Authorization: Bearer <PAT>` | Programmatic API access (default). |
| `ClerkSessionCookie` | `__session` cookie | Browser-driven onboarding flow. |
| `StripeSignature` | `Stripe-Signature` header | The Stripe webhook ingestion route only. |

Every admin mutation also requires an `X-Admin-Operation-Reason` header
captured into the audit envelope.

## Stability contract

The `v1` prefix is **public and semver-compatible**. Breaking changes
require a new path prefix (`/v2/...`); the CI gate
(`openapi-validate.yml`) runs `oasdiff breaking` against the base
branch on every pull request and fails unless a `breaking-api-ok`
label is applied with reviewer sign-off.
