{
  "name": "@corelink/get-corelink-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker serving the CoreLink CLI install one-liner at https://corelink-get.humangr.com (Phase 0.H).",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env prod",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@sentry/cloudflare": "^8.45.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.18",
    "@cloudflare/workers-types": "^4.20260619.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.7",
    "wrangler": "^4.103.0"
  }
}
