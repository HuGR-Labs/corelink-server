# Nuclear red-team — r34 FOCUSED recovery (2026-06-17): rounds 3 + partial 4 vs HARDENED main

> Focused variant (corelink-redteam-nuclear-r34.js) — ran ROUND 3 (deep/TOCTOU/composition) + partial ROUND 4 against main AFTER PRs #312/#317-#321/#323, deduped vs the prior 25+39. Round 4 hunters + chains hit a TRANSIENT server throttle (NOT the daily usage limit) → **11 confirmed is a LOWER BOUND**. Run wf_c604d39b-1f1.

- boundaries: 18 · confirmed: **11** · severity {"high": 9, "medium": 1, "low": 1} · reach {"any-free-tenant": 7, "authed-tenant": 1, "leaked-secret-only": 3}

## Highlights: this run caught a NEVER-LANDED fix + SECOND-ORDER re-opens of THIS session's fixes

### 1. [HIGH | any-free-tenant | 5/5 | round 3] OCI registry reads (docker pull) bypass ALL four quota/billing brakes — unmetered free egress from the shared multi-tenant cache
- **chain:** 1. Sign up for a free CoreLink account, mint a normal (even read-only `cas:r`) PAT.
2. `GET /token` with `Authorization: Basic <pat>` against the OCI realm → the container runs full Option-B verify and mints a read HMAC bearer (works for free tier; only requires cache-read scope).
3. Loop `docker pull` (or raw `GET /v2/<repo>/manifests/<ref>` + `GET /v2/<repo>/blobs/<digest>` + `HEAD`) at arbitrary rate/volume against the shared `_oci` DO.
   - Worker: OCI is pure pass-through (worker/src/index.ts:1718-1754) — it returns BEFORE the per-tier quota block (index.ts:2026) so the monthly `monthly_request_counts` UPSERT (index.ts:2072) NEVER fires for OCI.
   - Container `oci_quota_gate`: gated by
- **impact:** Direct money loss + quota bypass: every native-plane GET is metered (Worker increments monthly_request_counts for every request regardless of verb at index.ts:2072, and native CAS read charges the $-ceiling at cas.rs:551-555), but the SAME read via OCI is metered on neither axis and is not even rate-limited. A free tenant (500K req/mo contracted) c
- **evidence:** crates/corelink-container/src/routes/oci.rs:775 (`let is_write = matches!(*req.method(), PUT|POST|PATCH)`) wrapping BOTH the $-ceiling (oci.rs:785) and request-count (oci.rs:796) — reads fall through unmetered (explicit comment oci.rs:767 "Read methods (GET/HEAD) are not charged here"). worker/src/index.ts:1718-1754 OCI pass-through returns before 
- **fix:** Charge OCI reads on both metering axes, keyed on the VERIFIED HMAC bearer tenant (same source the write path already uses). In crates/corelink-container/src/routes/oci.rs:oci_quota_gate (775): drop the `if is_write` wrapper so GET/HEAD also resolve oci_bearer_tenant(&st.realm_key, req.headers()) and run rc.check_and_increment (monthly request-count, fail-OPEN/429) for every authenticated read; keep gate.check ($-ceiling) on writes only IF reads are intended to be free-but-counted, or include reads in the $-ceiling too for full cost attribution (recommended given R2 Class-B GET + egress COGS). 

### 2. [HIGH | any-free-tenant | 5/5 | round 3] Unmetered, unthrottled OCI /token Argon2id memory-bomb → OOM the shared _oci container → registry outage for ALL tenants
- **chain:** 1. Attacker signs up for a Free tenant via Clerk → onboarding mints them ONE real PAT (valid HMAC, valid Argon2id secret). 2. They fire thousands of CONCURRENT `GET https://corelink-oci.humangr.com/token?scope=repository:x:pull` requests, each with `Authorization: Basic base64("x:<their-real-PAT>")`. Example: `seq 5000 | xargs -P 5000 -I{} curl -s -o /dev/null -u x:$REAL_PAT https://corelink-oci.humangr.com/token?scope=repository:a:pull`. 3. Worker route match: `path === '/token'` → routeKind=oci_token → tenantId='_oci' (worker/src/index.ts:507-508). Worker carve-out (index.ts:1718-1755) forwards RAW: NO extractAuth, NO checkRequestQuota, NO rate-limit, and explicitly `h.delete('x-corelink-t
- **impact:** Full outage of the OCI registry plane for EVERY tenant (the `_oci` DO/container is a single shared instance). Because the container co-hosts the data plane, an OOM-kill / sustained CPU saturation can ripple to CAS/AC/Bazel/Turbo served by the same process. Cost-amplification + uptime loss; launch-day griefing by a competitor at the price of one fre
- **evidence:** NO concurrency cap on the adapter Argon2id: crates/corelink-container/src/adapter_pat.rs:263-320 (verify_capability) — spawn_blocking Argon2id at line 304, dummy-burn at 287-293, with no Semaphore/permit (grep 'Semaphore' across crates/corelink-container/src returns nothing). The prior-fix claim '#4/#5 PatVerifier semaphore' is absent from current 
- **fix:** Add a bounded concurrency gate on the OCI /token Argon2id mint path so a flood of concurrent verifies cannot fan out to unbounded 64-MiB blocking allocations. Concrete approach: (A) Introduce a process-wide tokio::sync::Semaphore sized to the container's safe Argon2id concurrency budget = floor(usable_RAM_MiB / (m_cost_MiB * safety)) — for standard-1 (~4 GiB) and 64-MiB m_cost, cap at ~16-24 permits. Wire it into PatVerifier::verify_capability in crates/corelink-container/src/adapter_pat.rs:304 (and the dummy-burn at :289): acquire a permit BEFORE spawn_blocking; if acquire_timeout (e.g. 250 m

### 3. [HIGH | any-free-tenant | 5/5 | round 3] Turbo idempotent-rewrite (durable=false) byte-accounting roll-back is keyed on KEY-EXISTENCE not CONTENT — replay a tiny PUT then overwrite with 100 MiB to store unbounded storage at ~0 accounted bytes (storage-quota bypass)
- **chain:** Attacker holds any valid PAT for their own tenant T with cas:rw scope (Free tier is fine). Turbo artifact keys are OPAQUE client-chosen strings (turbo_v8.rs:40-44: 'Turbo's artifact hash is OPAQUE ... stored verbatim ... without any hash-integrity verification'). Step 1: `PUT /v8/artifacts/AAAA?teamId=t&slug=s` with a 1-byte body. In handle_put (turbo_v8.rs:545-573) the route RESERVEs byte_len=1 via acc.accrue → Accrued (kept). The bridge calls R2KvStore::write (r2_kv.rs:172-189): `preexisted=false` (fresh key) → backend.put stores 1 byte → returns durable=true. Route keeps the 1-byte reservation. Net accounted: +1 byte; R2 holds 1 byte. Step 2: `PUT /v8/artifacts/AAAA?teamId=t&slug=s` AGAIN
- **impact:** Storage-quota / storage-cost bypass at an arbitrary amplification ratio (100 MiB stored per ~1 accounted byte ≈ 1e8:1). The per-tier storage cap (tenant_storage_state.bytes_quota) becomes inert for Turbo: a Free tenant (10 GB nominal cap) can store hundreds of GB of real R2 data billed to CoreLink's R2 account while bytes_used never approaches the 
- **evidence:** crates/corelink-container/src/storage/r2_kv.rs:182-188 — `preexisted` is computed SOLELY from `backend.get(&object_key)` returning Some(_) (key existence), then `Ok(!preexisted)` is the durable bit; the comment at lines 175-177 asserts 'Turbo artifacts are content-keyed, so a key that already exists holds the same artifact' — this assumption is FAL
- **fix:** Root cause: the #25 idempotent-rollback assumes same-key⇒same-bytes, which holds for content-addressed CAS/Bazel but is FALSE for Turbo's opaque, unverified key. Two correct SOTA fixes:

PREFERRED — make the rollback content-accurate by accounting the DELTA, not all-or-nothing on key existence. Change the CasWriteStore contract from returning a bool `durable` to returning the prior object size (Option<u64>), so the route can compute the true byte delta:
 - r2_kv.rs:182-188 — the presence probe already does backend.get; use its byte length: `let prior_len = block_on(get(object_key)).ok().flatte

### 4. [HIGH | any-free-tenant | 5/5 | round 3] Turbo storage-quota evasion: divergent overwrite is mis-classified as an idempotent re-write (durable=false) → the byte reservation is rolled back, large artifacts stored for free
- **chain:** Attacker holds a valid PAT (any tier, incl. Free) with cas:rw scope. Turbo artifact keys are <team_id>/<hash> where `hash` is an OPAQUE Turborepo cache-key, NOT a hash of the bytes (crates/corelink-turbo-bridge/src/adapter.rs:7-8 explicitly: 'Turbo's hash is opaque ... we cannot pass it as claimed_hash'; r2_kv.rs:172-189 stores bytes verbatim with NO content verification, and the test rt_last_write_wins_and_empty_overwrite at r2_kv.rs:418 proves overwrites with DIFFERENT bytes are allowed/last-write-wins).

Step 1: PUT /v8/artifacts/AAAA?teamId=x with a 1-byte body. Route reserves 1 byte (turbo_v8.rs:545-573 acc.accrue). R2KvStore::write does get(key)=absent → preexisted=false → durable=true
- **impact:** Storage-cap bypass → unbounded free storage at $0 against CoreLink's R2 COGS. Directly attacks the product's margin (the whole point of the storage-governance platform). A competitor or a swarm of Free accounts inflates CoreLink's R2 bill without limit while the bytes_used counter — the value the eviction worker + storage-quota policy read — stays 
- **evidence:** crates/corelink-container/src/storage/r2_kv.rs:172-189 (R2KvStore::write: `preexisted = get(key).is_some()`, returns `Ok(!preexisted)` as `durable` — presence-only, NO content-equality check; comment at adapter.rs:65 FALSELY assumes 'artifacts are content-keyed, so the same key holds the same'); crates/corelink-turbo-bridge/src/adapter.rs:5-12 (exp
- **fix:** Root cause: crates/corelink-container/src/storage/r2_kv.rs:182-188 returns durable purely from key PRESENCE, but on a non-content-addressed surface presence does NOT imply unchanged bytes, so the route's full-reservation rollback (turbo_v8.rs:591-597) is unsound for a divergent overwrite.

SOTA fix (net-byte-delta accounting, mirrors what the AC/CAS contract MEANS rather than copying its boolean):
1. In R2KvStore::write, fetch the prior object on the presence probe (already doing get()), capture prior_len = prior bytes.len() (Some) or 0 (None). The op is byte-idempotent ONLY when prior_len == 

### 5. [HIGH | any-free-tenant | 5/5 | round 3] Turbo storage byte-accounting UNDERFLOW via size-asymmetric idempotent re-PUT (#25 fix re-opened a hole on the one opaque-keyed surface)
- **chain:** 1. PUT /v8/artifacts/<hash>?teamId=<t> with a 1-byte body. Turbo's R2KvStore key is `<teamId>/<hash>`; the key is fresh so writer.write returns durable=true (corelink-turbo-bridge/src/adapter.rs:212-219, handler.rs:325-343). Route accrues byte_len=1 (turbo_v8.rs:536,548-550). tenant_storage_state.bytes_used += 1. R2 now stores 1 byte.
2. PUT /v8/artifacts/<hash>?teamId=<t> AGAIN, same hash, but now with a 100 MiB body (Turbo hash is opaque and is NEVER content-verified against the body — turbo_v8.rs:491 takes `hash` as a bare Path param, length-validated only; the bridge comment literally says 'opaque key — no hash verification', handler.rs:325). The key already exists, so the writer OVERWRI
- **impact:** Storage $-cost bypass / cost-amplification: a Free-tier tenant stores effectively unlimited bytes in R2 while the per-tenant storage cap (tenant_storage_state.bytes_quota) is never tripped — CoreLink eats the R2 storage + egress COGS with zero billing signal. Drives Free-tier COGS far past the ~$5 model; griefing/competitor cost-attack at scale.
- **evidence:** crates/corelink-container/src/routes/turbo_v8.rs:536 (`byte_len = body.len()`), :548-550 (accrue byte_len), :591-596 (release the SAME new byte_len on durable==false). The release amount is the NEW body size, not the prior stored size — no prior-size read anywhere. Root cause: `durable=false` (corelink-turbo-bridge/src/adapter.rs:212-219, handler.r
- **fix:** Root cause: turbo_v8.rs:591-596 releases the NEW body length on durable==false, but for the opaque Turbo key the overwrite can replace a small object with a large one (or vice-versa). Must reconcile bytes_used to the actual on-disk delta.

SOTA fix:
1. corelink-turbo-bridge/src/adapter.rs CasWriteStore::write — change the return from Result<bool> to Result<Option<usize>> (or a small WriteOutcome{ created: bool, prior_len: Option<usize> }). The R2KvStore::write at r2_kv.rs:182-188 already does the presence probe via backend.get(&object_key); when Ok(Some(prior)) it now has prior.len() in hand —

### 6. [HIGH | authed-tenant | 5/5 | round 3] Turbo opaque-key overwrite + durable=false rollback ⇒ unlimited storage at bytes_used≈0 (storage cap fully bypassed)
- **chain:** Turbo PUT is keyed by an OPAQUE Turborepo identifier (key = `<teamId>/<hash>`), NOT content-addressed, and the body is NEVER bound to / verified against the key (corelink-turbo-bridge/src/handler.rs:325 'Durable store (opaque key — no hash verification)'; r2_kv.rs:172-189 PUTs unconditionally). On a re-PUT to an existing key, R2KvStore::write probes presence (HEAD), still overwrites with the NEW bytes, and returns durable = !preexisted = false (r2_kv.rs:182-188). The route's rt-nuclear #25 rollback then RELEASES the whole reservation on durable==false (turbo_v8.rs:585-597), even though brand-new (and larger) bytes were just durably stored.

Concrete requests (PAT for tenant T, teamId=evil):

- **impact:** Direct monetization defeat: a tenant stores effectively UNLIMITED bytes in R2 while the authoritative bytes_used counter (the value the storage-quota policy + eviction worker read) stays near zero. Free/Solo tenants get unbounded storage at $0 — exactly the inert-storage-cap hole #297 set out to close. CoreLink eats unbounded R2 storage + egress CO
- **evidence:** crates/corelink-container/src/routes/turbo_v8.rs:585-597 — `if !resp.durable { ... acc.release(&caller_tenant, byte_len) }` rolls back the full reservation on any overwrite. The guarding assumption (comment at turbo_v8.rs:587-590 'an idempotent re-write stored NOTHING new') is FALSE for Turbo: the key is opaque and the body is unverified, so an ove
- **fix:** Reconcile the byte DELTA on overwrite instead of blanket-releasing the whole reservation. (a) crates/corelink-container/src/storage/r2_kv.rs:172-189 — change CasWriteStore::write to return the prior object's size, not a bool: HEAD via the backend (add a size probe, mirroring r2_s3.rs `head_size`) to get old_len before the PUT, so the caller can compute the true storage delta. (b) crates/corelink-container/src/routes/turbo_v8.rs:585-597 — on overwrite (preexisted), release only `old_len` (net change = new_len - old_len), not `byte_len`. Concretely: reserve new_len up front (keeps the over-cap f

### 7. [HIGH | leaked-secret-only | 3/5 | round 3] Present-but-malformed PAT_SIGNING_KEY rotation sibling silently disables the native Argon2id backstop fleet-wide, re-opening the leaked-PAT_SIGNING_KEY forgery chain
- **chain:** PRECONDITION SETUP (operator, benign): To rotate the signing key the runbook says `wrangler secret put PAT_SIGNING_KEY_NEW` (worker/src/index.ts:88-96). The operator stages a NEW (or PREV) sibling with a typo, an odd-length hex string, or a <32-byte value.

WHAT THE WORKER DOES (stays UP): worker/src/index.ts:890-891 builds the verify set as `[PAT_SIGNING_KEY, PAT_SIGNING_KEY_PREV, PAT_SIGNING_KEY_NEW].filter(k => typeof k === 'string' && k.length >= 64)` — a malformed/short sibling is SILENTLY DROPPED; the Worker keeps HMAC-verifying with the remaining valid key(s) and returns no error.

WHAT THE CONTAINER DOES (silently goes DOWN): crates/corelink-container/src/adapter_pat.rs:221-226 — `fr
- **impact:** Fleet-wide silent removal of the ONLY container-side possession check on the native + audit + customer + users planes. Combined with a leaked signing key it yields full cross-tenant read/write of CAS/AC/Bazel/Turbo, cross-tenant audit-log exfiltration (/v1/audit/:tenant/export), customer-dashboard impersonation (/v1/customer/* incl. minting genuine
- **evidence:** ASYMMETRY: worker/src/index.ts:890-891 (Worker `.filter()` silently DROPS a malformed sibling and keeps serving) vs crates/corelink-container/src/adapter_pat.rs:221-226 (container `Some(None) => return None` REFUSES to build the verifier on a malformed sibling). PROPAGATION: native_pat_gate.rs:206-209 (`PatVerifier::from_env()?` ⇒ whole gate None);
- **fix:** Eliminate the Worker/container asymmetry by making BOTH sides fail the SAME way on a malformed-but-present sibling, and add a prod startup guard so a silently-disabled gate cannot ship. Concretely: (A) crates/corelink-container/src/main.rs (startup, near the native_pat_gate build at routes.rs:365 — lift this into main or pass a flag): when running in prod (gate by the same env signal that proves prod, e.g. StorageEnv/D1 present + PAT_SIGNING_KEY present), treat native_pat_gate_from_env()==None as FATAL — log error and std::process::exit(1)/abort instead of warn!, so the container fails-CLOSED 

### 8. [HIGH | leaked-secret-only | 5/5 | round 3] Leaked internal-auth key → arbitrary cross-tenant CAS blob erase + PERMANENT 410-Gone poison (CAS-erase route has NO dsr_requested legitimacy gate, unlike the DSR route that was fixed in #18-21)
- **chain:** 1. Attacker obtains the shared key K (CORELINK_INTERNAL_AUTH_KEY). 2. /_internal/cas/* is public (worker/src/index.ts:632 `path.startsWith('/_internal/')`, F4 owner-gated, no IP allowlist). The path maps to the `erase` consumer (index.ts:206 internalConsumerForPath defaults `/_internal/cas/*` to `erase`), and resolveConsumerKey falls back to the shared key when CORELINK_ERASE_AUTH_KEY is unset. 3. Attacker learns a victim tenant_id (observable / enumerable) and a target content digest. For SHARED public/deterministic deps (the network-effect moat) the digest is identical across tenants, so the attacker simply hashes the public artifact themselves — no victim secret needed. 4. Attacker fires:
- **impact:** Per-blob irreversible cross-tenant data deletion + a DURABLE 410 poison that survives re-upload (permanent denial of a cached artifact for the victim tenant). At scale across a victim's public-dep digests: griefs/ransoms a competitor by bricking their build cache (every cache GET 410s → builds fall back to cold/upstream, cache value destroyed) with
- **evidence:** crates/corelink-container/src/routes/cas_erase.rs:149-276 (handle_erase) — the ONLY gates are internal_auth_ok (constant-time) + prepare_erase; NO dsr_requested / ownership / legitimacy check. crates/corelink-handler-cas-erase/src/handler.rs:145 — `if req.path_tenant != req.auth_tenant` is the entire cross-tenant guard, and both fields are attacker
- **fix:** Bind the per-blob CAS erase to a legitimacy/ownership proof the same way the DSR route is, in crates/corelink-handler-cas-erase/src/handler.rs:prepare_erase + crates/corelink-container/src/routes/cas_erase.rs:handle_erase. Concretely: (a) Add a legitimacy gate before the R2 delete in handle_erase (around cas_erase.rs:226, after prepare_erase, before is_tombstoned/erase_blob) that requires either a verified DSR `dsr_requested` row for (tenant,digest-owning-request) via the existing DsrLegitimacyStore, OR proof that the *caller* owns the tenant — i.e. require a real per-tenant PAT/principal rath

### 9. [HIGH | leaked-secret-only | 5/5 | round 3] Per-hash CAS-erase route has NO dsr_requested legitimacy gate — the rt-nuclear #18/#19 fix only hardened the DSR mass-erase leg, leaving the sibling per-blob erase a pure leaked-key cross-tenant deletion primitive
- **chain:** 1. Obtain the erase secret (CORELINK_ERASE_AUTH_KEY, or — because every consumer key falls back to it — the shared CORELINK_INTERNAL_AUTH_KEY). 2. Enumerate or learn a victim tenant UUID + a target blob digest (build artifacts / cached deps are content-addressed and frequently deterministic, so digests are predictable for public deps; tenant UUIDs leak via support/error/log channels). 3. POST https://corelink.prod/_internal/cas/<victim_uuid>/<blake3_digest>/erase  with headers `x-corelink-internal-auth: <leaked_key>` and body `{"tenant":"<victim_uuid>","reason":"x"}`. 4. handle_erase (cas_erase.rs:handle_erase) passes internal_auth_ok, then prepare_erase only checks `req.path_tenant != req.a
- **impact:** Cross-tenant destructive data deletion + permanent 410-Gone tombstoning of arbitrary victim CAS blobs, with no cost accrual (free griefing/ransom). Unlike the DSR mass-erase leg (now gated by the dsr_requested pre-check), this leg lets the SAME leaked key delete any single tenant's blobs without any durable proof-of-request — defeating exactly the 
- **evidence:** crates/corelink-container/src/routes/cas_erase.rs handle_erase — only guard is internal_auth_ok + prepare_erase; `grep legitimacy|dsr_requested|is_requested cas_erase.rs` returns nothing (no legitimacy binding). crates/corelink-handler-cas-erase/src/handler.rs:145 `if req.path_tenant != req.auth_tenant` — cross-tenant check compares two attacker-co
- **fix:** cas_erase.rs handle_erase (between step 4 prepare_erase and step 6 erase_blob, ~cas_erase.rs:226): add a mandatory durable legitimacy pre-check mirroring the DSR leg. Concretely: (1) Add a `LegitimacyStore` collaborator to CasEraseRouteState (cas_erase.rs:107) with `async fn is_erase_authorized(tenant, digest, reason) -> Result<bool,String>`, backed by the same D1 dsr_requested / takedown-ticket table the orchestrator uses (corelink-container/src/routes/dsr/legitimacy.rs), and make build_state_from_env (cas_erase.rs:615) return None unless it is present (fail-CLOSED, same posture as the TDK ga

### 10. [MEDIUM | any-free-tenant | 3/5 | round 3] Bazel findMissingBlobs is a full-GET + full-rehash existence probe → 4096× R2-egress & SHA-256 CPU cost-amplification per request (flat $-charge, no per-byte ceiling, no concurrency cap)
- **chain:** Preconditions: a valid cas:rw PAT (a free signup suffices). STEP 1 (prime, one-time): PUT ~4096 large CAS blobs into the Bazel SHA-256 keyspace via PUT /bazel/v2/<inst>/uploads/<uuid>/blobs/<sha256>/<size> — make each blob as large as the 10 MiB global body limit allows (GLOBAL_BODY_LIMIT_BYTES=10*1024*1024, crates/corelink-container/src/main.rs:296). Each PUT is charged a flat $0.001 (DEFAULT_COST_PER_OP_MICROS=1000, tenant_quota.rs:89), so priming 4096 blobs costs ~$4.10 once. STEP 2 (amplify, repeatable each cycle): POST /bazel/v2/<inst>/findMissingBlobs with body {"blobDigests":[{hash:<the 4096 present sha256s>,sizeBytes:N},…]} (≤ FIND_MISSING_BLOB_CAP=4096). The handler (bazel_v2.rs:696
- **impact:** Cost-amplification against CoreLink COGS: ~40 GiB of R2 GET egress + ~40 GiB of SHA-256 hashing per ~$4 of metered spend, repeatable each billing cycle and per free tenant. Concurrent batches (no read-side concurrency cap) pin CPU/memory and induce 503s for all co-tenants on the shared container (Turbo/CAS/AC/Bazel/adapters share the process). The 
- **evidence:** crates/corelink-container/src/routes/bazel_v2.rs:696-759 (handle_find_missing: no concurrency guard in signature; charges flat per-digest then loops reads) + bazel_v2.rs:140/112 (BazelPutGuard / BAZEL_WRITE_CONCURRENCY_LIMIT exists only on writes) ; crates/corelink-bazel-bridge/src/find_missing.rs:131-143 (per-digest cas.read() in a serial loop for
- **fix:** Two-part fix in the find-missing read path. (1) Replace the full-GET existence probe with a HEAD: add a cheap existence method to CasReadHandler (e.g. fn exists(&self, req) -> Result<bool, CasHandlerError>) backed by R2CasHandler using the already-present head_size (r2_s3.rs:203, HeadObject — no body, no rehash) instead of read()/get(). Wire InMemoryFindMissing::find_missing (find_missing.rs:131-143) to call exists() rather than read() — this is the SOTA REAPI behavior (findMissingBlobs is supposed to be a HEAD probe) and eliminates the per-digest full-blob download + SHA-256 rehash entirely, 

### 11. [LOW | any-free-tenant | 5/5 | round 3] OCI registry PULLs (GET/HEAD blobs + manifests) bypass BOTH the per-tenant $-ceiling AND the monthly request-count cap — unmetered read amplification + request-cap evasion
- **chain:** 1. Sign up (free tier is enough). Obtain a PAT (cas:r is sufficient for pulls). 2. OCI two-leg auth: GET /token with Authorization: Basic base64(user:<PAT>) → container runs Option-B verify and mints an HMAC bearer scoped to the PAT's tenant. 3. Pull blobs/manifests in a tight loop: `for d in $digests; do curl -H "Authorization: Bearer $OCI_BEARER" https://corelink-prod/v2/<repo>/blobs/sha256:$d; done` (and GET /v2/<repo>/manifests/<ref>). 4. Each GET is a real served read from the shared MoatCache/R2, returning bytes. NONE of these GETs accrue the $-ceiling and NONE increment monthly_request_counts. Repeat indefinitely / month after month — the tenant's $-ceiling never trips (it only ever s
- **impact:** Cost-amplification charged to CoreLink: unmetered R2 Class-B GET + Cloudflare egress on every OCI pull, $0 to the tenant. The monthly request-count cap (the contracted rate-card axis, 500K/mo free … 80M/mo max) is fully evaded on the OCI read plane — a competitor doing griefing or a free-tier abuser pulls the shared multi-tenant cache (public base 
- **evidence:** crates/corelink-container/src/routes/oci.rs:775 — `let is_write = matches!(*req.method(), Method::PUT | Method::POST | Method::PATCH); if is_write { … gate.check / rc.check_and_increment … }` — the $-ceiling and request-count are gated ONLY on write methods; GET/HEAD fall straight through to `next.run(req)` at line 802. The module comment (oci.rs:6
- **fix:** crates/corelink-container/src/routes/oci.rs:769-803 — meter the monthly REQUEST-COUNT axis on OCI reads, not just writes. The request-count gate is an SLO-style allowance limiter and is already fail-OPEN (request_count.rs:33-44, 247-257), so charging it on GET/HEAD is safe (no false 402s on availability). Concrete change: in `oci_quota_gate`, recover the tenant for ALL methods that successfully carry a verified bearer (oci_bearer_tenant, oci.rs:754-764) and call `rc.check_and_increment(&tenant)` for reads too; KEEP the $-ceiling (`gate.check`) gated to writes only (reads on the native plane ch

