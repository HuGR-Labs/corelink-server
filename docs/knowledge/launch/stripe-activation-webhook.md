---
type: "BillingControl"
title: "Stripe activation webhook — the money-path activation half"
description: "The LIVE Stripe webhook that verifies the signature, dedups the delivery, and writes the active tier_selections row the edge quota gate reads."
source_files:
  - apps/signup-worker/src/webhooks/stripe.ts
checkpoint_sha: "65a1ac1ad409316c4c41f4f8bb0dd7b1451c080f"
provenance: "AUTHORED"
tags: [launch, billing, stripe, webhook, activation, money-path]
timestamp: "2026-06-27T00:00:00Z"
---

# Stripe activation webhook — the money-path activation half

CoreLink's money path has two halves. The container's checkout adapter (see [the money path](/launch/money-path.md)) opens a hosted Stripe Checkout Session and the buyer pays on Stripe's PCI-owned page; this concept is the OTHER half — the LIVE webhook handler that turns that payment into served entitlement. It receives Stripe's `checkout.session.completed` delivery, proves it is genuinely from Stripe, dedups the redelivery, and writes the canonical `tier_selections … subscription_state='active'` row that the edge quota concept (`launch/edge-quota-tier-serving`) reads to serve a paid tier — its `quota.ts` edge filter requiring `subscription_state='active'` refuses any paid tier whose row is NOT active. This is the DEPLOYED activation path — it runs in the `signup-worker` at the Cloudflare edge. A second activation arm exists inside the container webhook, but it is SECONDARY: this Worker handler is the one wired to the live Stripe endpoint and is the only writer that flips a tenant from `pending_checkout` to `active` on the production path.

# Role

The handler `handleStripeWebhook` is the single live activation authority. It runs a strict order before any side effect: verify the signature, parse, AWAIT the idempotent entitlement writes (returning 500 on failure so Stripe redelivers), then claim the delivery, then emit analytics exactly-once. Three enforcers carry the load this concept covers: (1) `verifyStripeSignature` proves the body is from Stripe and is not a replay (`apps/signup-worker/src/webhooks/stripe.ts:180-238`); (2) `claimWebhookEvent` makes the non-idempotent analytics emit fire exactly once via an `INSERT OR IGNORE` claim (`apps/signup-worker/src/webhooks/stripe.ts:542-549`); (3) `activatePaidTierSelection` writes the canonical `active` row that grants access (`apps/signup-worker/src/webhooks/stripe.ts:724-750`). The price→tier reverse map keeps an in-Stripe plan change mapped back to a CoreLink tier (`apps/signup-worker/src/webhooks/stripe.ts:292-316`).

This handler now ALSO owns the RUNNER entitlement lifecycle — a SECOND, separate axis from the cache tier. A runner subscription is its own Stripe subscription (disjoint price env set `STRIPE_PRICE_ID_RUNNER_*`), so the cache-path helpers all resolve null for it and the cache activation is a clean no-op. The runner arms instead seed/revoke `runners_entitlement` (migrations 0070/0072) via a dedicated `runner_billing` map (migration 0087), which keys the runner Stripe subscription id → tenant so it never clobbers the one-row-per-tenant `tenant_billing` cache row and a tenant can hold BOTH a cache and a runner subscription. On `checkout.session.completed` a `metadata[tier]=runner_*` maps the runner subscription into `runner_billing` and returns without any cache activation (`apps/signup-worker/src/webhooks/stripe.ts:1247-1300`); on `customer.subscription.created`/`updated` (the first events carrying the runner PRICE) `runnerEntitlementFromSubscriptionPrice` seeds the entitlement on a granting status and revokes it otherwise (`apps/signup-worker/src/webhooks/stripe.ts:449-482`, seed/revoke dispatch at `:1520-1595`); and `invoice.payment_failed` (`:1770-1795`) / `customer.subscription.deleted` (`:1835-1858`) unconditionally revoke through `runner_billing` (an inherent no-op for a cache subscription).

# How it works

- **Signature verify before any side effect.** `verifyStripeSignature` parses the `Stripe-Signature` header (`t=<ts>,v1=<hex>…`), computes `HMAC-SHA256(secret, ` + "`${timestamp}.${rawBody}`" + `)`, and compares against each `v1` candidate — over the RAW body, never the re-serialized JSON, so no canonicalization drift can invalidate the HMAC (`apps/signup-worker/src/webhooks/stripe.ts:180-238`).
- **Replay guard (±tolerance).** The signed timestamp must be within `MAX_TIMESTAMP_AGE_MS` (5 minutes) of now, else the delivery is rejected as a replay — a captured-and-resent body past the window fails closed (`apps/signup-worker/src/webhooks/stripe.ts:209`).
- **Constant-time-ish compare.** The expected hex is diffed against each candidate by XOR-accumulating every char into `diff` and only accepting when `diff === 0`, so a partial-prefix match leaks no early-exit timing oracle (`apps/signup-worker/src/webhooks/stripe.ts:234`).
- **Idempotency claim (process-then-claim).** After the entitlement writes COMMIT, the handler `INSERT OR IGNORE`s the Stripe `event_id` into `stripe_webhook_events_processed`; `meta.changes === 1` means first successful delivery (emit), `0` means a redelivery PK-conflict (skip the non-idempotent MRR emit while the idempotent writes re-run harmlessly) (`apps/signup-worker/src/webhooks/stripe.ts:542-549`).
- **The activation write.** `activatePaidTierSelection` does an `INSERT INTO tier_selections … VALUES (…, 'active', …) ON CONFLICT (tenant_id) DO UPDATE SET … subscription_state = 'active'`, guarded `WHERE subscription_state <> 'active'`, so a duplicate/out-of-order completion can neither downgrade nor re-stamp an already-active row; the UPSERT (not a bare UPDATE) means even a lost container `pending_checkout` persist still yields an `active` row (`apps/signup-worker/src/webhooks/stripe.ts:724-750`).
- **Price→tier reverse map (cache axis).** `tierFromSubscriptionPrice` reads `items.data[0].price.id` (or legacy `plan.id`) and matches it against the `STRIPE_PRICE_ID_{TIER}` env vars — the SAME vars the checkout backend used to pick the price — returning null (never a guess) when no tier matches, so an in-Stripe plan swap maps back to the right CoreLink tier (`apps/signup-worker/src/webhooks/stripe.ts:292-316`).
- **Runner seed/revoke (separate axis).** The runner mirror is `runnerEntitlementFromSubscriptionPrice`, which reverse-maps the subscription's price against the DISJOINT `STRIPE_PRICE_ID_RUNNER_*` env set (scanning every `items.data[*].price.id` plus legacy `plan.id`); a hit seeds `runners_entitlement` on a granting status via `upsertRunnersEntitlementBySubscription` and revokes it otherwise via `revokeRunnersEntitlementBySubscription`, both resolving tenant THROUGH `runner_billing` (subscription id → tenant), all idempotent so a redelivery converges. Because the runner and cache price env sets are disjoint, at most one of the two paths ever has work to do on a given subscription (`apps/signup-worker/src/webhooks/stripe.ts:449-482`, `apps/signup-worker/src/webhooks/stripe.ts:951-1054`).

# Invariants

- A delivery with a bad or absent signature, or a timestamp outside the 5-minute window, produces NO side effect — verification returns false before parse, and the handler 400s `invalid_signature` (`apps/signup-worker/src/webhooks/stripe.ts:209`).
- The signature compare never exits early on a mismatching byte — equal-length candidates are XOR-folded and only `diff === 0` accepts (`apps/signup-worker/src/webhooks/stripe.ts:234`).
- The non-idempotent analytics emit fires exactly once: the claim is `INSERT OR IGNORE` on the `event_id` PK and a redelivery hits `changes === 0` (`apps/signup-worker/src/webhooks/stripe.ts:542-549`).
- Activation is idempotent and non-regressing: the `ON CONFLICT DO UPDATE … WHERE subscription_state <> 'active'` guard means a redelivered/out-of-order completion cannot downgrade or re-timestamp an already-active row (`apps/signup-worker/src/webhooks/stripe.ts:724-750`).
- An unrecognized Stripe price maps to NO cache tier change — `tierFromSubscriptionPrice` returns null rather than guessing a SKU (`apps/signup-worker/src/webhooks/stripe.ts:315`).
- The runner axis never touches the cache tables and vice-versa: a runner price is in a DISJOINT env set from the cache price map, so `tierFromSubscriptionPrice` returns null for a runner price (and `runnerEntitlementFromSubscriptionPrice` returns null for a cache price), and the runner entitlement is written ONLY to `runner_billing`/`runners_entitlement` (migrations 0087/0070), never `tenant_billing`/`tier_selections`. A tenant can therefore hold both a cache AND a runner subscription (`apps/signup-worker/src/webhooks/stripe.ts:449-482`).
- Runner entitlement is fail-CLOSED on cancel/failure: `invoice.payment_failed` and `customer.subscription.deleted` issue the runner revoke UNCONDITIONALLY (resolving through `runner_billing`, so it is an inherent no-op for a cache subscription and idempotent on redelivery), so a lapsed runner subscription cannot retain entitlement (`apps/signup-worker/src/webhooks/stripe.ts:1770-1795`, `apps/signup-worker/src/webhooks/stripe.ts:1835-1858`).

# Gotchas

- **This is the deployed activation path; the container webhook is SECONDARY.** This handler is the only writer on the LIVE path that flips the row to `active` (the in-process `corelink-tier-selection::ledger` is test-only and never runs in production) (`apps/signup-worker/src/webhooks/stripe.ts:689-693`).
- **`subscription_state='active'` is the canonical access gate, not `tenant_billing`.** The quota path reads `tier_selections.subscription_state='active'`; `tenant_billing` is the secondary mirror, so every entitlement change updates the canonical gate here, not just the billing row (`apps/signup-worker/src/webhooks/stripe.ts:32-35`).
- **`checkout.session.completed` cannot price-cross-check.** The session payload carries no price-shaped data and the handler makes no Stripe API calls, so the price↔tier mismatch assert runs only on the subsequent `customer.subscription.created`/`updated` event, where the price IS in the payload — this is the SAME reason a runner entitlement can only be seeded on the subscription event (checkout sessions carry no price), while the checkout branch only maps the runner subscription into `runner_billing` (`apps/signup-worker/src/webhooks/stripe.ts:1355-1362`).
- **`current_period_end_ms` is null at activation.** The session object does not carry it; it is backfilled by the `customer.subscription.created` event Stripe fires immediately after, so do not read it right after the activation write (`apps/signup-worker/src/webhooks/stripe.ts:1310-1313`).
- **A missing `BILLING_DB` binding fails CLOSED.** Without the D1 binding the handler returns 503 (not a silent 200) so Stripe retries — a paid checkout is never acked with zero entitlement writes; the runner writes ride the same `requiredWrites` await/500-on-failure durability contract (`apps/signup-worker/src/webhooks/stripe.ts:1152-1157`).
- **The dispatched event allowlist is a single source of truth, not a literal.** `HANDLED_EVENT_TYPES` is built from `handled-stripe-events.json` — the SAME file the reconcile script (`scripts/ops/stripe-reconcile-webhook-events.sh`) sets the live Stripe endpoint's `enabled_events` from — so the runtime dispatch set and the dashboard subscription cannot silently drift (a live endpoint subscribed to fewer events than the code handles would never receive a cancel/downgrade delivery) (`apps/signup-worker/src/webhooks/stripe.ts:492`).

# Citations

1. `apps/signup-worker/src/webhooks/stripe.ts:180-238` — `verifyStripeSignature`: HMAC-SHA256 over `${timestamp}.${rawBody}`, parsed from the `t=…,v1=…` header, compared over the raw body.
2. `apps/signup-worker/src/webhooks/stripe.ts:209` — the ±5-minute replay guard on the signed timestamp.
3. `apps/signup-worker/src/webhooks/stripe.ts:234` — the constant-time-ish XOR-fold compare (`diff === 0` accepts).
4. `apps/signup-worker/src/webhooks/stripe.ts:542-549` — the `INSERT OR IGNORE` idempotency claim into `stripe_webhook_events_processed` (process-then-claim, exactly-once emit).
5. `apps/signup-worker/src/webhooks/stripe.ts:724-750` — `activatePaidTierSelection`: `INSERT … ON CONFLICT DO UPDATE SET subscription_state='active'`, the activation write the edge quota gate reads.
6. `apps/signup-worker/src/webhooks/stripe.ts:292-316` — `tierFromSubscriptionPrice`: the `STRIPE_PRICE_ID_{TIER}` cache price→tier reverse map.
7. `apps/signup-worker/src/webhooks/stripe.ts:315` — the fail-safe null return when no configured cache price matches.
8. `apps/signup-worker/src/webhooks/stripe.ts:32-35` — the canonical access gate is `tier_selections.subscription_state='active'`; `tenant_billing` is the secondary mirror, updated on every entitlement change.
9. `apps/signup-worker/src/webhooks/stripe.ts:689-693` — this handler is the only writer on the LIVE path that flips the row to `active` (the in-process ledger is test-only); the container webhook is secondary.
10. `apps/signup-worker/src/webhooks/stripe.ts:1355-1362` — why the price↔tier cross-check cannot run on `checkout.session.completed` (also why the runner entitlement is seeded on the subscription event, not the checkout).
11. `apps/signup-worker/src/webhooks/stripe.ts:1152-1157` — fail-CLOSED 503 when `BILLING_DB` is unbound.
12. `apps/signup-worker/src/webhooks/stripe.ts:1310-1313` — `current_period_end_ms` left null at activation, backfilled by `customer.subscription.created`.
13. `apps/signup-worker/src/webhooks/stripe.ts:492` — `HANDLED_EVENT_TYPES` is built from `handled-stripe-events.json`, the single source of truth the reconcile script also uses to set the live endpoint's `enabled_events` (runtime allowlist ↔ dashboard cannot drift).
14. `apps/signup-worker/src/webhooks/stripe.ts:449-482` — `runnerEntitlementFromSubscriptionPrice`: reverse-maps the DISJOINT `STRIPE_PRICE_ID_RUNNER_*` env set → runner entitlement (null for a cache price).
15. `apps/signup-worker/src/webhooks/stripe.ts:951-1054` — the runner D1 writers: `upsertRunnerBilling` (0087 subscription→tenant map), `upsertRunnersEntitlementBySubscription` (seed 0070), `revokeRunnersEntitlementBySubscription` (revoke), `markRunnerBillingStatusBySubscription` (status mirror).
16. `apps/signup-worker/src/webhooks/stripe.ts:1247-1300` — the `checkout.session.completed` RUNNER branch: maps the runner subscription into `runner_billing` and returns without any cache activation.
17. `apps/signup-worker/src/webhooks/stripe.ts:1520-1595` — the `customer.subscription.created`/`updated` runner seed-on-grant / revoke-otherwise dispatch (first events carrying the runner PRICE).
18. `apps/signup-worker/src/webhooks/stripe.ts:1770-1795`, `apps/signup-worker/src/webhooks/stripe.ts:1835-1858` — unconditional runner revoke on `invoice.payment_failed` / `customer.subscription.deleted` (resolved through `runner_billing`; no-op for a cache subscription).
