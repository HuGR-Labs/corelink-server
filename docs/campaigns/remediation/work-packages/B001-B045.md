# Work-package contracts — B-001..B-045

> **Planning fragment, read-only census converted to contracts.** Baseline:
> `origin/main@76fa37de9bcf12b5e562edce129ad1328f9f8be2`, fetched 2026-09-02
> (America/Sao_Paulo). This document records scope and decisions; it does not
> claim that any live state below remains true after the stated observation.
>
> **Authority and method.** `BACKLOG.md` is the source of truth. The contract
> shape follows `docs/internal/DELEGATION-WP-CONTRACT.md`; the dependency and
> gate discipline follows `docs/campaigns/remediation/ROADMAP.md`. Every WP
> below is exclusive: an agent may edit only its allowlist, and a shared
> `BACKLOG.md`/`CHANGELOG.md` update is a later lead integration action, not an
> excuse to overlap implementation worktrees.

```wp-coverage
B-008 WP-B008
B-012 WP-B012
B-013 WP-B013
B-028 WP-B028
B-029 WP-B029
B-032 WP-B032
B-035 WP-B035
B-044 WP-B044
```

## Coverage and live cross-check

The current open population in the inclusive numeric range B-001..B-045 is
exactly eight IDs:

| ID | status/owner at baseline | live observation made 2026-09-01 | execution class |
|---|---|---|---|
| B-008 | open / owner | No implementation PR identified; the workflow has already received HTTP 202 responses, but delivery to a human is unproven. | owner-only operational verification |
| B-012 | open / owner | No implementation PR identified; bot-created events still need a non-`GITHUB_TOKEN` credential. | owner-only GitHub configuration |
| B-013 | open / owner | No implementation PR identified; three private keys are outside the repository. | owner-only destructive local action |
| B-028 | open / tl | `gh api` reported 3 open alerts: `extract-zip` and two `image-size`, all without a published patch. | external advisory triage |
| B-029 | open / tl | `load-test-nightly.yml` still has its schedule commented out. | owner-gated staging enablement |
| B-032 | open / owner | Digest reported 7 overdue Critical vendors. | owner-gated compliance refresh |
| B-035 | open / owner | The eight v1.0.0 legal instruments still contain TLS 1.3 claims. | counsel/owner-gated legal revision |
| B-044 | open / tl | No runner PR identified; observe-only reconciliation exists, while the join/teardown mechanism remains unproven. | external platform observation, then inert implementation if proven |

The following IDs are deliberately not WPs because their baseline status is
`done`: B-001, B-002, B-003, B-004, B-005, B-007, B-009, B-010, B-011,
B-014, B-015, B-016, B-017, B-018, B-019, B-020, B-021, B-022, B-023,
B-024, B-025, B-026, B-027, B-030, B-031, B-033, B-034, B-036, B-037,
B-038, B-039, B-040, B-041, B-042, B-043 and B-045. B-006 is `parked`.
No ID in the range is omitted or represented twice.

The station classification branch `origin/claude/esforco-classificacao-4e1455`
is a historical classification at `401f9d3e` (115 open items), not live state.
It agrees on the broad owner/external shape but predates the current B-044
`owner: tl` reclassification and the current 107-item open population. The
roadmap is likewise an execution-method source, not a current status source.

## Contract template and lead obligations

Each WP below includes: exact allowlist and non-goals; read-first material;
decided change; invariants; completeness population and failure modes; DoD;
quality/gates; predecessor and integration order; status/PR disposition; and
the required return card. The delegate must use the closed-world clause:

> Do not add anything not listed in this WP. If something else is required,
> stop and report it in the card instead of doing it.

The lead owns line-by-line diff review, citation verification, test execution,
mutation/revert proof, `[Unreleased]` CHANGELOG entry, DCO, secrets checks,
OKF re-anchor after final rebase, and `bash scripts/pre-merge-gate-check.sh
--merge <PR>`. A clean GitHub status alone is not DoD.

## WP-B008 — PagerDuty delivery verification (owner-only)

**Scope and exclusive allowlist.** No repository edits. Use only the PagerDuty
read-only incident/escalation log and the already-deployed evidence paths
`.github/workflows/audit-chain-daily-verify.yml`,
`.github/workflows/audit-archive-lag.yml`, and
`.github/workflows/billing-reconcile-daily.yml` as read-only references.

**Read first.** Read B-008 in `BACKLOG.md`; `docs/internal/2026-08-24-owner-decision-brief.md` §3;
the dispatch steps in the three workflows; and
`crates/corelink-slo/src/pagerduty.rs` to distinguish the in-memory dispatcher
from external Events API delivery.

**Decided change.** The owner emits a PagerDuty read API key; the lead
correlates every relevant accepted event with an incident and escalation
timeline. A HTTP 202 is recorded as API acceptance only, never as proof that a
person was paged.

**Non-goals.** Do not rotate or replace `PAGERDUTY_ROUTING_KEY`; do not create
an integration; do not add a new sink; do not expose event payloads or secrets.

**Invariants.** Existing write routing remains byte-compatible. Incident
correlation covers the same period and event IDs as the workflow evidence.
Absence of an incident is an explicit negative result, not a green shortcut.

**Completeness and failure modes.** The population is all SEV-0/SEV-2 events
dispatched by the cited workflows in the observation window. Missing read
credential, incomplete incident export, or only an HTTP response without
timeline evidence is HALT and leaves B-008 open.

**Definition of Done.** Owner access exists; the complete event set is
correlated to delivered/escalated incidents or a documented no-delivery result;
the owner decision and residual risk are recorded; the backlog status is then
updated by the lead.

**Quality/gates.** No secrets in evidence; timestamps and event identifiers are
retained; no inference from prose. Since this WP has no repo diff, repository
CI is not a substitute for the PagerDuty evidence.

**Predecessor and integration.** No technical predecessor. It is owner-only and
can be executed independently; lead records its result before any alerting
claim is reused by another WP. No PR is active for B-008 at the baseline.

**Return card.** `observed_at`; access method (without key material); event
population/query; delivered/escalated/not-found result; evidence references;
residual decision; blockers. Never include the API key.

## WP-B012 — bot-created PR CI credential (owner-only)

**Scope and exclusive allowlist.** No repository edits. Only GitHub repository
Actions/secret settings or creation of a dedicated GitHub App/PAT.

**Read first.** B-012 and its owner brief; workflow trigger semantics in
`.github/workflows/`; the `GITHUB_TOKEN` recursion rule; and the secret
handling rows in `docs/internal/secrets-checklist.md`.

**Decided change.** The owner creates a fine-grained PAT or GitHub App
credential dedicated to bot PR creation/updates with exactly the needed
`contents:write` and `pull_requests:write` authority, then wires the bot to
use it.

**Non-goals.** Do not reuse a release/deploy token; do not widen permissions;
do not modify workflow filters merely to make an unchecked PR appear green.

**Invariants.** One secret, one purpose; least privilege; masked logs; rotation
and revocation path documented; bot-created events receive ordinary checks.

**Completeness and failure modes.** The population is one genuinely
bot-created PR plus every required check expected for its changed paths. A
human-created PR, a green PR with skipped checks, or a credential that cannot
be rotated is not proof. GitHub API denial is HALT, not “no alerts”.

**Definition of Done.** Credential provisioned, a bot PR opened/updated, checks
observed, and rotation/revocation evidence recorded. The lead then updates the
BACKLOG status.

**Quality/gates.** No secret value in the repository or card; inspect the actual
event actor and check set; verify no release secret was reused. No repo PR is
needed for the external-only action.

**Predecessor and integration.** None. Owner-only and independent of B-028 and
B-133; do not combine it with Dependabot-policy code changes. No active B-012
PR exists at the baseline.

**Return card.** Credential type/scopes (not value); bot event ID; check set;
rotation owner; evidence; blockers.

## WP-B013 — private-key secure deletion (owner-only)

**Scope and exclusive allowlist.** The owner’s local filesystem only, exactly:
`~/Downloads/corelink-app.pk8.pem`,
`~/Downloads/corelink-runners-fleet.2026-06-15.private-key.pem`, and
`~/Downloads/corelink-runners.2026-07-13.private-key.pem`.

**Read first.** B-013 in `BACKLOG.md` and the owner brief. Confirm the target
names before acting; `githugr-clerk-pubkey.pem` is explicitly public and is not
a target.

**Decided change.** The owner performs secure permanent deletion of the three
private-key files and independently checks their absence.

**Non-goals.** No agent deletion; no repository cleanup; no deletion of the
public key; no broad recursive cleanup of `~/Downloads`.

**Invariants.** Exactly three private targets; public material remains; target
paths are resolved before deletion; no claim is made about backups outside the
target directory.

**Completeness and failure modes.** All three paths must be absent after the
operation. A missing path before deletion, permission failure, or unexamined
duplicate is recorded as incomplete, not silently accepted.

**Definition of Done.** Owner confirms secure deletion and per-path absence;
the lead records the external evidence and updates B-013.

**Quality/gates.** This is a destructive owner action, not an agent PR. Never
print key contents or hashes; retain only filenames, sizes/dates if needed.

**Predecessor and integration.** None. Independent and owner-only.

**Return card.** Timestamp; three per-path outcomes; public-key preservation;
known limits (for example, backups not inspected).

## WP-B028 — Dependabot alert adjudication

**Scope and exclusive allowlist.** Read-only GitHub Dependabot API plus
`package.json`, `pnpm-lock.yaml`, and `pnpm.overrides`. A later lead-only
BACKLOG evidence update is outside the delegated allowlist.

**Read first.** B-028; current Dependabot alert objects; `package.json` override
for `ip-address`; lockfile dependency paths; build/bundle manifests; and the
Dependabot policy/runbook. At the dated census the three open alerts were
`extract-zip` (high, no patch) and two `image-size` (high, no patch).

**Decided change.** Produce one reachability and remediation verdict per live
alert: patch when a vendor patch exists; otherwise record no-patch status,
build-time reachability, exposure analysis, and a recheck trigger. Do not
manufacture closure by dismissing an unpatched advisory.

**Non-goals.** No speculative dependency upgrade; no lockfile churn; no policy
workflow rewrite; no dismissal without an explicit reason and owner.

**Invariants.** Compare the set of alert IDs/packages, not only its count.
Each alert has a verdict. API absence caused by missing permission is HALT.
Build-time-only is an evidence-backed reachability claim, not “safe” by itself.

**Completeness and failure modes.** Population is every alert returned with
`state=open` at execution time, including newly appearing alerts. Truncated
pagination, empty API output, unknown manifest path, or unresolved transitive
reachability leaves the WP incomplete.

**Definition of Done.** Every live alert has advisory version, patch status,
dependency path, production reachability verdict, rationale, and recheck date;
the lead verifies the claims and updates B-028 only if its closure semantics
are actually met.

**Quality/gates.** No unsupported “fixed” claim; no secret output; test any
manifest change in both old/new dependency states; run relevant package audit
and the pre-merge gate if a lead-authorized diff later exists.

**Predecessor and integration.** None. Read-only and disjoint from B-012 and
B-133. No active B-028 PR exists at baseline.

**Return card.** Observation date; API query/pagination; exact alert set;
per-alert verdict/evidence; unresolved risks; no fabricated test claims.

## WP-B029 — staging load-test enablement (owner-gated; depends on B-070)

**Scope and exclusive allowlist.** Repository files
`.github/workflows/load-test-nightly.yml` and
`.github/workflows/endurance-2h-nightly.yml`; the staging DNS/Worker/D1/R2
deployment; and the `K6_TARGET_HOST`/`K6_STAGING_*` secrets consumed by those
workflows. Do not touch unrelated load scripts.

**Read first.** B-029 and §8 of the owner brief; both workflows; all referenced
`tests/load/k6/*` scenarios; `scripts/load-test-baseline-check.py`; and B-070
for the staging environment contract.

**Decided change.** Recommended path: after the owner approves the cost,
stand up a real staging deployment and, in the same changeset that wires its
secrets, re-enable the weekly schedule. If the owner chooses retirement, stop
and hand back a separate retirement contract; do not partially execute this
enablement WP.

**Non-goals.** Never point at production; never re-enable cron against missing
staging; never make an empty/failed matrix green; never change the regression
threshold merely to pass.

**Invariants.** Target hostname is staging/dev and is rejected otherwise. All
scenario secret references resolve in the staging environment. Every requested
scenario emits a result. Baselines are environment-specific and are reset on
incompatible staging changes.

**Completeness and failure modes.** Population is the complete matrix:
`signup`, `webhook`, `dsr`, `cas`, and `byok`, plus every secret reference in
both workflows. Missing DNS, any missing secret, empty summary, truncated
matrix, or a baseline produced by a different environment is HALT.

**Definition of Done.** B-070 is deployed/proven first; staging DNS and
deployables are verified from the destination; all scenarios run manually;
baseline comparison is proven both red and green; the schedule is re-enabled
only in the same reviewed change as staging wiring.

**Quality/gates.** No wall-clock assertions; inspect production-like destination
state rather than workflow prose; run k6 syntax and baseline self-tests; run
secrets-matrix/checklist validation, DCO, CHANGELOG and pre-merge gate. The
lead must verify the schedule polarity against B-029 after merge.

**Predecessor and integration.** Owner cost decision first; **B-070 is a hard
predecessor**. Integrate after B-070 and before any claim that nightly load
regression exists. This WP is exclusive of B-032 and all Dependabot work.

**Return card.** Decision (enable/retire); B-070 evidence; destination refs;
secret-name inventory without values; scenario result set; baseline evidence;
gate outputs; blockers.

## WP-B032 — Critical-vendor review refresh (owner-gated)

**Scope and exclusive allowlist.** `specs/_compliance/VENDOR-RISK-REGISTER.md`,
the seven Critical-vendor due-diligence evidence records, and the Drata
workspace/API. The seven-vendor population is Cloudflare, Stripe, Clerk, AWS,
Google Cloud, Azure, and Drata.

**Read first.** B-032; `VENDOR-RISK-REGISTER.md` §§1–5;
`VENDOR-RISK-METHODOLOGY.md`; each Critical vendor-DD file; the compliance
weekly digest and its runbook; and the owner brief §6.

**Decided change.** The owner obtains current SOC 2/ISO and vendor evidence
through Drata/approved vendor channels; the lead then updates the register
version and per-vendor records with dates, links and reviewer identity.

**Non-goals.** Do not backdate reviews; do not mark an item current without
evidence; do not confuse the overdue threshold with the >2× paging trigger;
do not rewrite sealed historical baselines.

**Invariants.** Exactly seven Critical vendors are covered. Evidence is
vendor-specific and current to each cadence. A digest with `vendor_breaches=0`
is claimed only after all seven records are updated and independently checked.

**Completeness and failure modes.** Missing Drata access, stale/partial
attestation, broken evidence link, or only six of seven refreshed records keeps
the WP open. A digest parser failure is not evidence of zero breaches.

**Definition of Done.** Seven reviews complete, evidence archived, register
version/effective dates updated, digest reports zero overdue, and compliance
gates/runbook checks pass. Lead updates B-032 afterward.

**Quality/gates.** Preserve auditability and reviewer attribution; no invented
attestation. Run `python3 scripts/compliance-weekly-digest.py --dry-run --json`
without a status-swallowing pipe; run docs/secrets/compliance checks and the
pre-merge gate for any authorized repository diff.

**Predecessor and integration.** Owner must provision Drata access. No technical
predecessor. Integrate as a serial compliance-register change; no other WP in
this interval edits these files. No active B-032 PR exists at baseline.

**Return card.** Observation date; seven-vendor set; evidence refs/dates;
digest output; unresolved vendor actions; owner/counsel blockers.

## WP-B035 — counsel-approved TLS legal revision (owner/counsel-gated)

**Scope and exclusive allowlist.** Exactly these eight versioned instruments:

- `legal/dpa/v1.0.0.en-US.md`, `legal/dpa/v1.0.0.pt-BR.md`,
  `legal/dpa/v1.0.0.es-419.md`;
- `legal/dpa/STANDARD-CONTRACTUAL-CLAUSES-EU.md`;
  `legal/dpa/SUB-PROCESSOR-COMMITMENTS.md`;
- `legal/privacy-notice/v1.0.0/en-US.md`,
  `legal/privacy-notice/v1.0.0/pt-BR.md`,
  `legal/privacy-notice/v1.0.0/es-MX.md`.

`apps/docs/src/pages/trust/center.tsx` may be changed only after the approved
replacement version exists. No other legal or marketing surface is in scope.

**Read first.** B-035; ADR-0072; all eight instruments; the DPA metadata and
related-documents declarations; the sign-up assertion in the Trust Center; and
§7 of the owner brief.

**Decided change.** Recommended path: draft and publish v1.0.1 with the
accurate TLS 1.2 minimum/1.3-when-supported wording, preserve v1.0.0
byte-intact, obtain counsel approval, then repoint sign-up and handle notice to
existing v1.0.0 signers as counsel directs.

**Non-goals.** Do not edit v1.0.0 in place; do not self-set
`legal_review_status: approved`; do not raise the edge floor to 1.3 as an
engineering shortcut; do not alter unrelated collateral.

**Invariants.** Historical executed bytes remain immutable. Every replacement
has version, effective date, related-document consistency and counsel identity.
Published claims never exceed the actually enforced TLS floor.

**Completeness and failure modes.** All eight claims must be corrected in the
approved version. Missing counsel approval, one untouched instrument, broken
sign-up pointer, or absent notice decision is HALT. A draft is not a legal
closure.

**Definition of Done.** Counsel-approved v1.0.1 is published; sign-up points to
it; the eight-file verify is green in done polarity; historical v1.0.0 remains
unchanged; legal/docs checks and lead review pass.

**Quality/gates.** Exact textual diff reviewed by counsel; no fabricated
approval; run legal/docs reality and secrets checks, add DCO/CHANGELOG, re-run
OKF after final rebase, and merge only with the pre-merge gate.

**Predecessor and integration.** Owner/counsel approval is a hard predecessor.
No technical predecessor. Integrate serially because these are a single legal
package, but they are disjoint from the staging, vendor-register and PagerDuty
WPs. No active B-035 PR exists at baseline.

**Return card.** Counsel decision and approval reference; version/effective
date; eight-file verification; sign-up pointer; v1.0.0 preservation proof;
notice decision; gates; blockers. Never claim approval from a draft.

## WP-B044 — orphan-box reconciliation and teardown proof (external)

**Scope and exclusive allowlist.** The sibling `corelink-runners` repository
only: `deploy/cloudflare/src/index.ts`, symbols
`reconcileOrphanBoxes`, `listRunningInstances` and
`listSpawnedBoxHandles`; the directly associated runner runbook and flag/secret
declarations. No `corelink-server` source or backlog edit belongs in the agent
worktree.

**Read first.** B-044; the merged observe-only reconciliation in runners;
Cloudflare Containers API behavior; the `sbox:` handle writer/reader; and the
existing runner deployment/metrics runbooks. Take the application scope and KV
pagination as established pre-work, not as new work.

**Decided change.** First observe a real candidate and classify it by the
instance-to-handle join. Only a type-1 candidate (a box spawned by our handle
whose `sbox:` write was lost) may proceed to an inert teardown implementation
behind the `RECONCILE_ORPHAN_TEARDOWN` secret. A type-2 candidate or a window
with no candidate produces an explicit “programmatic teardown blocked/not
proven” record and keeps observe + alert; it does not authorize a destroy.

**Non-goals.** Do not use the account-level instance endpoint; do not assume
the 403 instance-delete probe is a fix; do not destroy a candidate before its
type is proven; do not enable the teardown secret in the implementation PR; do
not change `corelink-server`.

**Invariants.** Instance enumeration is restricted to the runner app. KV
enumeration is fully paginated and fails closed on truncation/error. A name in
the historical `sbox.h` set is required for type-1 classification. Type-2 or
unknown candidates are never destroyed. The teardown path is inert by default,
has a per-tick destroy cap, namespace routing, a distinct
`orphan_box_reaped` metric, and a floor above the longest job because destroy is
liveness-blind.

**Completeness and failure modes.** Population is every observed runner
instance/candidate and the complete paginated `sbox:` set over the observation
window. Zero scans, missing log ticks, truncated output, pagination errors,
unclassifiable names, or no proof that the join is live are HALT—not evidence
of safety or closure. A type-1 path is incomplete until its inert behavior is
tested and destination-observed; type-2/none is complete only as a documented
external limitation with observe/alert retained.

**Definition of Done.** The observation window and complete population are
recorded; candidate type is proven or the external limitation is evidenced; if
type-1, the inert teardown, cap, metric, routing and runbook are reviewed and
tested, then observed live before any owner-controlled enablement; if type-2 or
none, no teardown is shipped and B-044’s honest blocked/retired disposition is
recorded.

**Quality/gates.** Prove behavior from the runner deployment/logs, not prose or
workflow green. No live destroy during testing; no wall-clock assertions;
preserve fail-closed pagination and app scope. Run the runner repository’s
tests, deployment/config and secret checks, DCO/CHANGELOG/OKF checks where
applicable, and the pre-merge gate in the owning repository. A delegate must
not claim tests it did not run.

**Predecessor and integration.** B-002/#513 observe hardening is already
landed; the live observe credential/flag setup is a prerequisite. Integrate in
`corelink-runners` only after classification and destination proof. This WP is
external and disjoint from every server WP here; enabling the production secret
remains a lead/owner deployment action.

**Current PR status.** No B-044 PR was present in the live open-PR query dated
2026-09-01. The old station classification called this owner+external; current
`BACKLOG.md` says `owner: tl`, so the external API limitation—not a presumed
owner credential—is the present block.

**Return card.** Baseline/ref and observation timestamps; instance population;
complete `sbox:` pagination evidence; type classification; exact files changed
(if any); invariant/failure-mode results; inert/live destination evidence or
external limitation; gates; owner enablement state; blockers. No secrets or
unverified test claims.

## Lead integration order

1. Keep the six owner/external gates explicit: B-008, B-012, B-013, B-029,
   B-032 and B-035 cannot be “implemented around” their required human act.
2. Run B-028’s read-only adjudication independently; integrate any resulting
   BACKLOG evidence update serially because `BACKLOG.md` is shared authority.
3. Provision/verify B-070 before dispatching B-029. Do not merge a re-enabled
   schedule without destination proof.
4. Observe and classify B-044 in `corelink-runners`; only then authorize its
   inert implementation path, followed by live proof and owner-controlled
   enablement.
5. For every implementation PR, rebase onto the current `origin/main`, redo
   citation and mutation checks, and run the pre-merge gate without pipes.

## Required return-card shape for every delegated WP

`WP-ID`; baseline SHA; files actually changed (or “none”); ordered change
items; invariant evidence; completeness population and failure modes checked;
tests/gates run by the lead (delegates must not claim execution); unresolved
questions; owner/external blockers; proposed PR/commit if any. Any out-of-scope
requirement is reported, not implemented.
