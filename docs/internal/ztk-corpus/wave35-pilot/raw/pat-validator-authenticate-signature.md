# PatValidator::authenticate signature
vars: F=corelink-reapi/src/pat.rs

`PatValidator::authenticate` (F:225) in corelink-server => sig=`(token: &str, request_id: &str)` (two args). x single-arg `(token: &str)` shape.