---
title: "OKF Worker-edge-plane expansion — frozen authoring brief (BR5)"
type: "Audit"
status: "TO AUTHOR"
tags: ["okf","expansion","worker-edge"]
---
# The C10b oracle covered only crates/routes/adrs — the Worker EDGE PLANE (worker/src + apps/, holding auth/quota/money/tenant-resolution) had ~0 concepts. Author the 6 below + extend 4. CONTRACT: follow docs/internal/okf-wiki/01-okf-corelink-profile.contract.md + the format of an existing peer concept (read e.g. docs/knowledge/auth/pat-moat.md). Each cite MUST be the EXECUTED enforcer line (open it, confirm). checkpoint_sha = 7f62573f2be4f07352de830fe98f400bb1345adb. provenance: AUTHORED. timestamp 2026-06-27T00:00:00Z. Honest designed-vs-wired. Validate (C1-C10b) green before done. Do NOT run okf_index.py/commit (lead finalizes).

## NEW CONCEPTS (each cites the named executed enforcers; add ALL named files to source_files)
1. docs/knowledge/auth/edge-identity-resolution.md — Human/dashboard login: verify Clerk session JWT at the edge, resolve to tenant. Files: worker/src/lib/clerk_auth.ts, worker/src/lib/tenant_lookup.ts. Enforcers: clerk_auth.ts:173-174 (M1 azp present+allowlisted — Clerk lib skips azp when absent), :191 (M2 iss!==clerkIssuerUrl issuer pin), :228 (!claims.sub→401), :135-138 (CLERK_SECRET_KEY unbound→403 fail-CLOSED); tenant_lookup.ts:114 (SELECT FROM tenant WHERE clerk_user_id=?1), :125-126 (no row→404). githugr multi-issuer arm: clerk_auth.ts:127 + :346 — DORMANT (gated on GITHUGR_CLERK_ISSUER_URL/GITHUGR_TENANT_ID secrets). Core Clerk verify = wired+live.
2. docs/knowledge/auth/edge-pat-mint-lifecycle.md — The single edge PAT-mint authority (mintScopedPat) + 3 consumers (session→PAT exchange Seam C, runner mint/revoke, rotation), all reusing the container /_internal/pat/mint (one signing key, one audit). Files: worker/src/lib/session_exchange.ts, worker/src/lib/runner_mint.ts, worker/src/lib/auth_rotate.ts. Enforcers: session_exchange.ts:719-720 (audience!==tenantId→403 cross-tenant), :466-468 (mint throttle→429 fail-CLOSED), :153-163 (canonicalizePatScope unmappable→null→500), :568 (INSERT INTO pat); runner_mint.ts:174+:184 (SELECT runners_entitlement→no row→403, separate Runners axis, mig 0070), :280 (UPDATE pat SET revoked_at_ms WHERE pat_id=?2 AND tenant_id=?3 AND revoked_at_ms IS NULL); auth_rotate.ts:234 (SELECT old), :260 (pat-not-owned→403), :243 (revoked/missing→404). All wired+live.
3. docs/knowledge/launch/edge-quota-tier-serving.md — Edge read-side tier resolution + per-tier quota enforcement (incl. the billing launch-blocker filter). File: worker/src/lib/quota.ts. Enforcers: quota.ts:152 (SELECT tier FROM tier_selections WHERE tenant_id=?1 AND subscription_state='active' — refuses paid tiers on non-active rows; the launch-blocker filter), :171 (fallback tenant.tier), :344 (SUM bytes_used storage cap), :523 (atomic monthly INSERT...ON CONFLICT request quota), :85-92 (per-tier QUOTAS ceilings), F21 fail-open symmetry :187-188 + :234/:327/:336/:356. Live + load-bearing.
4. docs/knowledge/launch/stripe-activation-webhook.md — The LIVE Stripe subscription webhook that activates the tier_selections row quota.ts:152 reads. File: apps/signup-worker/src/webhooks/stripe.ts. Enforcers: :162-219 (verifyStripeSignature HMAC-SHA256 over ${ts}.${body}), :191 (±tolerance replay guard), :216 (constant-time-ish diff==0), :418-419 (INSERT OR IGNORE stripe_webhook_events_processed idempotency), :600-615 (INSERT INTO tier_selections...ON CONFLICT DO UPDATE SET subscription_state='active' — the activation write), :280-295 (price-id→tier map). Live (the deployed activation path; container webhook is secondary).
5. docs/knowledge/flows/signup-auto-provision.md — Clerk webhook → auto-provision tenant+PAT on user.created; enqueue GDPR erasure on user.deleted. Files: apps/signup-worker/src/webhooks/clerk.ts, apps/signup-worker/src/index.ts. Enforcers: clerk.ts:216 (deterministicDsrId), :295/:318 (no_user_id/no_tenant short-circuit), :323-326 (DSR_QUEUE unbound→500 fail-closed, never silent-drop), :388-392 (erasure enqueued); index.ts:126 (Svix/Stripe sig-header scrub regex), :26-32 (route table). Live.
6. docs/knowledge/compliance/dsr-edge-crons.md — Scheduled DSR verify sweep + erasure-queue consumer + PAT scrub cron. Files: apps/signup-worker/src/lib/dsr_verify_cron.ts, apps/signup-worker/src/lib/dsr_consumer.ts, apps/signup-worker/src/lib/pat_scrub_cron.ts, apps/signup-worker/src/index.ts. Enforcers: dsr_verify_cron.ts:165-168 (SELECT FROM dsr_requested WHERE status='requested' AND requested_at<=?1 — 24h SLA sweep), :99 (POST /_internal/dsr/verify); dsr_consumer.ts:59-64 (no erase key→retry fail-closed), :68-72 (POST /_internal/dsr/erase w/ x-corelink-internal-auth), :78-79 (svc-binding fetch); pat_scrub_cron.ts:166 (per-user PAT scrub PATCH); index.ts:89 (scheduled()). Crons wired+live (dsr_requested table present in prod D1). (Confirm the actual lib/ path of these crons — they may be apps/signup-worker/src/lib/ or src/crons/.)

## EXISTING CONCEPTS TO EXTEND (claims + source_files)
- planes/worker-edge.md: add claims for index.ts:427-470 CLIENT_TRUST_HEADERS + :475 h.delete() (server-trust-header strip / sole-setter) and index.ts:1574-1601 (padded constant-time /_internal/* gate) + :1565 resolveConsumerKey fail-closed. (index.ts already declared.)
- planes/durable-object.md: += worker/src/event_log_do.ts (ADR-0065 per-tenant append-only event-log DO; append/read event_log_do.ts:220-279) and worker/src/rollout_controller.ts — CAVEAT: rollout_controller is an UNWIRED stub (:48-52 returns NOT_IMPLEMENTED "WASM bridge not yet wired (Phase C)"); EVENT_LOG_DO binding is OPTIONAL (index.ts:55-59) — confirm bound in wrangler before any live claim.
- auth/pat-moat.md: note internal_auth.ts:75-89 resolveConsumerKey (per-consumer key + shared fallback, MIN_INTERNAL_AUTH_KEY_LEN=32) + :112-125 constantTimeSecretEqual (shared primitive). (internal_auth.ts already declared.)
- security/posture-overview.md: += apps/signup-worker/src/security-headers.ts (CSP/HSTS edge headers). Confirm path.
