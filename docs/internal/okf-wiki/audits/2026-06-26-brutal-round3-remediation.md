---
title: "OKF brutal Round-3 (7-lens) — remediation worklist"
type: "Audit"
status: "TO APPLY"
tags: ["okf","audit","brutal","remediation"]
---
# Round-3 remediation. Round 3 found FEWER but SHARPER (47): the value was B3 omissions + B6 exploit-defender + B4 full-ADR. Confirm each line in THIS worktree before citing. Describe REALITY honestly (omissions → caveats, absolutes → qualified). Do NOT run okf_index.py / edit index.md / commit. Some findings are CODE issues captured separately in 2026-06-26-brutal-round3-code-findings.md — the wiki's job is to describe the imperfect reality accurately, NOT to claim it's fixed.

## CLUSTER C1 — ADR status-notes (B4) + tag-norm + adr-0033 label
Add a brief "## Status vs shipped code" clarifying note (do NOT rewrite the decision) to each, grounded at the cited line:
- adr-0020-quota-ownership-s07-s08: over-ceiling is 402 not 429 (tenant_quota.rs:799,825) — same as 0068.
- adr-0031-neon-schema-pgcrypto: auth/PAT store is D1/SQLite (customer_d1.rs), not Neon-pgcrypto/RLS; pseudonym = SHA-256(email.trim().lowercase()) not HMAC; Neon = feature-gated analytics shadow only.
- adr-0032-webauthn-level3: prod WebAuthn engine is a stub (EngineNotConfigured, corelink-auth/src/webauthn.rs:64); no webauthn-rs in Cargo.lock; designed-only.
- adr-0039-chunker-public-api-stability: no standalone corelink-chunker crate; chunker is a module of corelink-cas (corelink-cas/src/chunker.rs).
- adr-0044-sbom-cyclonedx-toolchain: live workflows pin different versions (sbom.yml:32 cargo-cyclonedx 0.5.4 + CycloneDX-CLI 0.27.2; cosign 2.4.3) — the "canonical pin" is stale.
- adr-s11-001-mfa-step-up-destructive-arms-only: shipped DSR = one internal POST /_internal/dsr/erase + account-delete enqueue (main.rs:525-532); no 7-endpoint API, no MFA wiring — designed-only.
- adr-s11-003-erasure-salt-interim: salt = deterministic HMAC-SHA256(ERASURE_SALT_KEY, dsr_id), single global key, recomputed on the fly (customer.rs:1111-1130) — NOT per-tenant CSPRNG + AES-GCM vault.
- adr-s11-005-consent-symmetric-grant-revoke-schema: consent/revocation tables are Neon (corelink-privacy/src/consent/store.rs), not D1, and not deployed in prod (per ADR-S11-013).
- adr-s11-006-consent-purpose-12-arm-closed-enum: the enum carries #[non_exhaustive] (consent/schema.rs:99) — the ADR's "non_exhaustive FORBIDDEN / closed enum" is contradicted by code; grouping 4/3/3/2 also mismatches schema.rs:140-156.
- adr-s11-008-sub-processor-default-subscribed-tier-team-plus: the canonical billing taxonomy is 6 (free|solo|starter|pro|max|enterprise), not "5 incl team/business" (those are eviction Tier enum names) — defer to adr-s19-001.
- adr-s12-046-license-allowlist-7-osi: deny.toml also allows BSL-1.0 + CDLA-Permissive-2.0 (ratified ADR-S32-001); "7-osi" undercounts.
- adr-s14-004-byok-trait-envelope-encryption: DekCache/AAD logic lives only in corelink-byok; not invoked on any deployed CAS read/write path — designed-only (soften the present-tense "enforced in code").
- adr-s14-006-byok-kill-switch-no-operator-override: no prod blob is BYOK-encrypted (envelope path unwired) → the kill switch guards nothing live — designed-only.
- adr-s30-001-byok-mutually-exclusive-providers: defaults to InMemoryFake (lib.rs:61-62); no byok/wrap_dek call site in routes/ — "ships across four providers" is designed-only (the compile_error mutual-exclusion guard IS real).
- adr-s33-001-team-multi-seat-membership-model: STALE THE OTHER WAY — it's BUILT: migration 0074_team_member.sql, invite() INSERTs invited row → 201 (not 501), seat-removal revokes PATs (customer_d1.rs:1140-1174). Update the "does not exist / 501 stub" framing. ALSO add a supersede/cross-link note: it CONTRADICTS adr-s19-001 ("REMOVE Team") — flag the unresolved team-tier product question (owner call).
- adr-0033-audit-events-cloudevents: FIX the round-2 label — the streaming fn is link_chain_hash_streaming (chain.rs:152-162); :188-191 is link_chain_hash_from_canonical (non-streaming). Repoint the "streaming variant" cite.
- TAG-NORM (B7): unify the singular/plural tag splits so the pairs co-cluster — adr-0015 reproducible-build(s), adr-0018/adr-0035 invariant(s), adr-s11-008/sub-processors sub-processor(s), adr-0068/dollar-ceiling (add a shared tag). Pick the form already used by the partner concept.

## CLUSTER C2 — OCI trust-model + quota-precision + circular-watchdog (B3/B6)
- tenancy/isolation: (a) acknowledge the OCI bearer-resolved tenant path (oci.rs:834-841; Worker deletes x-corelink-tenant-id at worker/src/index.ts:1817-1828) — soften "the ONLY trustworthy tenant source is the DO header / nothing else" to note OCI resolves tenant from the HMAC-verified bearer in-process (add oci.rs to source_files); (b) the container fail-closed sentinel set does NOT include `_oci` (auth_tenant.rs:19) — fix the claim that `_oci` is a rejected sentinel.
- tenancy/storage-quota-header: omits the 2nd cap-delivery path — on OCI the cap travels INSIDE the verified bearer token (corelink-adapter-host/src/oci/auth.rs:217,289,353) because the Worker deletes the header; soften "the header is the SOLE seam on every data-plane write-forward" and fix the "absent header → attack" framing for OCI (absence is normal there).
- tenancy/request-quota: (a) "closed the OCI bypass" overstates — the mirror is fail-OPEN on D1 fault AND only armed when StorageEnv present; (b) no-bearer OCI reads are never counted (the gate is inside `if let Some(tenant)=oci_bearer_tenant`).
- tenancy/governance: the per-tenant rate limiter fail-OPENs for OCI (header deleted) via a separate per-repo oci_limiter (ratelimit_layer.rs:133-161,204-208) — soften "uniformly tenant-keyed across the data plane".
- tenancy/dollar-ceiling: omits the LeasedQuotaStore 16-op budget-lease wrapper (tenant_quota.rs:112-125,366-621) — the ceiling is consulted in D1 once per ~16 ops, NOT per-op atomic; reword to "approximate by design (budget-lease)". ALSO add the circular-watchdog caveat (see below).
- auth/hmac-fast-reject: the "FATAL when prod detected" backstop is CIRCULAR — prod-detection = StorageEnv::from_env().is_some() && PAT_SIGNING_KEY (main.rs:246-250) and the exit(1) fires ONLY for the native PAT gate (:253-265); the metering guards just return None when StorageEnv is None. Disclose: a dropped R2_S3_*/D1 var makes prod-detection false → the FATAL never fires and the native planes mount WITHOUT the Argon2id backstop (+ $-ceiling/byte-cap/request-quota all silently off). The config that disables the controls also disables the watchdog.
- storage/r2-cas-bucket: (a) it asserts 3× "cross-tenant key co-residence is structurally impossible" but OMITS the `_public` SHARED namespace (r2_s3.rs:571-601, live via npm/brew/pip — one fixed sentinel-UUID prefix all tenants read/write) — add the carve-out (mirror isolation's fix); (b) omits the 2nd key shape `<region>/<prefix16>/bazel/sha256/<digest>` (r2_s3.rs:453-458).
- storage/byok-envelope-encryption: the "exactly one provider, multi-provider rejected at compile time" guard is bypassed by `_internal-*`/`_matrix-test` features that link all KMS SDKs without tripping compile_error! (corelink-byok/src/lib.rs:159-188); prod single-SDK enforcement is cargo-deny — note both.
- flows/billing-quota-check: omits the LeasedQuotaStore wrapper (tenant_quota.rs:112-125) — "atomic check-and-accrue per-op serialized in one DB statement / closes TOCTOU" describes the INNER store; the live guard pre-debits a 16-op chunk and serves ~15 ops from an in-memory lease without D1. Qualify.

## CLUSTER C3 — compliance attestation reality + audit-chain + batch (B3) + B2 pat-moat
- compliance/dsr-erasure: (a) the Stripe backend verification re-fingerprint is a hardcoded no-op — verification_hash always returns CANONICAL_EMPTY_TENANT_HASH (adapter_stripe.rs:140-150), so a VerifiedComplete attestation can be signed while Stripe PII survives — the "never attested complete while subject bytes survive" invariant is currently NOT enforced for the Stripe arm; state this plainly; (b) B2: "status poll is constant-time confidential" → outcome-confidential / no existence oracle (not timing-constant — the hit arm emits an extra StatusPolled audit, endpoint.rs:429-437).
- compliance/erasure-attestation: the live signer hashes a self-referential SYNTHETIC audit_chain_segment_ids bundle with compute_hash, NOT the fail-closed validated_hash (attestation.rs:139-151) — the "validation fails closed on any empty mandatory field" invariant is vacuous (the bundle is a never-empty constant); the attestation proves a signature was produced, NOT that verifiable deletion evidence exists. ALSO: at VerifiedComplete the tenant row is already deleted, so resolve_region misses and the attestation region falls back to ERASURE_ATTESTATION_REGION (attestation.rs:82-102) — an EU tenant's erasure can be signed with the env-default region. State both honestly.
- compliance/audit-chain: NO live producer seals events into the chain — HashChainBuilder::append is test-only; the live erasure/audit path writes plain UNCHAINED CloudEvents to audit_outbox with chaining deferred to the unbuilt S-09 drain (dsr/audit.rs:7-8,82-84). Stop presenting `append` as enforcing the link "at write time" in the live path; the live trail is currently mutable unsealed D1 rows (the chain logic is real + tested, but not wired to a live producer).
- flows/cas-write: the batch route handle_batch_write has NO CasPutGuard per-tenant concurrency reservation (cas.rs:832-839 vs the single PUT's :722) — fix the Gotcha "batch mirrors this exact gate sequence" and Invariant #3's peak-memory bound: the CAS_WRITE_CONCURRENCY_LIMIT=8 cap is bypassed on batch (each concurrent batch buffers up to BATCH_MAX_BYTES=8MB).
- auth/pat-moat: B2 — "re-runs the FULL Option-B verification" overstates; on the six cache surfaces NativePatGate::verify on a verify-cache HIT (native_pat_gate.rs:170-176) returns Ok after a tenant-equality check and SKIPS D1+Argon2id for the 5s TTL. Qualify the absolute (add native_pat_gate.rs if you cite it).

## CLUSTER C4 — re-fixes (B1) + introspect/credential (B6) + ops (B5/B7)
- security/attack-surface-controlplane: RE-AUTHOR — my round-2 fix OVER-CORRECTED. The container has only THREE dedicated-key gates (internal_pat.rs:698 PAT_MINT, admin.rs:439 ADMIN, admin.rs:460 ERASE), ALL forwarded (durable_object.ts:552-554). `grep RUNNER_MINT crates/corelink-container/src` = 0 — there is NO container runner-mint gate; CORELINK_RUNNER_MINT_AUTH_KEY is a WORKER-edge key (worker/src/lib/internal_auth.ts:81) and runner mint reuses the container's /_internal/pat/mint via the already-forwarded PAT_MINT key (runner_mint.ts). So CP-1 is FULLY CLOSED on the container plane. Remove the fabricated "3 of 4 / runner-mint residual / self-inflicted-outage landmine"; state CP-1 fully closed on the container, runner-mint isolated at the worker edge.
- compliance/data-residency: my round-2 fix slightly UNDER-corrected — "a full per-region native-CAS topology is unbuilt in code" understates the env-configured path (cas.rs:414 R2_CAS_BUCKET is env-selectable; prod-lhr binds corelink-cas-eu). Reword to: the per-regional-container env path EXISTS (env-selectable bucket) but is NOT a blanket/tenant-routed assurance, and the do-not-onboard-EU-native-CAS guardrail (ADR-S14-009) stands. Don't say "unbuilt".
- auth/introspect: "fail-CLOSED at every step, never serves a plan it could not resolve" is contradicted by the gotcha "absent max_vcpu_h ⇒ wall-off (let the job through)" — the vCPU-h metered axis fails OPEN; qualify the headline (a missing max_vcpu_h row runs unbounded vCPU-hours).
- security/credential-handling: "scrub cron so an un-visited welcome cannot leave it resident" is presented as complete, but the concept never confirms the scrub cron is wired+running in prod — add a caveat that PAT plaintext lives transiently in Clerk private_metadata until the (unverified-live) scrub cron, so an IR responder must NOT conclude "no live PATs in Clerk".
- crates/operations: B5 — the reservation-TTL throughput is RESERVATION_THROUGHPUT_BYTES_PER_MS=1024 bytes/ms (reservation.rs:64), ~1.024 MB/s, NOT "1MB/s"; fix the figure (and add reservation.rs to source_files if you keep the formula). B7 — retag `ratelimit` → `rate-limit` to match the canonical tag.
- ops/reproducible-build: B7 — unify the `reproducible-build(s)` tag with adr-0015's form so the pair co-clusters.
