---
title: "OKF brutal 6-lens audit — remediation worklist"
type: "Audit"
status: "TO APPLY"
tags: ["okf","audit","brutal","remediation"]
---
# Brutal-audit remediation. Confirm each line in THIS worktree before citing. Cite the IMPLEMENTING line, never a //!/barrel/trait-sig/const/test. Keep checkpoint_sha unchanged UNLESS told to re-checkpoint. Do NOT run okf_index.py or edit index.md (the lead regenerates once at the end). Verdict: TRUTH/symbols/numerics all held; these are grounding-precision + designed-vs-wired + residency-overclaim fixes.

## CLUSTER R1 — anti-drift enforcer coverage (add the REAL enforcer file to source_files + repoint the invariant cite off the lib.rs //! barrel)
- crates/audit-analytics.md: +`corelink-audit/src/redact.rs` (PII *Hash newtype defs ~:54 → INV-AUDIT-NO-RAW-PII), +`corelink-audit/src/link_hash.rs` (`compute_content_hash` :156 → JCS-determinism), +`corelink-transparency-log/src/submit.rs` (`witness_or_degrade` :87 → Rekor fail-OPEN). Repoint those 3 invariants off the `lib.rs` //! lines.
- crates/auth-pat.md: +`corelink-pat/src/types.rs` (Drop/zeroize :41,:83 + len<32 reject :219-226 → PatPlaintext-unloggable/PatSigningKey), +`corelink-clerk/src/jwks.rs:65` & +`corelink-clerk/src/adapter.rs:287-290` (RS256-only / alg-confusion defense). Repoint off the lib.rs //!.
- crates/billing-commerce.md: +`corelink-tier-selection/src/ledger.rs` (`select_tier` :191-260 → DPA-first gate + audit-atomic-with-handler + per-tenant D1 row-lock). Repoint all 3 off `lib.rs`/`dpa.rs` //!/sig.
- crates/container-platform.md: repoint config CAS-version + emit-before-mutate from `config-do/lib.rs` //! → `corelink-config-do/src/store.rs:223-251` (+source_files); repoint TenantScopedKey audit-hook from the r2_real.rs type/sig → the call site `corelink-cf-bindings/src/r2_real.rs:337-339` (audit_and_scope, called by put :414/delete :442).
- crates/operations.md: repoint to the IMPL lines (files already declared): blob_meta soft_delete impl (not trait-sig), reachable :177-210 (the `>=` protect :186-188), ratelimit `key.rs:82` (tenant-leftmost field order, just outside the cited range).
- crates/privacy-compliance.md: erasure-attestation-signed enforcer is the CONSUMER crate (`ErasureAttester::attest_erasure`) — declare it or note it's cross-crate; repoint DSR audit-fail-closed from the TEST fixture `dsr/audit.rs:292` → the real `?` propagation `corelink-dsr/src/endpoint.rs:312-318`; repoint zeroize from the //! to the `ZeroizeOnDrop` derive line.
- crates/adapter-hosts.md: INV-BAZEL-NO-GRPC has NO positive code enforcer — it's `corelink-bazel-bridge/Cargo.toml` dep-absence; say so (negative/dep-graph guard) + optionally +Cargo.toml.
- flows/governance.md: **RE-CHECKPOINT** — `checkpoint_sha cfb46abd` is stale; 5 cites drifted. Advance checkpoint_sha to current HEAD, make a real body edit (C5b), and repoint: rate-limiter `router.layer(from_fn_with_state)` :825 (not :819-822); revoke `requires_cache_write→FORBIDDEN` :783-790; `tenant()` def :242-245 + Err guard :231-234; router routes :185-198.

## CLUSTER R2a — residency + tenancy (the compliance pitch↔code cluster — HIGHEST product value)
- compliance/data-residency.md: **REMOVE the EU-CAS-is-live overclaim.** "prod-lhr binds corelink-cas-eu/corelink-ac-eu so EU CAS+AC bytes land in EEUR" CONTRADICTS its own cited ADR-S14-009: per-region CAS buckets are UNBUILT (gated on bucket provisioning, "cannot be completed in code alone"); native CAS region today is a LOGICAL `R2_CAS_REGION` tag, NOT physical EU residency; the launch invariant is "do NOT onboard a customer with a contractual EU data-residency requirement for native CAS." Reword to that reality. Also: serving colo reads `R2_CAS_REGION` but the bucket is keyed from `R2_CAS_BUCKET` (`cas.rs:414`, default corelink-cas-prod) — fix the "same env keys the bucket" overstatement. Verify the reject status code (concept says 409 `residency_violation`; ADR-S14-002 says 451) against `residency.rs` and use the code the handler actually returns.
- storage/r2-cas-bucket.md: region-in-key is a LOGICAL tag, NOT physical residency (ADR-S14-009: "the 'add R2_CAS_REGION' fix is a false-confidence trap and must not ship as the residency fix"). Reword "a region migration is a key-prefix change" so it does NOT imply residency-sufficiency.
- compliance/sub-processors.md: the concept claims 8 sub-processors incl Clerk, but the machine-validated `legal/sub-processors.md` lists only 7 (Clerk omitted) — state the ACTUAL validated count and flag Clerk as a disclosure gap (do NOT claim 8-incl-Clerk as validated). Also hedge "residency pinned per tenant.primary_region" → it's the global `R2_CAS_REGION`, not tenant.primary_region.
- tenancy/isolation.md: the absolute "tenant co-residence is structurally impossible / every blob belongs to exactly one tenant" MUST acknowledge the `_public` shared-namespace EXCEPTION (public-packages pip/brew dedup cross-tenant under `_public`; attack-surface-dataplane already acknowledges it). Add the carve-out.

## CLUSTER R2b — auth / erasure / native (cold-checked)
- surfaces/native-cas.md: **CORRECT the understatement** (cold-check confirmed). The native plane is NOT "pure key lookup" and Argon2id is NOT "only on the adapter plane". The NativePatGate backstop (red-team finding #4) runs HMAC→D1→**Argon2id** (cached; warm hit skips) at the TOP of each billable handler, AFTER scope+tenant, BEFORE storage (`native_pat_gate.rs:194`, `:140-194`); the read path also runs the $-ceiling quota gate + 410-tombstone before R2 when wired in prod (`cas.rs` CasHandlerState `quota`/`tombstones`/`pat_gate`). Keep HMAC-fast-reject for the fast path, but add the Argon2id backstop + read-path control-plane gates.
- compliance/erasure-attestation.md: drop the present-tense LIVE "persisted in R2 (7y) + indexed in D1 + served at GET /v1/public/keys/erasure/{region}.pub" (claims 2 & 15) → designed-not-wired (match claim-1's honest hedge + CLAUDE.md). Fix INV-ERASURE-ATTESTATION-SIGNED: it says MUST/fail-CLOSED/BYOK-only, but dsr-erasure (the shipped path) says best-effort/fail-OPEN, emitted from the 24h verify sweep for ordinary tenants → reconcile to best-effort/fail-OPEN + not-BYOK-only.
- compliance/dsr-erasure.md: line-drift — sign-gate is `dsr.rs:503` (not :443-446); the canonical-empty/sentinel determination lives in `corelink-privacy-erasure-worker` (add it or note cross-crate); receipt+SLA `endpoint.rs:327-333` (not store.rs:133-158); verify+sign :498-507; 422-Rejected :457-459.
- flows/introspection-fabric.md: "the single place that owns PAT verification" is FALSE — it DELEGATES to the native gauntlet (it says so at :27). Soften to "delegates to the shared native PAT gauntlet". Fix the `build_state_from_env` cite (this concept cites :660-683; auth/introspect cites :19-30 — open auth_introspect.rs and cite the ONE real definition line in both).
- auth/introspect.md: fix the `build_state_from_env` cite to the real line (reconcile with introspection-fabric).
- auth/argon2id-verify.md: "scope.rs:112-149 is THE single fail-CLOSED scope truth" conflicts with d1-pat-store "customer_d1.rs owns the scope vocabulary" — one overstates "single"; reconcile (scope.rs = the gate; customer_d1 map = the vocabulary source — name both roles). Cross-link ADR-0069: the fast-hash decision is about the HMAC FAST-REJECT layer; Argon2id remains the possession proof in the backstop/adapter — they are DIFFERENT layers, not contradictory.
- auth/d1-pat-store.md: line-drift — listing SELECT :902-908 (not :888-894); idempotent revoke UPDATE :1060-1064 (not :1049-1051); scope-map `map_requested_scopes` :307-324 (legacy-NULL handling is in `adapter_pat.rs:144-150`, not this file). Note revocation truth is D1 (`revoked_at_ms`); cross-link/supersede ADR-0030's stale "Neon pat.revoked_at is SoT".
- storage/storage-quota-header.md: REMOVE the fabricated "RFC 9331" label — the header is `X-Rate-Limit-Type` per the sprint contract §5; RFC 9331 is ECN/L4S, unrelated.

## CLUSTER R3 — stale + ADR supersede-notes
- security/attack-surface-controlplane.md: **STALE — CP-1 is now PARTIALLY CLOSED.** `worker/src/durable_object.ts:552-554` forwards 3 of 4 dedicated keys (PAT_MINT/ADMIN/ERASE); only RUNNER_MINT remains un-forwarded. Update the frontmatter description, intro thesis, How-it-works #2/#3, the "self-inflicted 401 outage" invariant, and the gotcha to reflect 3/4 forwarded (finding partially closed, isolation now real for pat-mint/admin/erase). **+`worker/src/durable_object.ts` to source_files** (fixes the audit-doc-as-only-source_file C5 blind spot). Re-checkpoint.
- planes/container.md: fix the Turbo bucket — `corelink-turbo-cache-prod` → real default `corelink-turbo-prod` (`crates/corelink-container/src/storage/r2_kv.rs:248`); +`r2_kv.rs` to source_files + cite :248. (The separate-store / no-tombstone-gate part is correct — keep it.)
- adr/adr-0030-revocation-propagation.md, adr/adr-0069-pat-fast-hash.md, adr/adr-0033-audit-events-cloudevents.md, adr/adr-0068-dollar-ceiling.md: add a brief "## Status vs shipped code" note where the ADR's described state differs from what shipped (0030: PAT store is D1 `revoked_at_ms`, not Neon; 0069: fast-hash is the fast-reject layer — Argon2id remains the possession backstop; 0033: the per-tenant chain hash is BLAKE3 per audit-chain, SHA-256 is only the Rekor hashedrekord; 0068: $-ceiling returns 402, never 429 — that's the request-count axis). Do NOT rewrite the decision; these are historical records + a clarifying status note.
