---
title: "OKF brutal Round-4 (verify-the-findings) — remediation worklist"
type: "Audit"
status: "TO APPLY"
tags: ["okf","audit","brutal","remediation"]
---
# Round-4 = adversarial VERIFICATION. It refuted 2 of my own Round-3 over-claims (attestation NOT theater; batch NOT a DoS), caught a 3rd controlplane over-correction, and found real DRIFT (sdk stubs-as-live, testing/ describes a rewritten harness). Owner's axis: the wiki must speak the TRUTH vs code + cite a line the anti-drift gate can WATCH. Confirm every line in THIS worktree. Do NOT run okf_index.py / edit index.md / commit.

## CLUSTER R4-A — attack-surface-controlplane (3rd authoring — get it EXACTLY right, cold-verify each line)
The Round-3 re-author swung to "CP-1 FULLY CLOSED" — WRONG. The verified truth:
- The container has 3 dedicated-key gates, each dedicated-PREFERRED (uses CORELINK_<X>_AUTH_KEY if set else shared): PAT_MINT `crates/corelink-container/src/routes/internal_pat.rs:698` (resolve_internal_auth_key), ADMIN `admin.rs:439`, ERASE `admin.rs:460`. The DO forwards all 3 dedicated keys to the container env `worker/src/durable_object.ts:552-554`.
- **admin + erase ARE genuinely closed:** the Worker's Path-A proxy forwards the per-consumer RESOLVED key to the container `worker/src/index.ts:1622`, so when the dedicated key is provisioned the admin/erase gate receives it → per-consumer isolation real.
- **pat-mint is NOT isolated (and provisioning its dedicated key is a live landmine):** the only live pat-mint callers — token-exchange `worker/src/lib/session_exchange.ts:374,493` and runner dispatch `worker/src/lib/runner_mint.ts:130` — hardwire the SHARED key `CORELINK_INTERNAL_AUTH_KEY` into `x-corelink-internal-auth` via `mintScopedPat`. So even though the DO forwards `CORELINK_PAT_MINT_AUTH_KEY`, the worker's pat-mint REQUESTS still send the shared key; with the dedicated key provisioned, `internal_pat.rs:698` resolves dedicated-preferred → rejects the shared key → 401. The self-inflicted-outage landmine MOVED from the (non-existent) runner-mint gate to pat-mint.
REWRITE the concept to this: CP-1 is 2/3 closed (admin+erase, per-consumer key forwarded via Path-A proxy); pat-mint isolation is NOT real because its live consumer sends the shared key, and provisioning the dedicated PAT_MINT key self-inflicts a 401 until the worker's mintScopedPat/token-exchange is updated to send the dedicated key. REMOVE the "fully closed / isolation real for all / runner-mint reuses the already-forwarded PAT-mint key" text and the wrong runner_mint.ts:127 cite (it shows the SHARED key — cite it as proof pat-mint sends shared). Keep checkpoint_sha or re-checkpoint to HEAD if you change cited ranges + make a real edit.

## CLUSTER R4-B — re-correct MY Round-3 over-claims to the verified truth (the wiki currently lies in the alarmist direction)
- compliance/dsr-erasure + compliance/erasure-attestation: Round-3 said "a VerifiedComplete can sign while Stripe PII survives / fail-closed invariant vacuous". VERIFIED WRONG (V1): the VerifiedComplete gate is DUAL — `orchestrator.rs:503` requires `prior.outcome.is_successful()` AND the canonical-hash check; the prior success tombstone is only written if the backend erase succeeded fail-CLOSED (`adapter.erase(...)?` propagates → no tombstone → VerifiedPartial → NO sign, `orchestrator.rs:339,449-477`). So PII surviving the erase BLOCKS VerifiedComplete — it is NOT a sign-while-PII-survives path. The synthetic bundle hard-codes exactly the 3 fields `validated_hash` checks (`evidence.rs:68-85`), so compute_hash ≡ validated_hash here (equivalent, not a bypass). The region fallback is correct (the erase deletes the tenant row by design; US-only; the published per-region key verifies). RE-CORRECT: the attestation is a real Ed25519 offline-verifiable artifact gated on a fail-CLOSED cross-backend erase; the ONLY real residual is that the Stripe verify-sweep re-fingerprint (`adapter_stripe.rs:140-150`) is a DEFERRED no-op (ADR-S11-013) — a defense-in-depth deepening, NOT theater. Keep the audit-chain "no live producer" point (V3 CONFIRMED it).
- flows/cas-write: Round-3 said batch is a "memory-amplification DoS". VERIFIED OVERSTATED (V2): every data-plane request is hard-capped at 10 MiB by `DefaultBodyLimit` (`main.rs:462`) and per-tenant arrival is rate-limited (100 rps / burst 200, `ratelimit_layer.rs:122,128` via `routes.rs:825`), so peak memory is BOUNDED. RE-CORRECT: the batch route omits the per-tenant CasPutGuard hard semaphore (`cas.rs:832-839` vs single PUT `:722`), giving it a LOOSER concurrency bound (rate-limit-derived, not a hard 8-slot) — a hardening gap, NOT an unbounded memory DoS. Soften the Invariant/Gotcha accordingly.
- auth/introspect: the "empty concurrency row → reject" cite `auth_introspect.rs:382-394` actually returns `Ok(None)` (field omitted, cache-only tenant), not a reject. Fix the wording/cite (the vCPU-h fail-OPEN disclosure at `:85-92` is accurate — keep it).
- auth/pat-moat: remove "constant-time" from "constant-time tenant-equality check" — `native_pat_gate.rs:172` is a plain String `==` (immaterial — tenant-id isn't a secret — but not constant-time).

## CLUSTER R4-C1 — sdk-reference stubs-as-live + the anti-drift blind spot (V5)
- ops/sdk-reference: the three SDKs are presented as the live way a customer talks to CAS, but the shipped network put/get/stat are STUBS — only the BLAKE3 verify core is real: JS/wasm `get_inner` returns `Vec::new()` with `// Stub` (`crates/corelink-wasm/src/lib.rs:180-181`, `put_inner:199`, `stat_inner:204-210`, `pat`/`tenant_id` are `#[allow(dead_code)]` :71,74); Python `get` stub (`tools/sdks/python/src/lib.rs:110`); Go bridge computes BLAKE3 only (`tools/sdks/go/src/go_bridge.rs:186-217,236`). REWRITE to disclose: the SDKs ship the real client-side BLAKE3 verify core; the network client (put/get/stat over HTTP) is a STUB / not yet wired. ADD the impl files to source_files so the anti-drift gate can SEE the stubs: `crates/corelink-wasm/src/lib.rs`, `tools/sdks/python/src/lib.rs`, `tools/sdks/go/src/go_bridge.rs` (cite the stub lines).
- surfaces/public-packages: How-it-works #6 cites `routes/oci.rs:754-790` which contains a STALE code comment ("charge on write methods PUT/POST/PATCH") contradicting the real gate that charges on EVERY method incl reads (`oci.rs:842-849`). Repoint the cite to `:842-849` (the real charge-all-methods gate).

## CLUSTER R4-C2 — testing/ + process-doc DRIFT + anti-drift blind spot (V6, the owner's core axis)
These concepts make CURRENT-CODE/CURRENT-STATE claims but their source_files are FROZEN docs → the C5 gate watches the doc forever and can never catch the drift. Fix BOTH the stale content AND the grounding (cite the real code/file so the gate can watch it):
- testing/gate-machinery: states as current "`expect_denied` accepts 401|403|404 across ~50 call-sites" + "runner exits 0 unless fail>0 (green-by-vacuum, all-gated prints GREEN)" — BOTH FIXED in code (#484/#489/#495/#501): the helper is now `expect_gate_denied` 401/403-only (`tests/e2e-user-journeys/src/harness.rs:436`) and the runner REFUSES green-by-vacuum / goes RED on all-gated (`tests/e2e-user-journeys/src/main.rs:117-163`). Update the prose to the fixed reality + ADD harness.rs + main.rs to source_files + cite them.
- testing/real-user-suites: the "77 PASS / 0 FAIL / 24 GATED / 8-of-12 personas never run / quota-hard-cap gated" counts are stale (un-gated by #488/#489/#490). Soften the frozen counts to "see the live suite" + cite the harness/runner where it makes a code claim; if a precise count can't be code-grounded, state it as a point-in-time snapshot, not current.
- testing/e2e-strategy: same M1/M3 (green-by-vacuum / expect_denied) described as live — reconcile to the fixed harness.
- ops/release-process: "262 spec docs / validate_specs.py exit 0" is stale (CLAUDE.md gate is 463/0); name `scripts/cut-v1-0-0-ga-tag.sh` as the mechanism — verify it exists + cite it; update the count or mark it a snapshot.
- (If time permits, scan ops/{deploy-runbook,perf-playbook,dev-deployment} + launch/go-live-readiness for the same "all-docs-source-files makes a current-code claim" pattern and either ground the claim in code or mark it explicitly as a point-in-time/process doc — but do NOT fabricate code cites.)
