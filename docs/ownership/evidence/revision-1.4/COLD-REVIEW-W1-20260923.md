# W1 independent reference review — 2026-09-23

Reviewer: `/root/cold_review_exact_contracts` (agent; separate from artifact author). Scope: the three `REFERENCE.md` files below, current source at campaign HEAD `3df52eb71acdd4e00084d42f0b64191674bf42eb`, and `docs/ownership/STANDARD.md` §6. No Cargo build, test, runtime, or deployment action was performed. No skill, blast-radius, or maintenance verdict is implied. The review context was inherited from the campaign, so this record is an independent source check but must not be presented as a fresh-context final approval.

| Package | Reviewed SHA-256 of `REFERENCE.md` | Profile; lines / words / bytes | Verdict |
|---|---|---|---|
| `corelink-adapter-host` | `e745a55c080d5dc82f3a6a178992cdacf243ce355408f2b3aba91efe438ba439` | H; 118 / 1,953 / 17,469 | **FIX_FIRST** |
| `corelink-rate-headers` | `247fd0a63747718f2fad9bd545bbc8245a3b2aa71c0a5096b77ee0cfb26e8b69` | S; 99 / 948 / 8,933 | **FIX_FIRST** |
| `corelink-reapi` | `f1800db858720aaf6fa22045e868a6d0afc74cdf9fde5a648a5d38a527ae284c` | H; 101 / 1,073 / 12,745 | **FIX_FIRST** |

The document checker returned `IMPLEMENTED_CHECKS_PASS` for each file with its declared profile and `--root .`. Sizes fit the candidate caps. The checker explicitly does not certify semantic completeness, profile eligibility, runtime claims, or cold review. Source files under `corelink-adapter-host` and `corelink-reapi` have no diff from their documents' pinned `source_commit`; `corelink-rate-headers` has a later manifest repository-field edit, described below.

## Findings requiring repair

1. **All three — R01–R08 section contract is unmet.** `STANDARD.md` §6 requires R01 identity, R02 boundaries, R03 implementation, R04 contracts, R05 state/flows/invariants, R06 configuration/targets, R07 failures/observability, and R08 verification/evidence. `adapter-host` follows this only through R04; its R05–R08 cover auth, upstream, integrity, and unknowns. `rate-headers` and `reapi` use R01–R08 as eight invariant headings. The information is partly present in other places, but the required navigation and several explicit dimensions (configuration/default/activation, state ownership and lifetime, failure/observability routing) cannot be recovered reliably from the labeled sections. Reorganize the existing evidence under the required sections and fill material gaps; a heading-only rename would not suffice. Evidence: each reviewed document's R01–R08 headings; `STANDARD.md:206-230`.
2. **`corelink-adapter-host` — OCI quota behavior omitted from the public contract.** API-009 lists `storage_cap_bytes: Option<i64>` on `BlobStore::finalize_upload`, while its prose says finalize persists. The actual `OciBlobBridge::finalize_upload` parameter is `_storage_cap_bytes` and is ignored; the constructed `CasWriteRequest` receives no quota value. The reference flags absent upload-session tenant checks, but not this separate quota propagation gap. State the port's intended quota contract and the concrete bridge's current behavior as distinct, with a falsifiable invariant/current status and relation to tenant storage policy. Evidence: `crates/corelink-adapter-host/src/oci/ports.rs:82-109`, `src/oci/bridge.rs:121-155`, `REFERENCE.md:72,85`.
3. **`corelink-rate-headers` — stale current-manifest claim.** R01 says the *current* manifest's `repository` is `https://github.com/HuGR-dev/corelink-server`; current `crates/corelink-rate-headers/Cargo.toml:9` says `https://github.com/HuGR-Labs/corelink-server`. Its pinned source commit predates that metadata edit, but the wording asserts present state. Update to the current value or explicitly label historical evidence. Evidence: `git diff 38f43b6d6b7f461edbd3a9bdd5cff34f2fd64018..HEAD -- crates/corelink-rate-headers/Cargo.toml`.

## Verified source matches and bounds

- `adapter-host`: cargo/brew/npm/pip port traits and capability defaults match API-001–008; OCI `BlobStore`, `ManifestKvStore`, `ManifestResolver`, and `TenantResolver` method shapes match API-009–011; shared SSRF helpers match API-012. The upload tenant defect recorded in INV-005 is real: `OciBlobBridge` stores `HashMap<String, Vec<u8>>` and accepts `_tenant` for append/finalize/cancel. H eligibility is plausible from five adapter families and 65 source files, but a semantic-module count was not certified.
- `rate-headers`: API-001–007 method and return shapes, argument order, builder clamps, 60-second global-circuit values, circuit trait methods, embedded schema constant, and pure signal helpers match inspected source. Manifest declares the two stated test targets. No HTTP emission or D1 application was inferred.
- `reapi`: API-001–008 principal signatures and described source paths match inspected orchestrator, read, find-missing, PAT, capability, and audit code. The document correctly distinguishes a best-effort orphan callback from deletion: current `R2DeleteReconciler` logs and defers physical deletion. `host-server` is the default feature in the manifest, but no selected build or route reachability was verified. H eligibility appears plausible from the source-module inventory, but was not certified by a semantic count.

Next review must use changed-byte hashes and recheck the three findings. These FIX_FIRST verdicts are not approvals of the references or the other three artifact families.
