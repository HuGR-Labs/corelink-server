# Final independent cold review — CO-1 standard (2026-09-23)

**Verdict: FIX_FIRST.** Review scope was the current checkout bytes at `3df52eb71acdd4e00084d42f0b64191674bf42eb`, not prior review conclusions. The standard is not ready to freeze or use as a publication prerequisite. This review did not change code, package artifacts, the registry, or remote state.

## Evidence that passes

- The metadata-only exception is fail-closed for document bytes: `ownership_gate.py` lines 89–129 parses both frontmatters, removes only the four permitted identity lines, and compares every remaining byte. Lines 199–225 require a hashed `REVIEW` snapshot, a hashed current artifact, a `REVIEW` readback, and the exact changed-field set. The schema requires the snapshot and readback fields; the regression suite covers semantic/body and nested-mapping changes.
- Confirmed issue publication requires a hashed issue snapshot, intended-body hash, marker, repository ID, canonical repository and `number`-matching `/issues/<number>` URL (`publication_gate.py` lines 92–113; `generate_registry.py` lines 102–147). With the correct repository root, a fresh in-memory build reports 105 packages, structural/integrity `PASS`, cold review `UNVERIFIED`, publication `NOT_PUBLISHED`, and calibration `PASS`. No review-record directory exists in this checkout, so no package is being represented as cold-approved.
- The five calibration rows in `PILOT-CALIBRATION-READBACK-20260923-CURRENT.md` contain profile, measured lines/words/bytes, population, overflow, and capacity decision. `calibration_errors()` compares reported metrics against the current four files for each pilot and returned no errors. This certifies capacity measurements only.
- Cold checks: `python3 -m unittest discover -s docs/ownership/tests -q` passed 147 tests; `python3 docs/ownership/tests/adversarial_probe.py` met 13/13 synthetic expectations.

## Required fixes

1. **P1 — Candidate issue identity is not fail-closed.** `publication_gate.py` lines 66–84 checks a matched issue's repository URL but never checks that its URL path is `/issues/<number>`. A complete synthetic snapshot containing `number: 7`, URL `https://github.com/o/r/issues/8`, a matching marker and passing prerequisites returned `REUSE_OR_RECONCILE` with that inconsistent pair. Require positive integer `number` and exact canonical issue URL for every matched candidate before reuse or reconciliation; add a regression test. The later readback rejects this pair, but the preflight already presents it as a reusable issue.
2. **P1 — Registry review state skips the Git baseline check.** `generate_registry.py` lines 85–97 calls `record_errors()` and can label a record `REVIEW_EVIDENCE_CONSISTENT`; `record_errors()` checks current source hashes but does not prove that those bytes existed at `source_commit`. Only `ownership_gate.py` CLI lines 380–390 performs `merge-base` and `git show <source_commit>:<path>` checks, and the registry does not call that path. Reuse the baseline validation in registry review-state derivation and test a record whose current hashes match but whose declared Git baseline does not.
3. **P1 — Registry CLI has an unsafe default root.** `generate_registry.py` line 17 sets `ROOT = TOOLS.parent.parent`, which resolves to `<repo>/docs`, while `--root` defaults to `ROOT` at line 270. Calling `build(ROOT, ...)` produced population `0` and `STALE` calibration; calling it with the repository root produced 105 and `PASS`. `main()` writes JSON and Markdown even when calibration is stale, then returns 1. Correct the root and fail before writing outputs when population/calibration validation fails; test the CLI without `--root`.
4. **P1 — Freeze/current-main pins are stale.** `STANDARD.md` lines 503–509 calls `MAIN-DRIFT-READBACK-20260923-CD74.md` the latest remote readback, while the current evidence includes `MAIN-DRIFT-READBACK-20260923-E0F.md`. At this review, local `origin/main` was `a18d1146ac6cea79ef36ff56c00682a869060460`, a descendant of `e0f231110524fe81ed0b8d3451903879daf9d519`, while `registry.json` and `index.md` still claimed observed main `e0f2311`. The E0F readback itself marks affected packages and current-main reconciliation blocked. Refresh the readback and generated pin against the actually observed main; reconcile affected source/manifest evidence before a freeze or issue publication. Preserve the separate `UNVERIFIED` and `NOT_PUBLISHED` states.

These findings block contract freeze. The byte comparator, exact publication readback, and calibration checks can remain as implemented while the four gaps are repaired and independently rechecked.
