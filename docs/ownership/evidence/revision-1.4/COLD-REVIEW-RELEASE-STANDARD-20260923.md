# CO-1 release-standard cold review — 2026-09-23

**Verdict: FIX_FIRST.** The candidate standard and tools are substantially safer, but these exact bytes are not ready to freeze. This is a review of the current checkout at `3df52eb71acdd4e00084d42f0b64191674bf42eb`; no earlier cold-review report was used as an oracle. It does not approve a package, publish an issue, or attest a merge to `main`.

## Reproduced evidence

- `python3 -B -m unittest discover -s docs/ownership/tests -p 'test_*.py' -q` passed **149 tests**. The adversarial documentation probe met **13/13** expectations.
- The default registry root resolves to this checkout. A direct `build(root, live_main)` returned **105** packages, all `structural=PASS`, all `cold_review=UNVERIFIED`, **0** published, and `calibration=PASS`. The generator's invalid-root and stale-calibration tests verified refusal before either output file was changed. The current five-pilot calibration table matches measured artifact bytes; its capacity decisions do not claim semantic approval.
- Source-baseline review paths use an ancestor test and compare each declared source hash to `git show <source_commit>:<path>` before admitting a record. The existing registry has no package review records to promote. The ledger has **105 BLOCKED** items, all with `contract.frozen=false`, and the generated registry accordingly reports `NOT_PUBLISHED` for all 105.
- Synthetic issue identity probes: a valid fixture yielded `ELIGIBLE_FOR_SERIAL_CREATE`; a different contract repository, a different matched-issue repository, or a mismatched issue number/URL yielded `BLOCKED`. No issue write was attempted.
- `git ls-remote origin refs/heads/main` and local `origin/main` both returned `3721a9b7121c472c573823774d59194239d4e250`. GitHub returned canonical `HuGR-dev/corelink-server`, numeric repository ID `1232040291`; the old `HuGR-Labs/corelink-server` name redirects to that repository. The diff from the prior `a18d1146` pin to `3721a9b7` contains **17 workflow files** and no Cargo manifest, lockfile, Rust source, or ownership-document files. Existing older package source pins still require their separately stated reconciliation.

## Required fixes before freeze

1. **Bind the contract URL to the actual final standard.** `publication_gate.py` checks a GitHub URL's shape and repository, but not that the SHA exists or that its path is the frozen `docs/ownership/STANDARD.md`. A synthetic URL ending in `/blob/` + 40 `f` characters + `/DOES-NOT-EXIST.md` returned `ELIGIBLE_FOR_SERIAL_CREATE`. The current `STANDARD.md` SHA-256 is `c58f517b14ff609fd19de2cdcb48d4e82f664f6f6c4196bccbde07e8fa2984ec`; it differs from the bytes committed at campaign HEAD (`0938c5fe44f2b60d75956a47eaee4c0f12601b97cbc4573989203f8a415d076f`), and the path is absent from current `main`. Neither of those commits identifies these final working-tree bytes. Verify the path and commit/blob against the published standard, then pin its URL after the bytes are committed. See `docs/ownership/tools/publication_gate.py:52-58` and `docs/ownership/STANDARD.md:495-497`.
2. **Correct canonical repository identity across the issuance path.** The live canonical name is `HuGR-dev/corelink-server`, while all 105 ledger rows and the `render_issue.py`/`publication_gate.py` examples in `tools/README.md` use `HuGR-Labs/corelink-server`. The new exact URL checks correctly reject a canonical `HuGR-dev` issue URL when the expected name is the old alias. Reconcile ledger, examples, drafts and future frozen links to the current canonical name without changing the stable numeric repository ID or manifest markers. See `docs/ownership/plans/publication-ledger.json:13-25`, `docs/ownership/tools/publication_gate.py:35-41,69-74,107-114`, and `docs/ownership/tools/README.md:81,125`.
3. **Refresh and verify the current-main pin.** `STANDARD.md`, `registry.json` and `index.md` still state `a18d1146ac6cea79ef36ff56c00682a869060460`, while current main is `3721a9b7...`. The generator accepts any 40-hex string as `--observed-main`; the tests deliberately generate a registry with `aaaa...`, so its output does not itself prove a live pin. Reconcile the workflow-only delta, record a current readback, and make pin provenance checkable before regenerating the registry/index. See `docs/ownership/STANDARD.md:504-510`, `docs/ownership/registry.json:3`, `docs/ownership/index.md:3`, and `docs/ownership/tools/generate_registry.py:287-304`.
4. **Rebind final framework evidence to final bytes.** `changed-framework-files.json` has seven stale `current_sha256` values: `STANDARD.md`, `test_publication.py`, `test_registry.py`, `test_revision.py`, `generate_registry.py`, `ownership_gate.py`, and `publication_gate.py`. For example, it declares `12c4e3a4...` for the standard while the file is `c58f517b...`. Refresh the exact-byte inventory after all fixes and repeat this cold gate on that final set. See `docs/ownership/evidence/revision-1.4/changed-framework-files.json:3-14`.

The green tests and capacity measurements support the implementation, but none resolves these identity and final-byte gaps. Publication remains independently blocked by the ledger's pending prerequisites and absent frozen contract.
