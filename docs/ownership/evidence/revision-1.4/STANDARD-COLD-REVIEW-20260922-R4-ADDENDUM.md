# Standard cold-review R4 addendum — 2026-09-22

This addendum records evidence produced after R4 without changing the frozen
candidate bytes (`STANDARD.md` SHA-256
`e9b9c8ae77d3f12eff6955a466aa62453051a1217243db23dd68bc7a616a74d8`).

- The current five-pilot review wave is now recorded. `corelink-server` has a
  fresh four-artifact `APPROVE` set at the current-main source pin;
  `corelink-hash` has `APPROVE` for SKILL, REFERENCE and MAINTENANCE, but its
  BLAST_RADIUS remains `BLOCKED` because 42/42 peer states are unreconciled.
- The current calibration readback measures all five byte sets after the last
  artifact change and keeps them within S/H caps.
- These updates do not close G1 or G3: the standard is still not frozen,
  package contracts and authority are not approved, and local/runtime
  procedures remain unexecuted. G2 remains globally blocked with per-item gates
  pending and no publication authorization.

Verdict remains **BLOCKED**. This is an evidence addendum, not an approval.
