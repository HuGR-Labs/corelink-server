# Independent cold review — final package set (2026-09-23)

Review time: 2026-09-23 20:03 UTC. Checkout: `/private/tmp/corelink-ownership-campaign`, `HEAD=3df52eb71acdd4e00084d42f0b64191674bf42eb`. This review used the current artifact bytes, the cited source/manifests, `STANDARD.md` and `COMMON.md`; it did not read earlier review reports. No Cargo, checker, Rust test, network, or runtime operation was run. This Markdown review is not a `package-record.schema.json` record or evidence of an executed procedure.

The document caps were checked with `wc -l -w -c`, counting whitespace-separated words and UTF-8 bytes. All eleven files fit their declared S/H limits; the adapter-host H profile has a source-listed 42 semantic modules, and server H has 1,199 lines/10,351 words/95,319 bytes. REAPI reference is close to the S word cap at 2,976/3,000. These measurements do not establish semantic completeness. `git diff <source pin> HEAD --` found no tracked source change in the selected handler/customer, container/customer, adapter-host, or REAPI paths. Rate-headers differs in Cargo package metadata and README, which its reference calls out; its Rust source did not differ.

## Per-artifact verdicts on the bytes reviewed

`APPROVE` below means the reviewed source-scoped content passed this bounded cold reading. It does not promote the registry/index to `APPROVED`, resolve peer ownership, certify execution, or replace a schema-valid review record. `FIX_FIRST` identifies a required document correction. `BLOCKED` identifies acceptance evidence outside the authorized review scope.

| Artifact | SHA-256 of reviewed bytes | Verdict | Reason / required action |
|---|---|---|---|
| handler-customer `SKILL.md` | `ac41a678ff7a12379963775e95ba14e5979c642b88389f950295d411b2c62b3e` | APPROVE | S01/S04 activate for trait, fake, audit/SLI changes; S01/S06 exclude HTTP mount, D1, Worker, billing/identity and production actions. S03 routes to R/B/M and transverse OKF/built-not-wired skills. Source-wired native route is distinguished from shipped/runtime evidence. |
| handler-customer `REFERENCE.md` | `dc3cfc68be92e6463c30b662140511c1dc773a07007eea857e087b2661488b25` | APPROVE | Eleven trait methods, fake maps, audit ordering, partial mutation, tenant guard, SLI and D1/native route boundary are source-scoped. R03/R04/R05 lead from entry to method, state and failure records; no real customer or persistent sink is inferred. |
| handler-customer `BLAST_RADIUS.md` | `ccd37fad9b00543b8193f5cf3783584e9843d26050718d0e6717553bdedf14cb` | FIX_FIRST | B02's declared search paths omit `src/request/*`, `routes/customer/part-00-01.rs`, and `part-01.rs`, although B03 relies on their DTO and eleven HTTP call sites. Reconcile the stated discovery population and B06's 43=43+0 claim. REL-032 also explicitly lacks the SLO peer key/hash, while customer method peers remain UNAPPROVED. |
| handler-customer `MAINTENANCE.md` | `a961532b64175bd1dd98aecbeda8607d4ccf3594ff3a1a3ad4a8dee610cdd2be` | FIX_FIRST | PROC-003 says `EXECUTED_LOCAL` for four checkers and diff-check, but HC-PROC003 gives one 19:51–19:54 time range and summarized JSON fields, not each exact command's dated exit/output and diff scope as its own evidence rule requires. Attach per-command literal records or return PROC-003 to `REVIEWED_NOT_EXECUTED`. PROC-002's isolated selective-failure fixture and all Rust gates are also unexecuted and cannot be certified here. |
| server `BLAST_RADIUS.md` | `23ccbdb9564b1b468aeee21ca62da5962f27d898efccdeff50c8d8dea7348c28` | BLOCKED | Scoped REL-033 and REL-088–098 pass reciprocal key/hash/call-site reading, but the full 1,199-line server blast was outside this assigned review and cannot receive an artifact-wide approval here. These relations themselves say `SOURCE-PAIRED / UNAPPROVED` pending bilateral review. |
| adapter-host `REFERENCE.md` | `4290f79504229902e847ed4dfa899f2610dd0297a9977ac2f0d05bc32453a7e5` | APPROVE | Public family ports/routers, configuration, upstream literal-IP guard, and H eligibility are source-scoped. OCI upload tenant and storage-cap defects are accurately identified as violated INV-005/007, not claimed as fixed or tested. |
| adapter-host `BLAST_RADIUS.md` | `2c56060000ea90bbb26b2522af80bf879883c5c771ff2c8a746d9bab48596a94` | FIX_FIRST | REL-035–040 call their `repo:...` stable keys “Shared fingerprint” but provide no independent source-anchored contract digest. The same issue applies to REL-018/042; container peer is pending. Add a reproducible source-set fingerprint alongside the key and reconcile the peer records. The 21 inverse source files in B02 match a fresh literal `rg -l` census; the OCI defect propagation is explicit. |
| rate-headers `REFERENCE.md` | `030d5ed114ab3fd07f63b13182d5c9778db019a1ee050d7c54799fedacad1fee` | APPROVE | R04/R05 match the source's quantized 10-ID half-open sampler, two-of-three trip rule, manual-override SLI flag, audit-before-transition and process-local state. R01 distinguishes the newer Cargo repository metadata from the pinned Rust source. Current manifest identity still needs incorporation into the package evidence record before publication. |
| REAPI `REFERENCE.md` | `df87613913629f7fa9e8b5ad4292b18ff83bf90c956b09f497bf085e034d287f` | FIX_FIRST | API-014/015/016 point at blast REL-003 (`MetaStore::commit_put` write/outbox) for poisoning/cross-tenant/read-completed builders. The builders are source declarations; read-completed emitters use structured tracing after stream completion, and R05 itself says they do not stage outbox rows. Replace these relation links with the actual audit emission/source boundary, or add the missing relation. Write outbox UUID PK and `(request_id,event_type)` dedup are otherwise correctly separated. |
| REAPI `BLAST_RADIUS.md` | `4787617b6017e967278dc183ff819903a57abd1886183c7c1ca769defb72ae75` | FIX_FIRST | REL-017/020–024 have reciprocal adapter-host keys but no source-anchored contract digest; worker/meta backlinks remain pending for REL-002/003/025–027. B02 excludes `uuid` and `serde_json` as generic dependencies although the document's REL-028/003 and reference R05 make UUIDv7 audit identity and serialized JSON material boundaries. Reclassify/document them and update the population equation. Audit trace relation noted above is missing. |
| REAPI `MAINTENANCE.md` | `463fbdf4f1383826441f6b18fea67555d14cac289126aca73b202eaa4ff6bf77` | BLOCKED | PROC-006 is correctly marked `required_for_acceptance=true` and `REVIEWED_NOT_EXECUTED`; M04 gives exact documentation commands and predicates, but no checkout-specific per-command execution evidence exists in these bytes. An authorized executor must run and record them. Cargo rows remain accurately unexecuted; M05 distinguishes a fresh-R2/failed-meta orphan from rollback and avoids deleting duplicates. |

## Cross-file checks and release blockers

- Navigation sample: handler skill S03 → reference R04 → API-010; skill S03 → blast B03 → REL-031; skill S03 → maintenance M03 → PROC-002. Each takes at most three links, and the long records link back to their index. Server B03 indexes REL-088–098; adapter and REAPI B03 index their reviewed relation cards.
- Customer package REL-036 ↔ server REL-033 share `repo:1232040291:boundary:customer-server-package-001` and SHA-256 `0e27a188...13b2`. Its stated Git blob inputs resolve at the cited pins; recomputing the documented UTF-8 formula reproduces that digest. Customer method REL-001 ↔ server REL-088 likewise share the overview key and SHA-256 `f7decbc6...9886`; its handler, D1 and HTTP Git blob inputs resolve and recompute. All eleven method key/hash strings and peer anchors match between the two current documents. These checks establish byte/identity agreement, not semantic or runtime approval.
- The adapter-host ↔ REAPI manifest pair and five PAT family pairs have matching key strings and plausible source call sites. A stable key is an identity, not a source fingerprint under `COMMON.md#relacoes`; neither document supplies source blob set plus digest. The customer ↔ SLO key is local only. The REAPI ↔ worker/meta and adapter-host ↔ container peers are expressly pending.
- `COMMON.md#procedimentos` requires the selected local diagnostic/validation procedure to have execution evidence before acceptance. The reviewer was instructed not to run Cargo or runtime commands and did not run the documentary checkers. PROC-006 therefore remains blocked; PROC-003 needs its claimed evidence repaired or status lowered. No test, provider state, route serving, deployment, or production result follows from this review.
- There are no `docs/ownership/records/` package records in this checkout. Formal four-artifact approval, reviewer provenance, source-set hashes, procedure status reconciliation, and generated `REVIEW_EVIDENCE_CONSISTENT` state remain unavailable. The current index shows these packages as `UNVERIFIED`/`NOT_PUBLISHED`; this report does not change that status.
