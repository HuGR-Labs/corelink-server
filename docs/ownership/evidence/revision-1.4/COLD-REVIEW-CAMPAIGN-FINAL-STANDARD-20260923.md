# Independent cold review — campaign standard, current bytes — 2026-09-23

**Verdict: FIX_FIRST.** Reviewed the current worktree at `HEAD=3df52eb71acdd4e00084d42f0b64191674bf42eb` at 20:03 UTC. This is a standard/framework verdict, not four-artifact approval for any package or authority to freeze or publish. I read the current standard, schema, gates, generator, registry/index, ledger, and current evidence; I did not use earlier cold-review reports. No implementation, issue, or GitHub state was changed.

## Reproduced evidence

- `STANDARD.md` current SHA-256 is `cdd52d6a6ade272f126be37da3be83df905df09e2c17a1ec5648592c315759a6`. The 12 paths in `changed-framework-files.json` match their recorded current SHA-256 values. `git diff --check` passes.
- `python3 -m unittest discover -s docs/ownership/tests -v`: **154 tests, OK**. The expected `BLOCKED: calibration readback is stale: stale fixture` line is from a negative test, not a suite failure. `python3 docs/ownership/tests/adversarial_probe.py`: **13/13 expected, zero false acceptances**. These exercise documentary fixtures, not package runtime.
- `git rev-parse origin/main` and live `git ls-remote origin refs/heads/main` both returned `7f966dda8234f54b58766f33e5fca2ad02cc898b`. GitHub REST returned canonical `HuGR-dev/corelink-server`, numeric repository ID `1232040291`, default branch `main`; all 105 ledger rows use that name and ID.
- `generate_registry.py` succeeded both with its default root and with `--root /private/tmp/corelink-ownership-campaign`, using that main SHA and temporary output paths. Both regenerated JSON and Markdown outputs are byte-identical to `docs/ownership/registry.json` and `index.md`. Result: 105 packages, 105 structural `PASS`, calibration `PASS`, 105 cold-review `UNVERIFIED`, 105 `NOT_PUBLISHED`, publication count 0. Ledger: 105 `BLOCKED`, zero frozen contracts, zero issue IDs. There is **no current false `APPROVE` or `PUBLISHED` state**.
- The publication preflight requires the exact `docs/ownership/STANDARD.md` URL path and a commit on live `origin/main` containing the current bytes. Direct checks rejected the wrong path, `7f966dda...` because `STANDARD.md` does not exist at that main commit, and local `HEAD` because its committed standard SHA-256 is `0938c5fe44f2b60d75956a47eaee4c0f12601b97cbc4573989203f8a415d076f`, different from current worktree bytes. `git ls-tree origin/main docs/ownership/STANDARD.md` is empty. The candidate therefore has **no valid frozen publication commit**.

## Required fixes and residual gates

1. **P1 — Review records do not bind to the canonical standard.** `package-record.schema.json` permits any nonempty `standard.version` and `standard.path`; `ownership_gate.record_errors` checks that the named file has the supplied hash but does not require `docs/ownership/STANDARD.md`, the active version, or a frozen commit. I reproduced this in the existing synthetic control: the valid record returned `[]` errors, then a record with `standard.path=docs/ownership/ALTERNATE.md` (same bytes) and `standard.version=not-the-candidate` also returned `[]`. This can yield `REVIEW_EVIDENCE_CONSISTENT` for a review of the wrong policy. Bind actual review records to the canonical path and version, with a reviewed SHA and immutable commit when frozen; add a negative test for wrong path/version.
2. **P1 — Published state is not cross-checked against release prerequisites.** `generate_registry.publication_states` derives `PUBLISHED` from a ledger `CONFIRMED` row and matching issue readback, without rechecking its frozen standard URL or six preflight gates. `test_registry.py::test_publication_requires_exact_readback` explicitly accepts a synthetic `PUBLISHED` row lacking those fields. Current rows are all `BLOCKED`, so this does not create a false current state, but it conflicts with the standard's requirement that publication derive from review and checks. Either require the prerequisite evidence when producing `PUBLISHED`, or explicitly distinguish verified issue existence from compliant campaign publication in the index and contract.
3. **Release remains blocked by external state and current-main reconciliation.** Publish the exact reviewed standard bytes to canonical main and rerun the live path/SHA/commit preflight before freezing a URL. The candidate standard itself says package SOURCE/manifest/workspace/lock drift still needs reconciliation. The current registry has no accepted per-package review records, and the ledger has no frozen contract or eligible row. The 105-decision dedup record and Cargo census are preparation evidence; their earlier main/snapshot pins do not certify issue emission at the live main readback.

The standard's capacity, source-boundary, authority, per-artifact review, and fail-closed publication rules are present and the reproduced mechanical suite is green. The two framework gaps above prevent approval of these bytes as the final standard. After fixes, rerun a fresh cold review against the new standard SHA and verify the published main commit and package evidence separately.
