# Cold review — final package bytes after postfix — 2026-09-23

## Scope and method

Independent source-first review of the current package docs and skills in this checkout (`HEAD=3df52eb71acdd4e00084d42f0b64191674bf42eb`), for `corelink-hash`, `corelink-billing`, `corelink-server`, `corelink-cf-bindings`, `e2e-billing-flow`, `corelink-handler-customer`, `corelink-adapter-host`, `corelink-rate-headers`, and `corelink-reapi`. Read the standard, package files, relevant manifests/source, and current census evidence directly. Prior cold-review reports were not used as approval evidence.

Ran `check_docs.py` against all 36 package artifacts with their declared S/H profiles: all returned `IMPLEMENTED_CHECKS_PASS`. This checks documented structure and size only; the checker expressly does not certify semantic completeness, external links, cross-file three-click navigation, profile eligibility, or cold review. No Cargo, Rust, or runtime command was run.

## Per-package verdicts

| Package | Verdict | Evidence and exact blockers |
|---|---|---|
| `corelink-hash` | **FIX-FIRST** | The 43 REL entries are individually anchored and B06 states the bounded population and unknowns. Source identity is explicitly SOURCE-only at `cacc44f`. However, B06 claims a 43-row structured relation ledger, but no such ledger artifact is present in the checked-in ownership tree; only prose readbacks refer to it. Also REL-037–042 pair to server REL-082–087 at `b9b3ee8`/`91630ba`, while the latest `origin/main` server package differs extensively from that pin (see server row). Refresh or explicitly stale-mark these current-consumer relations before freeze. The docs disclose `peer_review=not_reconciled`, so no bilateral approval is inferred. |
| `corelink-billing` | **APPROVE — source-scoped** | The façade and implementation contracts distinguish reexports from owned modules; the 27 direct relations are individually listed. The current docs explicitly record the quota-CAS p99 change from `<5 ms` to `≤5 ms` and state that it was not executed. The artifact remains anchored at `cca798f`; its SOURCE and non-execution limits are clear. No package-specific blocker found for this pinned source snapshot. |
| `corelink-server` | **FIX-FIRST** | The docs are internally navigable and the customer method mirror is split into 11 endpoint relations with reciprocal handler links and per-method fingerprints. The source pin is `91630ba`, while fetched `origin/main=e0f231110524fe81ed0b8d3451903879daf9d519` changes 60 files under `crates/corelink-container`, including `src/lib.rs`, `src/reapi_ingress.rs`, and tests. The manual does not establish that the current-main package is covered by its pinned relation/method census. Reanchor to a selected current source commit or explicitly stale-mark the package and affected relations before freeze. |
| `corelink-cf-bindings` | **FIX-FIRST** | Source pin `cca798f` matches the reviewed package source. The reference groups materially different public contracts in API-011 (`create_multipart_upload` vs `upload_part`) and API-012 (`complete_multipart_upload` vs `abort_multipart_upload`); signatures, return values, and effects differ, so these are not one same-contract family under STANDARD §6. Split those contracts while preserving their exact evidence. Other direct boundaries are separately recorded; no runtime binding claim is made. |
| `e2e-billing-flow` | **APPROVE — source-scoped** | The harness is correctly described as a test package with fakes/in-memory state, and its eight direct relations distinguish test contracts from production reachability. Source pin `cca798f` has no package-tree delta in the fetched `origin/main` comparison. Procedures are presented as instructions; execution is not claimed. No package-specific blocker found. |
| `corelink-handler-customer` | **FIX-FIRST** | The 11 trait methods are atomic and individually paired to server REL-088–098; the docs mark those pairs `SOURCE-PAIRED / UNAPPROVED` and separate D1 selection/runtime unknowns. The reciprocal server package fingerprint is computed at `91630ba`, whose package tree is stale against current `origin/main` (see server row). Reconcile this peer package/route view after the server reanchor before treating the pair as current. |
| `corelink-adapter-host` | **APPROVE — source-scoped** | The W1 reference contains exact observable contracts, while the blast map gives per-surface relations and source-set fingerprint recipes. The recipe explicitly separates stable shared keys from fingerprints and labels unresolved peer confirmation as UNKNOWN/UNAPPROVED. The pinned package tree has no delta against fetched `origin/main`; no upstream, DNS, runtime, or deployment claim is promoted. No package-specific blocker found. |
| `corelink-rate-headers` | **APPROVE — source-scoped** | The revised reference records exact signatures, units, return/error behavior, half-open quantization, and observer failure ordering against source locations. Relations are separated by header, circuit, audit, metric, migration, and test/consumer boundaries. Its pinned package source has no relevant source delta against fetched `origin/main`; tests and HTTP/provider/runtime behavior remain explicitly unexecuted/unknown. No package-specific blocker found. |
| `corelink-reapi` | **APPROVE — source-scoped** | The revised reference gives distinct write/read/discovery/auth/audit contracts. The blast map lists direct boundaries and reproducible source-set inputs for the five adapter PAT calls, while explicitly withholding peer approval and runtime claims. The package tree at `6ed297f` has no delta against fetched `origin/main`; no package-specific blocker found. |

## Cross-cutting gate findings

- The independent manifest census reports 107 tracked `Cargo.toml` paths and 106 package identities, with explicit workspace/test/fuzz/archive/root classifications. It also states that the registry `observed_main` anchor is stale (`91630ba` vs fetched `origin/main=e0f2311`). Refresh that generated anchor before freeze/publication; do not treat the census as a current-main approval until then.
- The reviewed maintenance guides mark source checks separately from execution evidence. No procedure output was treated as run merely because the docs prescribe a command. The hash, billing, server, and REAPI manuals all keep their unexecuted gates visible.
- Cross-file anchor resolution, external URLs, and the complete three-click path rule remain unverified; `check_docs.py` reports these outside its certification scope.

## Verdict

**FIX-FIRST for freeze/publication:** repair the four package blockers above (`corelink-hash`, `corelink-server`, `corelink-cf-bindings`, `corelink-handler-customer`) and refresh the stale registry census anchor. The other five package suites pass this source-scoped review for their declared immutable pins. This verdict does not approve runtime behavior, Cargo resolution, tests, deployment, ownership assignment, or issue publication.
