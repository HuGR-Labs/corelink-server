# Release-package cold review — 2026-09-23

Independent review of current worktree bytes at `3df52eb71acdd4e00084d42f0b64191674bf42eb`, 19:37 UTC. I read `STANDARD.md` and `COMMON.md`, the ten named artifacts, current manifests and relevant Rust source. I did not read prior review reports, run Cargo, exercise runtime paths, or edit package artifacts. All ten `check_docs.py` calls returned exit 0, `IMPLEMENTED_CHECKS_PASS`, and no errors; that checker expressly excludes semantic completeness, external links, cold review, and three-click navigation. Source paths cited below are in this checkout. The handler-customer, adapter-host, and REAPI source paths inspected have no worktree diff from their frontmatter pins; the rate-headers manifest has metadata drift that its reference explicitly distinguishes from the source pin.

| Artifact | SHA-256 of reviewed bytes | Verdict | Release-gate reason |
|---|---|---|---|
| handler-customer `SKILL.md` | `ac41a678ff7a12379963775e95ba14e5979c642b88389f950295d411b2c62b3e` | **APPROVE** | S01–S07, source-only authority, negative triggers and routed evidence are present. |
| handler-customer `REFERENCE.md` | `dc3cfc68be92e6463c30b662140511c1dc773a07007eea857e087b2661488b25` | **APPROVE** | R01–R08, method/DTO and fake state contracts, native source wiring, and runtime unknowns agree with inspected source. |
| handler-customer `BLAST_RADIUS.md` | `8305876c13121e541c826b45b7f72f0333d35c0039a5440be4c8cf6c64f0800b` | **BLOCKED** | It explicitly leaves 11 method peers plus SLO/server package identities/fingerprints unapproved; bilateral closure required by COMMON is absent. See HC-1. |
| handler-customer `MAINTENANCE.md` | `207d2badc3ac789ac1f47ea3f0c77e4e54ec330ad372b9fb986c9f3326846c66` | **FIX_FIRST** | PROC-002's selective `Committed` failure exercise cannot be performed with the named in-memory sink as written. See HC-2. |
| adapter-host `REFERENCE.md` | `e7b5c88c094d3d1caddd1ae5c24f162b8b8df8cdf75c18131757c17b163cf3c9` | **FIX_FIRST** | API and INV rows lack per-record explicit anchors/index navigation; some API rows group distinct contracts. See AH-1. |
| adapter-host `BLAST_RADIUS.md` | `3032a66bd67fcfa8fe1cdcd78d8d892a6aac6a4c672bdc7a8a457b7ca1ab600d` | **FIX_FIRST** | B01–B06 have the wrong section roles, no population reconciliation, and omit a source-proven reverse consumer. See AH-2. |
| rate-headers `REFERENCE.md` | `11e2c3b4732d995edb7ca869a8423df205fdf191dd4137077fc7c64574bbbb5b` | **FIX_FIRST** | Eleven API anchors are all placed before the table, so an API link does not land on its contract row. See RH-1. |
| REAPI `REFERENCE.md` | `ebeb65f0fb57d2a1cc6b82f8350c38f5b7b0d3ea103e9cf08b3579764e616ae7` | **FIX_FIRST** | The material audit ID/PK derivation and batch-slot distinction are missing from R04/R05. See RA-1. |
| REAPI `BLAST_RADIUS.md` | `7e417fe33b8ab169cbcd8b6f3fc9387de47e9315b82b51165d40bf706ebf8428` | **FIX_FIRST** | The registry-dependency exclusion hides the material BLAKE3 audit-ID seam; worker/meta bilateral peers remain pending. See RA-2. |
| REAPI `MAINTENANCE.md` | `adb0374be07c82f3754ebfc5e046b268a9f34e29b6754a32fbf7c3fbe7a48028` | **FIX_FIRST** | PROC-006 claims execution without the per-command literal output, checkout identity and environment required for a reproducible procedure record. See RA-3. |

## Findings requiring action

**HC-1 — handler-customer blast, bilateral closure and navigation.** B03/B06 correctly distinguish the 11 method keys from the server package edge and label all 13 internal bilateral relations `UNAPPROVED`; they cannot be promoted on the current bytes. Reconcile REL-001–011 with method peers in the server/container document and REL-032/036 with qualified identity and source-anchored contract fingerprint, then re-review. The local navigation also needs correction: REL-012–022 backlink to `#b02`, REL-039/040 to `#b04`, and REL-032–038 to `#b06`, although their relation indexes are in B03. STANDARD §3 requires a record to return to its corresponding index. Source confirms the fake guard and mutation branches (`crates/corelink-handler-customer/src/handler.rs:426-939`) and native route merge (`crates/corelink-container/src/routes/build.rs:329-390`); runtime remains unknown as the document states.

**HC-2 — handler-customer maintenance, failure exercise.** PROC-002 says to use an in-memory sink and inject failure at `Committed`, then clear it and inspect the mutated row. `InMemoryAuditSink::inject_failure` makes **every subsequent** `emit` fail (`crates/corelink-handler-customer/src/audit.rs:190-265`). The fake emits `Attempted` before each selected mutation (`src/handler.rs:630-894`), so that sink fails before the row changes. Specify a selective test sink/fixture and exact assertion, or select an existing test that actually produces the post-mutation failure. Set explicit current `required_for_acceptance` values for PROC-001/002; their present conditional wording does not give a definite per-PROC value. The M05 code/data recovery distinction is otherwise sound; no local mutation test was run in this review.

**AH-1 — adapter-host reference, atomic navigation.** R04's API-001–017 appear only as unanchored table text and R05's INV-001–007 likewise lack explicit anchors/index entries. A link to `#api-009` or `#inv-005` cannot reach its record. Place stable anchors at each record and link them from indexes. Split contracts with different inputs/effects/errors where needed, for example API-009 combines OCI open, append, finalize and cancel. The source-observed OCI defects are accurate: `src/oci/bridge.rs:95-164` ignores `_tenant` for session operations and `_storage_cap_bytes` at final CAS write; `src/oci/push/upload.rs:257-384` finalizes before digest/size validation. The 42-module inventory supports H eligibility.

**AH-2 — adapter-host blast, census and roles.** STANDARD §7 requires B01 scope, B02 method/population, B03 direct relations, B04 transitive propagation, B05 change/impact/validation, and B06 coverage/unknowns. This file instead uses B01–B06 as topic buckets; it has no discovered/documented/excluded/unknown equations, no inverse Cargo resolution plan, and no transitive witness matrix. `crates/corelink-container/Cargo.toml:200` declares this package and `src/routes/{cargo/part-00,brew,npm,pip,oci}.rs` import its APIs, yet no REL captures that reverse package edge and five composition routes. REL-002 and REL-008 also describe the same Cargo CAS bridge/call sites without an atomic distinction. Rebuild the census and relation topology, then reconcile shared fingerprints to source-backed contract identities. The five adapter PAT backlinks to REAPI do match by key, but those matching strings alone do not close the broader graph.

**RH-1 — rate-headers reference, direct record links.** R04 puts `<a id="api-001">` through `<a id="api-011">` on one line above the table. Each link therefore lands at the same position, not the relevant row; move each anchor into its row or make a short record with its own anchor. The inspected `src/{headers,circuit,lib}.rs` supports the documented half-open quantization, process-local state, observer failure distinction, and static embedded migration. Current manifest repository metadata is separately identified as post-pin; no D1 application or HTTP emission is claimed.

**RA-1 — REAPI reference, audit identity.** R05 says `AuditEvent.id` is handler-supplied, but omits how that ID is determined at the handler boundary. `src/handler/audit_emit.rs:24-62` hashes `(client_request_id, tenant, digest)` with BLAKE3 and stamps UUIDv7 bits; `src/handler/audit_emit_batch.rs:20-53` adds `slot_idx` under a distinct domain for duplicate batch-read slots. `src/handler/per_blob.rs:118-133` and `src/handler/bytestream.rs:367-387` feed the non-slot ID into writes. This ID is the outbox primary-key/idempotency boundary; document its input, slot behavior, collision/failure implications and validation alongside the separate `(request_id,event_type)` dedup key. Existing R05 write/read order and R06 feature descriptions agree with source, and provider/runtime effects remain explicitly unknown.

**RA-2 — REAPI blast, population and peers.** B02 excludes all eight normal registry dependencies as generic, but `blake3` is used by the material audit-ID boundary above; the exclusion reason does not reconcile that effect. Add the audit-ID/PK relation and adjust the population equation. B06 openly says worker R2 REL-002/025 and meta REL-003/026/027 lack peer backlinks; do not call those bilateral contracts approved until peers and source-anchored fingerprints match. The adapter-host REL-017/020–024 keys do have reciprocal text; that check does not establish runtime or provider behavior.

**RA-3 — REAPI maintenance, procedure evidence.** PROC-006 and M04 report five documentary exit codes and verdict summaries but omit checkout SHA, exact execution environment/time per command, and literal checker stdout/stderr. COMMON's PROC record and STANDARD §8 require those fields for `EXECUTED_LOCAL`; either attach the evidence or mark execution as reviewed, unexecuted. M05's orphan/R2 versus D1/outbox recovery boundary is accurate to `src/orchestrator.rs:271-349` and `R2DeleteReconciler`'s warning-only implementation. Cargo targets remain clearly `REVIEWED_NOT_EXECUTED`, which this review did not change.

## Navigation and authority challenge

Starting from the handler-customer skill, a trait edit routes S03 → reference R04 → API row and S03 → blast B03 → REL; a fake-audit change routes S04 → reference R05 and blast B03; a native-route claim routes S04 → R03/B03 with `implemented` and source `wired` distinguished from `runtime_verified`. Route mounting and live billing alone are negative triggers. The skill refuses customer-data access and deployment. This meets its positive/negative routing and two authority-refusal challenge on current bytes. The handler reference's API rows and invariant anchors are directly reachable. The adapter and rate-reference anchor findings and handler blast backlinks are the navigation failures above.

No APPROVE here certifies deployed behavior or overrides the open bilateral and source/runtime boundaries. Semantic edits after these hashes require a new cold review under STANDARD §9.
