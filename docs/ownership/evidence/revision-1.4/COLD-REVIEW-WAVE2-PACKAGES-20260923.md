# Wave 2 package artifacts — independent cold review (2026-09-23)

Reviewer context: fresh agent review of the seven paths below against the current checkout at `3df52eb71acdd4e00084d42f0b64191674bf42eb` and `STANDARD.md` SHA-256 `12c4e3a4f9c446881abdbc6e92d94fd31c8fab385a9b2bd3194d68feab5838cc`. I read source, manifests, `COMMON.md`, and relevant peer relation anchors; I did not read prior review reports or author explanations. No Cargo, runtime, network, or package-file edits. Each artifact passed `check_docs.py` with `IMPLEMENTED_CHECKS_PASS`; that checker expressly excludes semantic completeness, cross-file navigation, external links, runtime claims, and cold review. These are substantive verdicts on the frozen bytes, not operational certification.

| Artifact | SHA-256 | Lines / words / bytes | Verdict |
|---|---|---:|---|
| `.claude/skills/own-corelink-handler-customer/SKILL.md` | `1eaa81e5aa87a7dd56b18d48891f5c09f61b34378917be91908cf61ccb88767f` | 87 / 671 / 5,684 | **FIX_FIRST** |
| `corelink-handler-customer/REFERENCE.md` | `93da1354d699b4474f1d9edcd622194af250c56cbcd96133c439670364ff7d58` | 162 / 2,025 / 18,414 | **FIX_FIRST** |
| `corelink-handler-customer/BLAST_RADIUS.md` | `5a839197f47304fc076d9670da50667cf45631749541d5c3187fb0172952e96c` | 234 / 1,929 / 20,193 | **FIX_FIRST** |
| `corelink-handler-customer/MAINTENANCE.md` | `629c03b567420d056b6b514fa20031de25c38fdb5047526c00855585945c948d` | 123 / 1,057 / 8,994 | **FIX_FIRST** |
| `corelink-adapter-host/REFERENCE.md` | `f9dec351a543a0af585fa3b3f523a118edf71c0be92be6c5188a48945e1dea49` | 129 / 2,180 / 19,328 | **FIX_FIRST** |
| `corelink-rate-headers/REFERENCE.md` | `458a053883f5abd10ddc92fade870189d045207b8ea89580d8ffc5b1fb6ceadd` | 109 / 1,114 / 10,159 | **FIX_FIRST** |
| `corelink-reapi/REFERENCE.md` | `1e3cbfa1554d527b0bdd10418a0ae62691fd327438ca2e5de4e71a785d162081` | 102 / 1,156 / 11,325 | **FIX_FIRST** |

All seven are below their declared line, word, and byte caps. The adapter source has 65 `.rs` files under `src/`, supporting H-profile scale; the REAPI source has 23, but its reference does not present the semantic-module inventory required to justify H. No capacity overflow was found. `FIX_FIRST` means required corrections and re-review of new bytes under STANDARD §9; no artifact is approved by this report.

## 1. Customer skill — FIX_FIRST

- S01 provides two positive trigger classes, but no task-level negative triggers. S02/S06 state authority limits; they do not tell a reader when a CoreLink task should *not activate* this skill. STANDARD §5 requires positive and negative triggers. The required three positive and two negative cold navigation challenges cannot be completed from explicit trigger rules.
- S07 asks for baseline, symbols, documentary results and unknowns, but omits the required change-packet fields **objective, selected gates, residual risk, and next responsible owner** (STANDARD §5). S05/S06 also blanket-stop at Cargo compilation/tests while M04 presents them as applicable validation. The skill should route an authorized isolated code-validation task to the correct gate and owner, with separate execution status; its current instruction makes that handoff ambiguous.
- The `okf-context` and `built-not-wired` links in S03 are present and appropriately distinguish source, wiring, and runtime. A navigation challenge from S03 reaches R04/B03/M04, but the target B/M section IDs currently have different meanings from STANDARD §§7–8, so the cross-file route is not yet reliable.

## 2. Customer reference — FIX_FIRST

- Required R01–R08 anchors exist, and API-001–024 plus INV-001–002 give useful source-specific local contracts. The fake's `query` applies `event_types` but not `since` (`src/handler.rs:895-939`), and `remove` reports zero PAT revocations (`src/handler.rs:834-894`); the reference states both limits accurately.
- R03 is titled “Superfície pública declarada” and R04 is titled “Audit, SLI e ordem local,” with the API contracts nested in R04. STANDARD §6 requires R03 **implementation map** and R04 **contracts**. The implementation map does not distinguish the container's D1 implementation, route factory, and composition path from the local fake in a navigable R03 record. Correct the section assignment and keep ownership of external implementations explicit.
- R08 says route selection remains unknown. Source permits a narrower classification: `crates/corelink-container/src/routes/build.rs:329-390` builds customer state, installs the native PAT gate, and unconditionally merges `customer::router(customer_state)`; `src/main.rs:389` calls the builder. The *source-declared native container route is wired*, conditional on selecting that binary/build path; shipped artifact, deployed configuration, external Worker forwarding, and traffic remain unknown. This also conflicts with peer `corelink-server/BLAST_RADIUS.md#rel-033`, which records the source mount. Record `implemented`, `wired`, and `runtime_verified` separately with scopes and evidence as STANDARD §4.2 requires. Source `routes/build.rs` SHA-256: `e9507394c43d8a996b9d40d532bfb66cbe6e8689db0324be70506cd92ed75668`; `main.rs`: `e63700401a2f53fbacbe029d97b03bd562aaebef18baf33ac13d070ce85f015f`.

## 3. Customer blast radius — FIX_FIRST

- B01–B06 anchors exist, but their contents are organized as exported methods, tenant denials, fake mutations, SLI, DTOs, and consumers. STANDARD §7 assigns B01 scope, B02 method/population, B03 direct relations, B04 transitive propagation, B05 change/impact/validation, and B06 coverage/unknowns. There is no B04 transitively reachable-node/path table or B05 change-impact matrix. Move the existing RELs under the required section roles.
- REL-001–011 label a boundary from this crate's trait methods to `D1CustomerHandler` as `reexport`. The `lib.rs` reexport is a local API exposure, while server trait implementation/invocation and Cargo dependency are different relations. The proposed arrow key is not the stable qualified shared key required by COMMON §“Relações”; fichas omit independent dependency/data/impact directions and do not reconcile per-method peers on the server side. `server REL-033` is only a package-level reciprocal edge, as B01 itself acknowledges. Resolve the atomic keys and backlinks instead of treating package reciprocity as method reciprocity.
- B06 inventories three normal dependencies, one repeated dev dependency, one reverse manifest consumer, and two local test files. It omits source-discovered semantic consumer/route edges in `routes/customer/part-00.rs:255-279` and `routes/build.rs:329-390`, the D1-vs-fake selection and native PAT gate. Its blanket “mount unknown” claim obscures the source-declared merge. The independent source search found these paths via `corelink_handler_customer`, `CustomerRouteState`, and `customer::router`; add them to discovered/documented/excluded/unknown counts and distinguish external Worker/deployment evidence.
- The 40 local REL anchors are navigable and the `corelink-slo REL-016` and `server REL-033` links resolve, but there is no complete population equation (`discovered = documented + excluded`) by STANDARD §7.5. Structural checker success does not close this gap.

## 4. Customer maintenance — FIX_FIRST

- M01–M06 anchors exist, but M02 contains a change case, M03 a guard case, M04 tests, M05 adjacent integration/recovery, and M06 recording. STANDARD §8 requires M02 **selection**, M03 **procedures**, M04 **tests**, M05 **recovery/compatibility**, M06 **escalation/record**. PROC-001–003 sit after M06 and their backlinks point to M02, so the skill's M03 link does not reach the procedure index in the expected section.
- M01–M06 use `STATIC_LOCAL` as “Modo,” whereas STANDARD §8 defines `READ_ONLY`, `LOCAL_ISOLATED`, or `AUTHORIZED_OPERATION` for procedures. The later PROC records use `READ_ONLY`, but PROC-002 step 5 calls for clearing an injected failure and checking live fake state; that is a local mutation and belongs under `LOCAL_ISOLATED` with inputs, expected state, failure stop, and recovery. PROC-003's checker procedure is read-only.
- The M04 table names exact Cargo targets and predicates but records them unexecuted, while the opening paragraph and skill S06 prohibit Cargo/tests. COMMON §“Procedimentos” requires applicable local diagnostic/validation procedures marked `required_for_acceptance` to execute before approval. This review did not run Cargo by assignment; the manual must clearly separate documentary checks already executable from isolated tests awaiting an authorized executor and keep execution evidence per PROC. The partial fake recovery note is useful but needs an executable local stop/recovery predicate and N/N-1/consumer compatibility handling for API changes.

## 5. Adapter-host reference — FIX_FIRST

- R01–R08 anchors and API-001–012/INV-001–007 exist. The two highlighted OCI gaps are source-real: `src/oci/bridge.rs:95-164` stores only `Vec<u8>` per upload UUID, ignores tenant in append/finalize/cancel, ignores `_storage_cap_bytes`, and calls `CasWriteRequest::new` without a quota cap (source SHA-256 `d1b093dd8e9bf258564959d93ec888915ec5fc621e72a444c79146e12faa4c47`). The reference appropriately marks INV-005 and INV-007 violated instead of claiming runtime behavior.
- R04 omits material exported HTTP composition contracts despite R02 describing this as an HTTP-facing package: Cargo `build_router(config)->Router`, Brew `build_router(config)->Result<Router,...>`, npm/pip `build_router(state)->Router`, and OCI `router(state)->Router` (`src/{cargo,brew,npm,pip,oci}/server.rs`). These differ in inputs, error behavior, activation and failure and require separate API records or explicit justified exclusions under STANDARD §6. R03's family map and R06's source-only caveat do not replace the contract entries.
- API-003 writes `BottleService::fetch(&self,&str,&str)` without its `async` boundary (`src/brew/bottle.rs:207-211`); several port entries similarly omit async in “exact symbols.” Record accurate signatures and effects. The 65 source `.rs` files support H eligibility, but the document should state the counted semantic-module inventory rather than rely on a raw file count. No Cargo or wire compatibility was tested.

## 6. Rate-headers reference — FIX_FIRST

- R01–R08 anchors and API-001–007 map the main builder/circuit interfaces. Source confirms the `remaining.min(limit)` and retry ceiling in `src/headers.rs:369-396`, and the migration is embedded but not applied. The manifest-only repository metadata drift is acknowledged separately.
- R03/R05/R07 provide “Atomic predicate” tables without `INV-ID`, enforcement point, violation, verification, and current-state fields together. STANDARD §6 makes that tuple mandatory for each invariant. For example, the “trip-predicate” text says one breached signal is alarm-only, but does not identify a specific test or current observed state; the header-bound and fake-seam predicates likewise lack IDs/backlinks. Add bounded INV records and connect API rows to their IDs.
- API-007 says `allow_halfopen_request` takes a percentage capped at 100, but the source quantizes the result through `(observation_id % 10) < cap / 10` (`src/circuit.rs:237-241`, SHA-256 `056848d8ba75d36c6db325c0436b6f5699754eace0412a38347cd67a4794ee79`). The contract should state the 10-slot granularity and effective rate, since values such as 9 yield 0%, 19 yield 10%, and 99 yield 90%; a reader changing sampling otherwise cannot predict behavior from the documented input alone.

## 7. REAPI reference — FIX_FIRST

- R01–R08 anchors and API-001–008 contain accurate major static claims: BLAKE3/4 MiB/5 MiB values, size-aware find-missing slot flags, and R2-before-D1 write order were found at the cited source paths. The document separates source evidence from deployment appropriately.
- R03's implementation map says only `handler`, `proto`, and `http_read` are gated by `host-server`. Current `src/lib.rs:73-138` also gates `timing_padding_wiring` and `worker_adapter` and reexports padding layers. Public `cas_get_router`, `cas_get_router_with_padding`, `CasWriteService`, `CapabilitiesService`, `ByteStreamService`, and padding-layer constructors are not represented in R04 API records. Their feature condition, inputs, output and failure behavior matter to transport owners. Update the map and contracts against `src/lib.rs` SHA-256 `5d5b4f84db9127cceadcd06b7a01de0e23923162129ae135a4e8e8a6c8bb306a` and the named modules.
- R05 has source-falsifiable prose but no `INV-ID` records with enforcement, failure, test/verification, and current state per STANDARD §6. The H declaration also lacks an inventory proving >20 **semantic** modules; 23 `.rs` source files alone do not prove that criterion. Either document the inventory or select S, which fits the current 102-line/1,156-word/11,325-byte reference.

## Verification boundary and next gate

I reran seven path-scoped documentary checkers; each returned zero errors and `IMPLEMENTED_CHECKS_PASS`. I inspected the cited source and reciprocal relation anchors, and measured exact current artifact bytes. I did not run the STANDARD §9 manual diagnostic/recovery challenge, Cargo tests, selected-target builds, deployment probes, or a complete resolved/inverse Cargo graph. New substantive bytes require an independent re-review per artifact; source wiring still needs a selected-artifact and deployment readback before any runtime claim.
