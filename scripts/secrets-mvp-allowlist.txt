# secrets-mvp-allowlist.txt — Wave 32 Phase D APPLY MVP-mode allowlist.
#
# put-secrets-prod.sh --mvp-only reads this list. Only these secrets are
# REQUIRED for the MVP deploy (Phase D APPLY won't fail on others — they
# downgrade to WARN). Per user mandate 2026-05-26: solopreneur MVP doesn't
# need Slack/Twilio/SendGrid (Resend instead)/HubSpot/Cookiebot/Drata/Neon×5
# /AWS-Azure-GCP BYOK at provision-time. Those are per-feature lazy.
#
# Re-classify as MVP only when the corresponding feature is wired into the
# customer path AND the operator has the secret value ready.
#
# Format: one env var name per line. Comments start with `#`.

# ── Cloudflare (3) ──
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
CLOUDFLARE_ZONE_ID_HUMANGR

# ── Clerk auth (2) ──
CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY

# ── Stripe billing (3 — webhook secret deferred to post-Phase-E) ──
STRIPE_SECRET_KEY
STRIPE_AUTH_MODE
STRIPE_PRICE_ID_STARTER

# ── Status page (BetterStack — Phase A inheritance) ──
BETTERSTACK_API_TOKEN
BETTERSTACK_PAGE_ID

# ── PagerDuty primary alerts ──
PAGERDUTY_ROUTING_KEY

# ── Email (Resend) ──
RESEND_API_KEY

# ── HMAC / internal signing (4 — openssl-generated, no vendor) ──
HUGR_AUDIT_CHAIN_HMAC_KEY
HUGR_PAT_SIGNING_KEY
HUGR_SESSION_HMAC_KEY
HUGR_OCI_TOKEN_KEY
